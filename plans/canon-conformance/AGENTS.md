# canon-conformance — plan memory

_Plan-scoped semantic sink (dream-routed): open threads · findings · frontier for this initiative.
Net-current; the runtime (task folders + PLAN.md) is authority, this is the reconstruction aid._

## Frontier (dispatch first)

`ready/` = **E1 acceptance-harness · E2 projection-boundary** (wave 0) — they gate the S1–S5 fan-out. Validate
every cell via `toolkit/cold-oracle` at **BLIND = priors-only** (fresh `/tmp`, no corpus/memory/root reads).

## Doc-audit findings — S5 targets (focused pass 2026-07-05, not exhaustive)

- The four root docs (`VISION/MODEL/ENGINE/CANON`) were **orphaned** (0 inbound refs). Handoff wired root
  `AGENTS.md` + `agent-anatomy/AGENTS.md` to them; S5 verifies the full wiring + regenerates human views.
- `docs/agent-conceptual-anatomy.md` declares a **rival "source of truth"** (the TS contract / 24-organ union,
  old STANCE–CONATUS framing, not `accept()`/5-Kinds) — and is a hand-authored mirror (projection-as-source).
  Reconcile: subordinate to `MODEL` or regenerate; drop the source-of-truth claim.
- Stale: `packages/agent-anatomy/{GLOSSARY,DESIGN}.md` (demolished idea-typology era) — verify vs current model, retire if dead.
- `ideas/` layer: its INVARIANT ⊆ `MODEL` — retire; relocate the `render:verbatim` genus bodies to cell homes.
- Civic-vocab residue on ρ=LLM surfaces → re-signify (`founders→maintainers · society→fleet · polis→workspace ·
oikos→package`); code identifiers + the repo name are Mav's gated infra refactor (rename ripples imports/paths).

## Notes

- `standing-oracle-gate` (was RTB, warm-cold-acceptance tail) is folded into **E1** — the enforced pre-land
  boundary; instrument at `packages/agent-anatomy/src/toolkit/cold-oracle/`.
- The killed `resignify-nico-provenance-charter` (7b742fe) proves the failure mode to avoid: never re-fuse a
  decoupled organ (Autonomy's exercise back into Provenance) — the `mav-archetype-green` bleed is the live instance (S1).
