# S1-organ-catalog

**canon-conformance** · **wave 1** · **Deps** ⊳`E1-acceptance-harness` · **Lane** Nico

## Inputs

- static: `packages/agent-anatomy/src/organs/` (159 value cells) · `MODEL.md`
- ⊳dep: `E1-acceptance-harness` (the gate)

## Objective

Every organ-value cell `accept()`s. Per cell: prose→σ\* (composite → formal; primitive → `⟨α,residue⟩`); depalimpsest
every `[[ ]]` → bare formal symbol; re-signify bespoke/civic anchors — decompose cold-misfiring fusions
(`principal-ic` → `(decision-authority ↾ individual-contribution) @ principal ⟨intrinsic⟩`); one home per concept.

## Method

Fan out per organ (24). **Pattern-prover first:** the principal-IC standing — triplicated + drifting across
`provenance/{nico-archetype-cyan, mav-archetype-green, principal-ic-root-red}`; `mav` restates the _exercise_
(`extract·serve·intent, never order-taker`) = `human-on-the-loop` content it already selects → cede to autonomy
(PARTITIONED de-bleed). Then each cell: pipeline stages, `verify` via the BLIND (priors-only) cold-oracle.

## Acceptance (falsifier)

- FAIL if any cell's `core` fails BLIND decode to its `intent` (priors-only — corpus not loaded).
- FAIL if any definiens retains prose, a `[[ ]]`, or a bespoke/civic anchor.
- FAIL if a concept is homed in more than one cell (PARTITIONED).

## Gate

SOURCE `.ts` edits in-remit; push GATED (Operator).

## Return

Per-organ diffs + the blind-oracle transcript per touched cell + the concept→home index showing single homes.
