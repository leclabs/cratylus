# S5-framing-and-projections

**canon-conformance** · **wave 1** · **Deps** ⊳`E1-acceptance-harness`, ⊳`E2-projection-boundary` · **Lane** Nico

## Inputs

- static: `README.md` (done) · `AGENTS.md` · `docs/` · `packages/agent-anatomy/ideas/` ·
  `VISION.md`/`MODEL.md`/`ENGINE.md`/`CANON.md` · `packages/agent-anatomy/{GLOSSARY,DESIGN}.md`
- ⊳dep: `E1-acceptance-harness`, `E2-projection-boundary`

## Objective

Wire the four root docs as the sole authority; retire the rival "source of truth" (`docs/agent-conceptual-anatomy.md`
declares "the contract is the TypeScript… 24-organ union") and the `ideas/` layer (its INVARIANT ⊆ MODEL; relocate the
`render:verbatim` genus bodies to their cell homes); regenerate every human view via `project-human`; re-signify
residual civic vocabulary on ρ=LLM surfaces (`founders→maintainers · society→fleet · polis→workspace · oikos→package`);
retire stale `GLOSSARY.md` / `DESIGN.md`.

## Acceptance (falsifier)

- FAIL if any doc asserts a source-of-truth other than `MODEL`/`CANON`, or the four root docs remain orphaned (no wiring).
- FAIL if any human view is hand-authored rather than `project-human` output.
- FAIL if retiring `ideas/` drops load-bearing content (genus relocated; INVARIANT ⊆ MODEL proven).
- FAIL if a civic-romantic anchor remains on a ρ=LLM surface.

## Gate

Doc + source edits; push GATED (Operator) — touches the top-level narrative + operator-facing docs.

## Return

Aligned docs + wired authority + the reconciled/retired competing-model docs + regeneration proof for human views.
