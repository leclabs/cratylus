# autonomy-dry-homing

**Lane** Nico · **Status** pending (a deliberate, separately-landed + deployed change — it reprojects
every SOUL).

The mission-command substance (own the _how_ · serve intent over literal words · never defer competent
judgment · escalate only a genuine fork) has no doctrinal home in the `autonomy/human-on-the-loop`
value cell (its definiens today is the thin oversight sense only). Ground truth (grep, 2026-07-02),
where the doctrine actually lives:

- `packages/agent-anatomy/AGENTS.md` (@nico section — "mission-command authority (Auftragstaktik …)")
- `src/organs/provenance/{nico-archetype-cyan,mav-archetype-green,principal-ic-root-red}.ts` +
  `src/organs/provenance/README.md` — each restates the intent-driven-expert doctrine inline

NOT leaks: `src/skills/carry-on.ts` (absorbs mission-command by the skill self-sufficiency law) and
`ideas/persona.md` (carries the per-turn countermeasure, not the doctrine).

## Scope

Home the doctrine in `src/organs/autonomy/human-on-the-loop.ts` (definiens extended; blind σ\*\_R check
on the wording — THE ORGAN INVARIANT). Reduce the leak sites: provenance fragments keep each agent's
identity claim, referencing rather than restating the doctrine; the pkg AGENTS.md @nico section cites
the cell. Land + fleet deploy in one motion; content-verify.

## Accept (falsifiers)

- The doctrine's load-bearing phrases grep to ONE doctrinal home (the value cell); identity claims and
  the skill-absorbed copy permitted, restatement not.
- Reader-density + projection-stability + null-organ gates green; SOULs reproject with the reduction;
  fleet content-verified.
