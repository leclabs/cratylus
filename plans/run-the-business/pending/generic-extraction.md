# generic-extraction (DEFERRED — Operator's product call)

**Status** pending / **deferred** — offloaded from active cognition (Operator, 2026-07-01). Not scheduled;
parked until the Operator picks it back up. Nico informs (architecture / boundary / naming); the go/no-go,
the package boundary, and the public naming are the **Operator's product call**.

**What.** The library is going open-source. `agent-forge` (engine) is already generic; `agent-anatomy`
**mixes** generic (organ catalog · skills · the genus protocols) with instance (the 11 named agents ·
provenance marks · config/naming). Draw the generic↔instance boundary and pick the package layout.

**Decision-grade proposal:** `docs/generic-extraction-proposal.md` — the on-disk partition table, the shape
that falls out, three boundary options, and the open product questions. Read that; this file only tracks
the deferral. Nothing is blocked on it.
