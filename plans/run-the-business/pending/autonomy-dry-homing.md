# autonomy-dry-homing

**Lane** Nico · **Status** pending (extracted from the completed `p4-stance-protocol-tail`; a
deliberate, separately-landed + deployed change — it reprojects every SOUL).

The mission-command substance (own the _how_ · serve intent over literal words · never defer competent
judgment · escalate only a genuine fork) is under-expressed in the `autonomy/human-on-the-loop` value
cell and leaks into the README gloss + provenance fragments + the Persona Protocol. Homing it in the
value cell is DRY-correct: one canonical home, the leak sites reduce to references/echoes.

## Scope

`packages/agent-anatomy/src/organs/autonomy/human-on-the-loop.ts` (the one home) + reduction of the
leak sites (README gloss · provenance fragments · `ideas/persona.md` — keep only what each surface
constitutively needs: provenance keeps the identity claim, the Persona Protocol keeps the per-turn
countermeasure, neither restates the doctrine). Blind σ\*\_R check on any rewording (THE ORGAN
INVARIANT). Land + fleet deploy in one motion; content-verify.

## Accept (falsifiers)

- The mission-command substance appears ONCE (the value cell); a grep for its load-bearing phrases
  finds no second doctrinal home (identity/countermeasure references permitted, restatement not).
- Reader-density + projection-stability + null-organ gates green; SOULs reproject with the reduction;
  fleet content-verified.
