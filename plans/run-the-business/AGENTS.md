# run-the-business — charter

**What this is.** The perpetual plan: the live backlog + standalone tasks that don't warrant their own
sharded initiative. It never retires (contrast [[plan-retirement]], which governs the transient initiative
scaffolds). Sweep `completed/` periodically; git is the recovery net.

**Founder boundary.** Nico owns constitution/roles/archetypes (the society); Mav owns
infrastructure/machinery (the substrate). `principal-ic` is intrinsic to both. A task here carries its own
owner; cross-boundary tasks name both leads.

## Standing initiative context

Most current tasks are the live tail of the **memory-tool-bundling** initiative (retired as a plan once its
first two tasks landed on main; the remaining work lives here). The load-bearing context:

- **Goal of that work.** Make a skill's runtime arm travel with the skill — the `episodic` runtime is a
  bundled companion asset of the `memory` organ, deployed to every fleet host, so the memory rituals have
  their tool wherever they run, with zero repo dependency on the host.
- **Founder split.** Mav: the companion-asset deploy capability, the `episodic` build/bundle, the
  dual-deploy machinery, the fleet cutover. Nico: the cell-structure ruling (dir-form), the `memory.md`
  Protocol edit naming the affordance, the `wake.md` migrate-trigger edit, signify/anchors.
- **Acceptance discipline.** Golden master (skills + agent SOULs deploy byte-identical except the
  deliberately-scoped change — enumerate any hash delta) · CE ∧ ME on any cell edit (Nico's gate) ·
  `episodic`'s atomic `compact` + two-leg `assertNoLoss` stay green · `verify.py` PASS, verbatim-organ
  regression clean. Re-sync onto `origin/main` before each execution phase.

`vault-reference-home` is from the **memory-model-redesign** initiative (the 5th memory home — cold,
on-demand); lower priority, self-contained in its task file.
