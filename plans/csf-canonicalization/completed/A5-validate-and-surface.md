# A5 — Correctly-named validate + two-audience surface

**Intent.** Mint the `valid`-applying process under its true name (from A1's blind test). Settle the surface: atoms = developer/agent-internal; `elicit` = layman door. Producers are pure reads; namer/realizer/validator commit (purity axis, not prohibition).

**Acceptance.** The validate-process cell lands under its canonical name; the surface (which atoms are user-facing vs internal) is documented; purity axis stated as prescription.

**Depends on.** A1, A3.

**Owner.** Nico.
