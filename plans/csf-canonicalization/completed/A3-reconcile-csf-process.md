# A3 — Reconcile process cells to the reference CSF formalization

**Intent.** Expose the operator's CSF formalization cleanly as named ops: `resolve · semanticPartition · depalimpsest · distill(primitive ∨ deepestFaithfulComposite) · canonical_anchor(=signum_aptissimum) · coalescence(merge same-anchor units)`. Reconcile conceptualize/signify/materialize/exemplify to it; program each to the `concept` contract (no sibling-naming).

**Acceptance.** Process cells expose every CSF op; exemplify = uniform composition gated by the (renamed) validate; verify PASS; blind-equivalence to the reference block.

**Depends on.** A1, A2.

**Owner.** Nico.
