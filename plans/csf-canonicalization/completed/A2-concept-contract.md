# A2 — Define the `concept` contract (the interface)

**Intent.** Elevate `concept` to a first-class contract every module programs to: the data type `⟨ gloss/meaning , anchor? , factorization? ⟩`. Producers emit it; consumers take it. This is the narrow waist that lets modules stop naming each other (H4 decoupling).

**Acceptance.** A cell defining the `concept` contract (kind: concept/structure), with the shared vocabulary (distinctions `D_R`, lattice `C_R`, gloss, anchor, factorization) homed here — not borrowed cross-cell.

**Depends on.** —

**Owner.** Nico.
