# A4 — Fold `probe` into `conceptualize` (verify blind)

**Intent.** A signifier is a degenerate source; `probe` = conceptualize-on-a-name + a naming-experiment use. Verify blind that conceptualize subsumes probe, then retire probe (un-deploy), keeping the experiment-with-candidate-anchors as a use, not a cell.

**Acceptance.** Blind test confirms subsumption; probe cell removed; refs fixed; conceptualize covers the signifier input; redeploy.

**Depends on.** A1.

**Owner.** Nico.
