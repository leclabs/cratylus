# A1 — Blind-test every atom name → canonical anchor

**Intent.** For each machinery process (`conceptualize`, `probe`, `elicit`, `signify`, `materialize`, `exemplify`, and the unnamed `validate` + CSF sub-ops `depalimpsest`/`distill`/`semanticPartition`/`coalescence`), hand a blind subagent the concept (no current name) and have it signify from first principles. Produce a divergence report: current name vs blind canonical anchor.

**Acceptance.** A table of {process, current anchor, blind anchor, verdict}. Confirms/repairs each atom's name. `validate` in particular re-named to what it actually does ("validate what?").

**Depends on.** — (read-only)

**Owner.** Nico.
