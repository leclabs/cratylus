# csf-canonicalization — plan context

Lead: **Nico** (corpus/anchors/process cells); **Mav** on projection-machinery (C2) + deploy (B2).

Method = the blind-signify audit proven this session: lock-my-answer-first, then a fresh
`general-purpose` subagent runs the identical prompt; divergence = contamination/non-canonical.
The same gate applies to atom names (A1) and the whole corpus (B1).

Self-application: `dogfooding` (the cell currently mis-anchored `self-application-is-mandatory`) is the
acceptance discipline — the corrected machine must re-derive the corpus equivalent-or-better.
