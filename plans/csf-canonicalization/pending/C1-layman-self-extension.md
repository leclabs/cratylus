# C1 — Layman self-extension flow

**Intent.** Design the product surface: a non-engineer's intent → `elicit` → CSF → recompose into cleanly-separated, canonically-named domain skills → compose agents-as-persons (the principal/sovereign/ambient/continual/self-aware stack already in the corpus). polis extends itself on the operator's behalf.

**Acceptance.** A design doc + the entry-point skill(s) a layman uses; the end-to-end flow demonstrated on one toy domain.

**Depends on.** A, B.

**Owner.** Nico.
