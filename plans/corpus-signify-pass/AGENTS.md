# corpus-signify-pass — charter

**What.** Run [[signifier-star-r]] (σ\*\_R) over the **entire** definition set. Many fragments still
carry far too much prose, and several drifted into communicating the **same idea** poorly — because
they were authored before σ\*\_R existed. Harvest every `[[wikilink]]` reference + its meaning into one
analyzable set, find the clusters that say the same thing, **[[elicit]]** the Operator on the genuine
candidates, then re-run [[signify]] to coalesce, re-anchor, and strip each fragment to minimal prose
([[precise-circumscription]]). Origin: Operator, 2026-06-20.

**The named target.** The concept **`principal-self`** (principal-ic / principal-agency / sovereign)
is muddled — which is _why_ neither Mav nor Nico embodies it properly (Mav executes instead of
challenging). σ\*\_R it to a sharp, adoptable concept. This is the highest-value cluster.

**Founder split.** **Nico — corpus LEAD:** owns [[signify]]/[[anchor-routing]], the coalescing and
re-anchoring, CE ∧ ME. **Mav — principal-ic + tooling:** the harvest tool, the duplicate-cluster
detector, drives the `/elicit` interviews with the Operator, the reconstruction/byte-identity gates.

**Method.** `/elicit` (binary-search the Operator's intended concept) on each candidate cluster
_before_ re-signifying — recover what the Operator actually means, don't guess. `/signify` assigns
σ\*\_R; coalesce concepts that resolve to the same anchor.

**Acceptance discipline.** CE ∧ ME on every touched cell · `verify.py` PASS (incl. reconstruction
R1/R2/R3) · byte-identity on the rendered fleet EXCEPT the deliberately-scoped anchor/prose changes
(enumerate them) · every coalescence is move-not-copy (one home survives).
