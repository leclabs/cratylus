# signify-star-r-pass

**Objective.** Run `/signify` (σ\*\_R) over the resolved concepts: assign each its fittest anchor,
**coalesce** fragments that resolve to the same anchor (move-not-copy), and strip every touched cell
to minimal prose composited from anchors. Nico leads.

**Preconditions.** `elicit/elicit-candidates` produced the recovered concepts (incl. `principal-self`).

**Operations.** Per cluster: assign σ\*\_R anchor; merge coalescing members into the one surviving
home; re-point inbound refs; depalimpsest the prose to minimal. Apply `principal-self` first and
reconcile `mav`/`nico` archetypes to adopt it. `resolve→glossary→verify` after each batch.

**Artifacts.** touched cells across `packages/mind/`, updated `GLOSSARY.md`.

**Acceptance (blind test).** Each coalesced concept has exactly one home; `verify.py` PASS; the
`principal-self` concept reconstructs from its anchor alone and the founder archetypes cite it.
