# repair-memory-trio

R=LLM. (the extra work `/exemplify` can't infer — de-palimpsest vs runtime + mint — then exemplify.)

obj ≜ reconcile {wake, dream, embedded-in-wake} vs runtime truth, mint missing skills, then
`/exemplify` the cleaned set.

dep ≜ ∅ (ready; ∥ exemplify-skills). pre ≜ read `references/kinds-and-invariants.md`; ground truth ≜
`~/.claude/skills/memory/episodic.mjs` (verb set V).

ops:

1. read `episodic.mjs` → enumerate V.
2. dream: fix script ref so `/dream` invokes real episodic verbs (¬palimpsest).
3. wake: pull each inline-embedded skill → own `packages/mind/skills/{α}.md`; wake references by name.
4. ∀ v ∈ V without a skill → mint `packages/mind/skills/{α}.md` (α=σ\*\_R).
5. `/exemplify` cleaned wake, dream, each new/extracted skill (D=file; R=LLM; s=file).

art → cleaned wake.md/dream.md + minted skills under `packages/mind/skills/`; canonical fragments +
manifests.

acc (blind) ⊨ `/dream`→real runtime; wake = refs only; ∀ v∈V ↔ exactly 1 skill; exemplify accept=valid
on the cleaned set.
