# exemplify-skills

R=LLM.

obj ≜ `/exemplify` the independent skill corpus → canonical `packages/mind/{kind}/{α}.md` + manifests.

dep ≜ ∅ (ready; ∥ repair-memory-trio, disjoint files). pre ≜ read `references/kinds-and-invariants.md`.

ops:

1. ∥ 1 nico subagent / file ∈ `packages/mind/skills/*.md` EXCEPT {wake, dream}. each `/exemplify`:
   D=file; R=LLM; s=file; home_slug=`{kind}/{α}`.
2. trust the skill (accept gate = round-trip + MECE; reuse/mint dedup).

out-of-scope ≜ {wake, dream, minting missing memory skills} → repair-memory-trio.

art → `packages/mind/{kind}/{α}.md` + `.manifests/<skill>.json`.

acc (blind) ⊨ ∀ of the 11 skills: accept=valid; concepts routed (¬unrouted); parents reference-composed.
