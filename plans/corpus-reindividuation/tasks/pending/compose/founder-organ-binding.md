# founder-organ-binding

R=LLM. (checkpoint = your "once you reach here"; decision + placement, not exemplify.)

obj ≜ bind `principal := agent ^ delegate := operator` at the correct organ in nico ∧ mav, by reference.

dep ≜ exemplify-agents ∧ exemplify-skills ∧ repair-memory-trio. pre ≜ read
`references/kinds-and-invariants.md`. organ UNDECIDED → `decisions/0001-founder-agency-organ.md`.
candidates: `address` (footing principal↔delegate) | `heuristics` (standing agency drive) | `mandate`
(claimed office).

ops:

1. rule decision 0001 (nico principal; rationale). default=`address`; fallback `heuristics`.
2. ensure canonical fragment `packages/mind/{organ}/{α}.md` = `principal := agent ^ delegate := operator`
   (σ\*\_R; mint if exemplify didn't).
3. nico.md ∧ mav.md: `[[α]]` at chosen organ section. remove broken `Construal:`-filed copy.

art → decision 0001; canonical organ fragment; updated nico.md + mav.md.

acc (blind) ⊨ both founders carry binding at same recorded organ, by reference, ¬inline; decision states
organ + why.
