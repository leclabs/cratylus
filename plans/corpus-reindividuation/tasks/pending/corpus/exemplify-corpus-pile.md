# exemplify-corpus-pile

R=LLM.

obj ≜ `/exemplify` the corpus pile (lexicon ∪ GLOSSARY as ONE D, filenames=accidents) → canonical
`packages/mind/{kind}/{α}.md`. satisfies danglers left by agents/skills phases.

dep ≜ founder-organ-binding (agents+skills reference-composed; danglers known). pre ≜ read
`references/kinds-and-invariants.md`. ¬fan-out per file — pile = one body.

ops:

1. D ≜ `packages/mind/lexicon/*.md` ∪ `packages/mind/GLOSSARY.md` as ONE corpus. R=LLM; s=file.
2. one `/exemplify` (produce conceptualizes the whole pile at once → dedup across files; name; realize
   per concept; accept). home_slug=`{kind}/{α}`; pure vocab → `lexicon`. reuse vs existing canon ¬fork.
3. consumed sources subsumed → retire `lexicon/*` + `GLOSSARY.md` (leave only uncovered residue, ideally ∅).

art → canonical `packages/mind/{kind}/{α}.md` covering the pile; `.manifests/*`; retired lexicon/GLOSSARY.

acc (blind) ⊨ accept=valid; pile concepts each defined once; prior danglers now resolve; round-trip:
reader reconstructs old glossary meaning from fragments.
