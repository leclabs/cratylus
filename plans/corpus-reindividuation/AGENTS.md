# corpus-reindividuation

R=LLM (repo rule; all internals). recovery plan: a prior agent piled `packages/mind` content into
large ill-formed glossaries ¬broken down by σ\*\_R. re-individuate → MECE context modules via ordered
`/exemplify` delegations.

invariant ≜ home = `packages/mind/{kind}/{α(c)}.md`, α(c)≜σ\*\_R(c), R=LLM. dispatch contract =
`references/kinds-and-invariants.md` (every subagent reads it first).

method ≜ delegate to `/exemplify` (= produce→name→realize + accept gate); ¬re-derive its pipeline.
order matters: agents+skills first (their refs define the demand), corpus pile last, then founder
binding, gate, memory.

lead ≜ Nico (principal). supersedes older overlapping plans (`mind-structure-flatten`,
`corpus-signify-pass`) per Operator directive — ignore them.
