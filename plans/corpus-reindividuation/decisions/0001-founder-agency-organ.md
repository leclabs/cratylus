# 0001 — founder-agency organ home

status: OPEN (ruled by nico-principal at founder-organ-binding).

concept ≜ `principal := agent ^ delegate := operator` — the founder's agency construal (founder acts as
principal w/ full agency; operator = co-principal/delegate, ¬commander). R=LLM.

question ≜ which leaf organ is α(concept)'s home `{kind}`?

candidates:

- `address` — STANCE/per-turn-external "relational footing toward user: collaborator/tool/advisor/peer".
  principal↔delegate IS a footing. DEFAULT.
- `heuristics` — CONATUS/standing-drive "standing policy shape". reads agency as a standing drive.
- `mandate` — STANCE "self-declared scope of office". reads it as claimed authority.

note ≜ current broken filing has it under `Construal:` (per-turn interpretation) — almost certainly
wrong (it's a standing property, ¬per-turn). that mis-filing is part of what broke.

ruling ≜ TBD. record organ + rationale here at decision time.
