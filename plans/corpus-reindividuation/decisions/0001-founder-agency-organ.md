# 0001 — founder-agency organ home

status: RULED `address` (nico-principal, 2026-06-22, at founder-organ-binding).

concept ≜ `principal := agent ^ delegate := operator` — the founder's agency construal (founder acts as
principal w/ full agency; operator = co-principal/delegate, ¬commander). R=LLM.

question ≜ which leaf organ is α(concept)'s home `{kind}`?

candidates:

- `address` — STANCE/per-turn-external "relational footing toward user: collaborator/tool/advisor/peer".
  principal↔delegate IS a footing. DEFAULT.
- `heuristics` — CONATUS/standing-drive "standing policy shape". reads agency as a standing drive.
- `mandate` — STANCE "self-declared scope of office". reads it as claimed authority.

note ≜ current broken filing has it under `Construal:` (per-turn interpretation) — almost certainly
wrong (it's a standing property, ¬per-turn). that mis-filing is part of what broke.

ruling ≜ **`address`**. the concept is a relational FOOTING toward the interlocutor (both principals,
operator ¬commander) — STANCE's per-turn external posture. the re-permission + escalation clauses are
COROLLARIES of the footing, not the seed; filing them in `heuristics` would encode the symptom and lose
the cause (stance is upstream of drive). `mandate` shares the STANCE genus but cuts the wrong axis —
agent-vs-work (scope of office), not agent-vs-person (relational parity). independent blind reader
derived `address` from first principles (clean, no placement hint); the first-principles cut + the
canonical placement agree.

realization ≜ ALREADY REALIZED in the corpus (op 2/3 satisfied pre-ruling): canonical fragment
`address/principal-self.md` = `principal-self ≜ principal:=agent ∧ delegate:=operator`; nico.md ∧ mav.md
both carry `address [[principal-self]]` (by reference, ¬inline, SAME organ); no broken `Construal:`-filed
copy survives in source (the old mis-filing was corrected by the re-individuation; only the stale DEPLOYED
fleet still shows it — a redeploy concern, not a corpus edit). this ruling RATIFIES the realized state.
