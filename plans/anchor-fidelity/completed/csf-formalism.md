# csf-formalism

**State:** completed · **Owner:** Nico · **Deps.** none

**What.** Converge a formal model that makes "true anchor" falsifiable. Done over this session as
CSF v1→v7 (terminal: `packages/mind/canonical-semantic-factorization.v7.proposal.md`).

**Result.** Reader-relative FCA + composition. `cl_R` closure over distinction-atoms `D_R`; concepts
`C_R` = closed sets; `prim_R` = join-irreducible; composites factor via `⊔` into a canonical minimal set
`F_R` (well-ordered by name); bipartite `CSF_R` (`⟨α,gloss⟩` for primitives, `⟨α,{factor-names}⟩` for
composites); `REC_R` reconstructs, grounding at primitive glosses; `aptissimum-for-R` checks primitive
anchors fire their glosses; **round-trip theorem** `aptissimum ⟹ REC(CSF)=intent` is the acceptance law;
reader-relativity is the bounding negative result. Validated by **five successive blind-`@nico` reads**
(v2→v6) — the v6 reader: "the formal core closes," objective matched ours.
