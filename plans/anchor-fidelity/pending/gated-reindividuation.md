# gated-reindividuation

**State:** pending · **Owner:** Nico · **Deps.** land-and-recut-anchor-cluster, stratify-corpus,
mechanize-fidelity-gate (phases 1-3) **+ Operator scale-opt-in** (multi-agent Workflow over 151 cells)

**What (phase 4).** Run the gated re-individuation of the whole corpus under the CSF acceptance law —
every cell brought to its true form: primitives glossed, composites factored into anchor-refs, the
unextracted shared primitives extracted, false anchors re-cut/fused, sentence-slugs re-anchored. Each
change gated by the blind-reconstruct (`REC(CSF)=intent`). This **is** the real
`corpus-hardening/fresh-optimization-pass` — the deliverable, now tool-driven and proven, not the
prose-trim subset.

**Done-when.** Every `ideas/` cell passes the fidelity gate; no false anchor, no sentence-slug, no
unextracted shared primitive survives; the whole corpus round-trips under `aptissimum ⟹ REC(CSF)=intent`;
fleet redeploy assessed.
