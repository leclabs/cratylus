# canonize-lessons

**The plan.** Make "bringing an agent up to speed" structurally unnecessary: a lesson learned by any agent must
land in the **projected, gated canon** (where every agent inherits it and a gate enforces it), not in one agent's
private memory. `RECOMMENDATION.md` is the correct-way: route project-truths to the strongest seam
(**gate ≻ cell ≻ governing-doc**), institutionalized as a `dream` CANON-PROMOTION route. Owner: this session.

## Why (censused)

`dream`'s routing sends `generalized-wisdom ⇒ PROCEDURAL` (private, per-agent) — there is NO route INTO the canon.
So lessons die in one agent's memory, un-gated + fleet-invisible. That is why I regressed this session despite
carrying `cold-decode-oracle`/`llm-native` in my SOUL: understanding is not encoding; a gate binds, memory drifts.

## Shards (MECE)

| shard                       | seam                       | concern                                                                                 | state |
| --------------------------- | -------------------------- | --------------------------------------------------------------------------------------- | ----- |
| M1 dream-promotion route    | mechanism (dream cell)     | the GENERAL fix — route canon-truths to the projected canon by default                  | done  |
| L1 fittest-sign-declare     | gate + cell                | encode "declare the fittest sign, never degrade" (SYMBOLS gate = decodability registry) | done  |
| L2 constitutional hierarchy | governing doc + ρ=LLM home | encode `llm-native ≻ VISION ≻ MODEL` + reconcile-up-the-order                           | ready |

M1 is the headline (institutionalizes promotion); L1·L2 are the backlog it would have caught. Independent —
dispatch concurrently (L1 before M1 only if M1 wants a new glyph).

## Already canonized this session (no shard — recorded for completeness)

- **σ\*-dispatch-prompt law** → the `llm-native` fragment (commit `393fb3e`). ✓ projected cell.
- **census-grounds-spec** → the `praxis` cell (commit `11fd326`). ✓ projected + gate-shaped.
- **⇏ fittest-glyph** → `operator-lexicon` declared (commit `be6589f`). ✓ gate.
- **terminology** (organ→dimension · genus→Persona/Constitution) → captured in `vocab-depalimpsest/C2`. ✓ gated task.

## Notes

- Each shard is blind-dispatchable (static · scope · accept), census-grounded (`census-grounds-spec`). Accept is
  falsifiable (grep + cold Ω\* read + tests + projection). Prefer a GATE seam over prose.
- ρ = LLM for the cells; the L2 governance rule needs a ρ=LLM home an agent reads at decision time (not only the
  `ρ=human` CANON.md).
