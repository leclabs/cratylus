# M1 — institutionalize the CANON-PROMOTION route in `dream` (the general mechanism)

**static (censused):** `packages/agent-anatomy/src/skills/dream.ts` (the `LAWS` routing block —
`generalized-wisdom(i) ∧ ¬projection-carries(i) ⇒ i ↦ PROCEDURAL`; `projection-carries(i) ⇒ drop`) ·
`packages/agent-anatomy/src/skills/wake.ts` (the load/orient reader) · `../RECOMMENDATION.md` (the rule) ·
the memory protocol surface (post-E6c: `memory --help` + wake/dream cells; `genus/memory.md` retired).

**scope:** add the promotion split to `dream.ts`'s routing (per RECOMMENDATION.md):
`generalized-wisdom(i) ∧ ¬projection-carries(i) ∧ canon-truth(i) ⇒ i ↦ CANON-PROMOTION` (author into the strongest
seam gate ≻ cell ≻ governing-doc; `¬canon-remit ⇒ emit a canon-candidate task to the curator`; then
`projection-carries ⇒ drop from PROCEDURAL`); the `¬canon-truth` remainder still `⇒ PROCEDURAL`. Declare
`canon-truth(i) ⇔ binds ≥1 agent-type ∨ the fleet ∨ mechanism/governance-fact ⟨¬this-agent-only⟩` and
`CANON-PROMOTION` in the DECLARATIONS. Keep the register (σ\*, dense, R=LLM); use only declared glyphs (extend the
lexicon per the L1 discipline if a fitter sign is needed — do NOT degrade).

**accept (falsifier):** `dream.ts` routes a `canon-truth` lesson to CANON-PROMOTION (grep the formalBlock);
`projection-carries ⇒ drop` still holds (post-promotion dedup); the projected `~/.claude/skills/dream/SKILL.md`
carries the new route (`project` to a tmpdir, grep); `pnpm -C packages/agent-anatomy test` green (symbols +
reader-density + skill-shape); a cold Ω\* read of the new law decodes "a fleet/system-level lesson is authored into
the projected canon, not parked in private memory."

**dep:** none. (L1 may land first if a new glyph is wanted; else independent.)
