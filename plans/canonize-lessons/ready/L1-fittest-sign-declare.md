# L1 — encode "declare the fittest sign, never degrade it" (gate = decodability registry)

**static (censused):** `packages/agent-anatomy/src/skills/formalize.ts` (the skill that authors set-builder
formal blocks from prose — the symbol-authoring seam) · `packages/agent-anatomy/src/skills/signify.ts` (the
sign/anchor home) · `packages/agent-anatomy/test/symbols.test.ts` (the SYMBOLS gate — header states its contract) ·
`packages/agent-anatomy/src/toolkit/operator-lexicon.ts` (the declared-glyph table + its header).

**lesson to encode:** source symbols are subject to `llm-native` — the FITTEST sign wins. The SYMBOLS gate is a
**decodability REGISTRY**, not an expression cap: when it rejects the fittest sign, the fix is to **declare the
sign** (add it to `operator-lexicon`, cold-verify its decode) — **never to degrade** the sign to a weaker declared
one. (Witnessed this session: `⊉`→`≠` was the wrong fix; `⇏` declared was the right one.)

**scope:** CENSUS the two candidate homes and place the law where it BINDS the authoring seam: (a) a law in
`formalize.ts`'s formalBlock (formalize produces symbol-bearing blocks — the natural disposition home), stated σ\*;
AND (b) a one-line CONTRACT in the SYMBOLS-gate header (`symbols.test.ts`) + `operator-lexicon.ts` header naming the
gate a decodability registry whose remedy for a rejected fittest-sign is DECLARE-not-degrade. Prefer the gate/cell
seam over prose. Do NOT restate it in per-agent memory (it is a project-truth → canon).

**accept (falsifier):** `formalize` (or the chosen cell) carries the declare-not-degrade law (grep); the SYMBOLS
gate header states its registry contract; a cold Ω\* read of the added law decodes "extend the gate's declared
signs to admit the fittest sign; do not substitute a weaker one"; `pnpm -C packages/agent-anatomy test` green
(symbols/reader-density/skill-shape); the projected SKILL.md carries it.

**dep:** none.
