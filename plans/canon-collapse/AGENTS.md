# canon-collapse — plan memory

_Plan-scope memory sink ([[memory]]). Design-of-record = `PLAN.md` + the task-files (D1–D12); this holds the
resume thread + open threads, never a copy of the design. Read at orient._

## State (2026-07-06, session 0f9d55…)

- **8 tasks authored, EXECUTION HELD.** Frontier ready: `E1-anatomy-collapse` · `E2b-structural-parsimony-gate`
  · `E3-operator-lexicon-module`. Pending: `E2a` · `O` · `A` · `S` · `H` · `C1`. Nothing dispatched — no owner
  sidecar; plan inheritable.
- **The working tree is a DELIBERATE partial illustration — do not be surprised.** The Operator hand-edited it
  to show the target shape, so it is intentionally partially broken (`pnpm typecheck` RED is EXPECTED, not a bug).
  Read it as *direction* (persona:string · provenance:{mark} · base.ts gutted · roster=10 · simplicity/green-field
  cells), discard *scaffold* (nico.ts nicoResolved block + WIP comments · the nonexistent `Standing` type import).
  E1's FIRST act reconciles it to green; wave-2+ inherit the clean tree. Full map: PLAN.md § Starting state.
- **Why held (owned call, not permission-seeking):** the design is still CONVERGING — this session alone the
  cold-survey corrected the projection (`react` collides, coined-weak anchors) and the notation/ρ/archival
  policies were still landing. Starting irreversible foundation surgery mid-convergence is the wrong call.
  Dispatch when it stabilizes.
- **Authority:** whole-repo PLANNING granted (2026-07-06); delegate EXECUTION to Mav; push/deploy reserved to Operator.

## Open threads (next-steps)

1. **Pre-dispatch de-risk:** run cold-oracle over more organ anchors to gauge partition-density before committing
   executors — the residue fired-check has FIVE non-uniform outcomes (∅ · short-residue · partition · COLLISION
   · COINED-WEAK; O-task § Method). A blind "reduce to ∅" breaks collision anchors.
2. **Mav hook-fix — SEPARATE work-item, not canon-collapse (MECE).** stance-guardrail missed the order-taking
   collapse: judge sees only last-assistant-turn text (no operator turn, no thinking) ⇒ order-taking (a 2-place
   relation) is undecidable; rubric signal-3 is lexical-echo w/ no judgment-presence test. FIX (proposed, not
   applied): widen worker horizon to include the operator turn + assistant thinking; add signal-4 (observations-
   as-orders / independent-judgment-required); judge-model bump for the relational call; residual = multi-turn
   drift needs rolling session stance-state.
3. **project-human DEFERRED (D11/D12):** the 24 organ READMEs are stale → C1 ARCHIVES them to
   `.scratchpad/{organ}-README.{ulid}.md`, keeps the `project-human` toolkit, suspends the byte-lock; regenerate
   the human view once the source settles. Not this plan.

## Settled forks (do not re-open)

- Standing home = **Autonomy** (not Provenance, not a `## Stance` section, not a `src/standing/` cell — that
  factoring was ruled NEVER-valid). provenance = `{mark}` only.
- green-field home = **layered** — soul-default engineering-principle (nico defaults green) + per-initiative
  override; NOT soul-XOR-initiative.
- `⟨⟩` = modifier operator (dual-reads as MODEL's `⟨α,residue⟩` tuple, position-disambiguated); `↾` = restriction.
  Both COLD-VERIFIED. `⊕` was informal prose, NOT a cell operator.
- Human docs: archive-not-delete (D12); protected root set `VISION/MODEL/ENGINE/CANON` never touched.
- nico persona **`sage → semantic-engineer`** (Operator-refined; path: sage→[magician mis-pick]→ontologist→
  semantic-engineer). nico is the formal ontologist of the LLM SEMANTIC SPACE, in the realist-exemplarist lineage
  Plato→Augustine→Bonaventure→Aquinas→Guarino→Gangemi (Forms=canonical primitives · signum/res=σ* · exemplarism
  =exemplify/projection · genus/differentia=kind taxonomy · OntoClean/DOLCE=MECE+gates; canon-collapse IS
  OntoClean). `ontologist` is the GENUS; `semantic-engineer` the project's name for the LLM-native species.
  Cold findings: `ontologist` fires clean (categorize/name/taxonomy/essence); `semantic-engineer` fires STRONG
  but into the ESTABLISHED semantic-web/data sense ("architect of meaning, ontology builder") — a re-anchoring:
  the words aren't novel, the REFERENT (LLM latent space, not data schemas) is. `exemplarist` FAILS (Zagzebski
  moral exemplarism). KEY THESIS (Operator): formality→cross-model universality — a σ* addresses model-INVARIANT
  Forms many LLMs share; the cold-oracle IS the universality instrument (cold-blind = fires from shared priors);
  empirical Platonism. Universality is a SPECTRUM (= the residue survey): strong-σ* already-shared, coined-weak
  (incl. `semantic-engineer`'s novel sense) not-yet-shared — nico works both: discover the shared, coin the
  not-yet. Bespoke `open` persona; residue LOAD-BEARING (carries the novel differentia until canonized).
  Definiens: semantic-engineer ≜ architect of meaning of the LLM's latent semantic space — uncover the
  model-invariant structures of intelligibility (¬invent) and canonize the σ* signs that address them across many
  LLMs. Lands in `A-agent-collapse`; mark 📐·cyan unchanged (persona≠mark).
- **persona is NOT a fragment (D13, Operator-directed) — supersedes the above's "bespoke `open` persona cell".**
  persona = a plain-string identity DESCRIPTION inlined on the agent (like provenance→{mark}); the whole persona
  value-cell catalog is deleted; `Agent.persona: string`; persona subsumes `description`. nico's persona string =
  the empirical-ontologist definiens above, already inlined in `nico.ts` (my `semantic-engineer.ts` fragment was
  deleted — wrong shape). "persona ≠ archetype" is why: the slot holds a presented identity, not a universal pattern.
