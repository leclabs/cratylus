# live sources cite paths inside long-retired plans and nothing gates it

> FILED, not specified. A stub: symptom + locus + provenance, no census, no
> acceptance. It exists so the defect was not chased when it was found. Whoever
> promotes it to `ready` owes it a real spec (`/praxis upsert`).

**Symptom.** live sources cite paths inside long-retired plans and nothing gates it

**Locus.** At least eight citations across five plans retired before `decomplect`, none of
which resolve: `README.md:15` and `docs/research/candidates.md:7` →
`plans/discipline-anchor/PLAN.md`; `docs/claude-agents-md-assumption.md:15` and
`docs/release-audit-checklist.md:4` → `plans/interop-hardening/completed/*.RETURN.md`;
`packages/memory/README.md:51` → `plans/scoped-memory-v2/SPEC.md`;
`packages/runtime/src/ports/provisional-v9.ts:5` →
`plans/close-out/pending/V9-heartbeat-mechanism.md`; and two guardrail fixtures under
`packages/canon/src/toolkit/guardrail/fixtures/` (which are recorded turns — arguably
history, and the scope call is part of the ruling this owes).

`retire` MEANS DELETE, so every retirement mints this class, and the corpus has no gate
that reads a `plans/<plan>/…` citation and asks whether it resolves. A cheap one exists:
`command-veracity` already walks every tracked file for `pnpm <script>` instructions and
holds them to a resolution set — the same walk over `plans/<…>` path citations, with the
same reach leg, would convict all eight. Note the discriminator the veracity gate needed
and this one needs too: a MENTION inside a closed record is not an INSTRUCTION.

**Provenance.** Filed 2026-08-05 from `96fde8e8`, while executing `retire-decomplect`.
