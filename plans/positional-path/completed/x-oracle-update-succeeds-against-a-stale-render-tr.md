# oracle:update succeeds against a STALE render tree and writes a hash that does not reflect source — it re-baselined to an unchanged sha after a projected cell was edited, and only a manual rm -rf of the render dir surfaced the real one

> FILED, not specified. A stub: symptom + locus + provenance, no census, no
> acceptance. It exists so the defect was not chased when it was found. Whoever
> promotes it to `ready` owes it a real spec (`/praxis upsert`).

**Symptom.** oracle:update succeeds against a STALE render tree and writes a hash that does not reflect source — it re-baselined to an unchanged sha after a projected cell was edited, and only a manual rm -rf of the render dir surfaced the real one

**Locus.** _(unfilled — the filer may not have known)_

**Provenance.** Filed 2026-08-06 from `e197fde3`, while executing `x-landing-monotonic`.
