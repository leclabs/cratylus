# landing(P) is monotonic but done(P) is not absorbing: a plan that briefly satisfied done and then gained shards reads landed forever, so praxis demands retirement of open work

> FILED, not specified. A stub: symptom + locus + provenance, no census, no
> acceptance. It exists so the defect was not chased when it was found. Whoever
> promotes it to `ready` owes it a real spec (`/praxis upsert`).

**Symptom.** landing(P) is monotonic but done(P) is not absorbing: a plan that briefly satisfied done and then gained shards reads landed forever, so praxis demands retirement of open work

**Locus.** _(unfilled — the filer may not have known)_

**Provenance.** Filed 2026-08-06 from `c02fbbfb`, while executing `t-positional-path-gate`.
