# T5.3 — Enabling-tone authoring convention (scoped to instructions)

**Phase** 5 (organ hygiene) · **Lane** Nico · **Depends on** none (current corpus) · **Gates** nothing.

## Finding (corrects the original framing)

No enabling-tone meta-rule exists, and it must **NOT** be corpus-wide: `charter` is _intentionally_ "negative and load-bearing." A blanket positivity rule would contradict it. So this is an **instructions-scoped authoring convention**, not a new organ.

## Scope

1. Add a one-line convention to `instructions/README.md`: directives are phrased as enabling preconditions — positive move first; reserve "never/don't" for `charter`.
2. Optionally rephrase trailing prohibitive clamps in `dry`/`first-principles`/`llm-native` only if it reads well; do not force.
3. No "tone" organ value; the convention lives in the README (+ a possible future verify lint, deferred).

## Acceptance criteria

- `instructions/README.md` carries the convention, bounded to instructions, explicitly excluding charter.
- Any rephrased cells pass verify + preserve semantics.

## Out of scope

A verify gate enforcing tone (future); charter cells; precedence doc (T5.4).
