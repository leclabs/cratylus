# T6.1c — verify.py gate-survivability: dissolve or port to TS

**Concern** verification · **Lane** Nico (decide which survive) + Mav (port survivors) · **Depends on** — (ready) · **Gates** T6.1e

## Context

Deleting `verify.py` must not silently drop a load-bearing gate. The deep-dive (2026-06-24) classified
every gate. The governing insight: **most gates checked that the MARKDOWN encoded a valid structure —
the TS types now encode that structure by construction, so `tsc` + the module system SUBSUME them.**

## Decision (Nico) — gate disposition

| verify.py gate                                                         | Disposition                                                                                                                                                             |
| ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `gate_agent_organ_refs`, `gate_mark`, `gate_schema_and_refs`           | **DISSOLVE** → tsc (wrong organ/arity/missing field = compile error; mark is a typed field)                                                                             |
| `gate_reconstruct` R1 (one home)                                       | **DISSOLVE** → one module per fragment IS one-home; imports are by-reference                                                                                            |
| `gate_reconstruct` R2 (no 8-word copy)                                 | **DISSOLVE for now** (authoring nit; revivable as a TS test if drift appears)                                                                                           |
| `gate_roundtrip`, `gate_verbatim_ref_free`, `gate_fences`, R3 manifest | **DISSOLVE** → the byte-identity oracle's job is done; once `.md` is gone there is no round-trip to check                                                               |
| `gate_no_holders`                                                      | **DISSOLVE** → no `holders:` field exists on the TS types                                                                                                               |
| `gate_skill_operative`, `gate_skill_provenance`                        | **PORT (light)** → a vitest test over the `SkillCell` modules: each has ≥1 operative element; composition XOR prose-formula (no double-cite)                            |
| `gate_symbols`                                                         | **PORT (light)** → a vitest test: every fence-interior glyph in each `.ts` definiens ∈ the declared table (`references/formal-symbolic-notation.md` col-1 ∪ exemptions) |

## Work (Mav — port the two survivors)

1. `packages/mind/test/skill-shape.test.ts` — operative-element + composition-XOR-formula over all `SkillCell` modules.
2. `packages/mind/test/symbols.test.ts` — fence-interior glyph coverage over all fragment/skill definiens
   against `references/formal-symbolic-notation.md`. Keep `references/` (not deleted in T6.1e).

## Acceptance (blind-testable)

- `pnpm --filter @leclabs/mind test` includes the two new tests, both green; they FAIL on an injected
  violation (non-vacuous: a bad-symbol fixture blocks, a non-operative skill blocks).
- Every other verify.py gate has a written DISSOLVE rationale (this file) — nothing dropped silently.

## Out of scope

Deleting verify.py itself (T6.1e).
