# T6.1d — auxiliary Python keep-or-drop decision

**Concern** corpus tooling scope · **Lane** Nico · **Depends on** — (ready) · **Gates** T6.1e

## Context

Beyond projection (`resolve.py`) and deploy (`deploy.py`), the Python toolkit carries authoring/
instantiation tools. Decide each: drop now (git is the recovery net) vs re-home to TS. None is in the
projection+deploy hot path, so none blocks the cutover — this is a scope decision recorded before T6.1e deletes.

## Decision (Nico)

| Module                               | Disposition          | Rationale                                                                                                                                                |
| ------------------------------------ | -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `glossary.py`, `compose/glossary.py` | **DROP**             | GLOSSARY.md demolished in the rebuild; dead.                                                                                                             |
| `emit_ir.py`, `render/ir.py`         | **DROP**             | IR-bridge superseded by direct adapter projection (T2.1); fixture committed.                                                                             |
| `emit_manifest.py`                   | **DROP (revivable)** | R3 routing-manifest producer; R3 dissolved (T6.1c). Revive in TS iff exemplify needs it.                                                                 |
| `intake.py`                          | **DROP (revivable)** | Anchor-routing intake is an authoring aid, not deploy; re-home to TS later iff wanted.                                                                   |
| `rebase.py`                          | **DROP (revivable)** | Brownfield instantiation; no koine analog yet, but out of the deploy path. `koine found` covers greenfield. Git-recoverable when a TS port is scheduled. |
| `init.py`                            | **DROP**             | Superseded by `koine found` (T3.1, parity).                                                                                                              |
| `config.py`                          | **DROP**             | `.polis.config` resolution lives in koine `src/deploy/config.ts`.                                                                                        |

KEEP (not Python projector, not cruft): `toolkit/continuity/` + `toolkit/guardrail/` (shell hooks),
`toolkit/AGENTS.md` + `CLAUDE.md` (rewritten in T6.1f), `references/`, `ideas/memory.md` + `ideas/AGENTS.md`.

## Acceptance (blind-testable)

- Each auxiliary `.py` has a recorded disposition; nothing in the projection/deploy path is dropped
  without a koine replacement; the revivable set is named so git-recovery is targeted.

## Out of scope

Executing the deletion (T6.1e).
