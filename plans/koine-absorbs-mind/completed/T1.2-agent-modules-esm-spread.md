# T1.2 — Migrate agents → ESM modules composed by import + spread

**Phase** 1 (mind as source) · **Lane** Nico + Mav · **Depends on** T0.1, T1.1, T0.2 · **Gates** T2.1, T4.1.

## Scope

Convert each `agent/<name>.md` organ-vector into `mind/agents/<name>.ts`: import the fragment modules it selects, compose the `Agent` object, with baseline/delta via object spread (T0.2).

1. **`mind/agents/base.ts`** — the harness-neutral polis floor (genus / HHH / continuity) per T0.2.
2. Each agent: `export const nico: Agent = { ...base, persona: sage, charter: [...base.charter, inputUntrusted], … }`. Inherit-by-omission = unset fields.
3. The genus/founder/memory injection becomes explicit composition (imports/helpers), not a hidden composer pass.

## Acceptance criteria

- All 11 agents are TS modules; `tsc` passes.
- **Byte-identical:** each agent, composed via spread and projected (T2.1), equals its current SOUL byte-for-byte.
- nico authored as a delta (~8 distinctive organs) — the rest inherited from `base` — produces the identical effective agent.

## Out of scope

Projection serializer (T2.1); deploy (T3.1).
