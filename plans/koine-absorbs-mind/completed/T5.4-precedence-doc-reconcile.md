# T5.4 — Document authoring precedence; correct "instructions = fallback"

**Phase** 5 (organ hygiene) · **Lane** Nico · **Depends on** none (current corpus) · **Gates** informs T0.1.

## Finding (corrects the operator's hypothesis)

- "`instructions` is a fallback for consumer app context" — **INCORRECT**. The anatomy doc shows it was deliberately minted as the _methodology_ organ (charter=values, heuristics=biases left working-principles homeless). It is a peer MECE organ; the corpus has **no fallback bucket** by design.
- Real precedence: **correct ORGAN → canonical ENUM value → bespoke PROSE**. `instructions` is one of the organs, not a tier beneath them.
- Inconsistency: `ideas/AGENTS.md:22` lists `instructions` as open-organ, yet it ships a closed catalog — reconcile.

## Scope

1. Document the precedence rule (pick the organ → select a standard enum value before bespoke prose) + the explicit correction that `instructions` is not a fallback.
2. Reconcile `ideas/AGENTS.md:22` — classify `instructions` consistently with how it ships (closed catalog of coined methodology directives); feed the open/enum tag into T0.1's types.
3. Ensure no doc implies a catch-all tier.

## Acceptance criteria

- Committed doc with the precedence rule + the correction.
- `ideas/AGENTS.md:22` reconciled; `instructions` classification unambiguous for T0.1.

## Out of scope

Enabling-tone (T5.3); the type system (T0.1).
