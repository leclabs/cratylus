# T6.1f — docs reframe: koine-only, .ts-is-source

**Concern** documentation / doc-mirrors-runtime-truth · **Lane** Nico · **Depends on** T6.1e · **Gates** plan retirement

## Context

After deletion, every doc that describes the Python pipeline or markdown-as-source is stale. Reframe to
the landed reality: **the anatomy IS the TS types; markdown is a projection; composition is ESM; koine is
the only projection+deploy machinery; mind is koine's first opinionated plugin.**

## Work

1. **Root `AGENTS.md`** — the `packages/mind` bullet: drop "Python `toolkit/` projector is dual-maintained
   pending retirement"; state koine-only projection (`koine project`/adapter) + deploy (`koine deploy`).
2. **`packages/mind/AGENTS.md`** — pipeline line and any `uv run … toolkit/*.py` invocations → koine commands.
3. **`packages/mind/toolkit/AGENTS.md`** — strip the Python pipeline spec (resolve→glossary→verify→deploy,
   init/deploy stages); reduce to what survives: the shell hooks (continuity, guardrail). Keep dual-deploy
   doctrine only as it now lives in koine (point to koine deploy/bundle).
4. **`packages/mind/TOOLKIT.md`** — retire or rewrite to the koine pipeline.
5. **`docs/agent-conceptual-anatomy.md`** — anatomy IS `@leclabs/koine/anatomy` TS types (not markdown cells).
6. Update `packages/mind/provenance/README.md` only if it survived T6.1b/e (it documents lineages).

## Acceptance (blind-testable)

- No doc instructs a Python toolkit command; every projection/deploy instruction is a koine command.
- A blind reader of the docs builds the correct mental model: TS source → koine projection → koine deploy;
  mind = koine's core plugin; markdown is output, not input.
- `pnpm lint`/prettier green on all touched markdown.

## Out of scope

The redeploy itself (T6.1g).
