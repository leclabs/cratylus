# T2.3 — Reader-density as projection variants

**Phase** 2 (adapters as projection) · **Lane** Mav · **Depends on** T2.1 · **Gates** nothing (enables future weak-llm targets).

## Reframe

The reader-density axis (weak-llm / strong-llm / strong-llm-lean) is **just a projection parameter** — different template-literal functions over the same typed fragments. No baked-in profile, no separate IR.

## Scope

1. A `density` parameter (or per-density projection fns) in the projection layer: `strong-llm-lean` (name-only refs), `strong-llm` (name + delineation), `weak-llm` (+ kind/verb cue).
2. The provenance header records the density (as today).

## Acceptance criteria

- The same corpus projects byte-identical to current `strong-llm-lean` output, AND can project at `weak-llm`/`strong-llm` from the same source with no re-authoring.
- Density is a function arg, not a property of the source modules.

## Out of scope

Deciding deployed density (stays strong-llm-lean); other harnesses (T2.4).

## Done (2026-06-24, `e7407ce`)

Reader-density is now a claude-projection **parameter** (`--density strong-llm-lean|strong-llm|weak-llm` →
`densityProfile` / `densityRef`, the port of `compose/reader.py render_ref`). Acceptance met: strong-llm-lean
**byte-identical** (no regression); weak-llm + strong-llm project from the same source and match the Python
oracle **byte-for-byte**; density is a function arg, not a module property. Gate green (koine 223, typecheck 4/4).

**Honest finding (for cutover/extraction).** The density axis is **dormant system-wide**: on the current
corpus the projected _body_ does not vary by density (only the `profile:` label line) — agents are verbatim
organ-vectors, and the skill composer renders names-only refs (density-blind), in **both** Python and TS.
So `densityRef` is a tested-but-unwired mechanism, faithful to the dormant Python `render_ref`. Whether to
**exercise** density (add a delineation-bearing surface for weak-llm targets) or **cut** it (parsimony, if no
weak-llm deploy is planned) is a real keep-or-cut decision for T6.1 cutover — flagged, not decided here.
