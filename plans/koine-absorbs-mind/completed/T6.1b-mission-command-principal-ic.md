# T6.1b — finish mission-command + principal-ic = the most elite IC (.ts source)

**Concern** corpus content / signification · **Lane** Nico · **Depends on** — (ready) · **Gates** T6.1g

## Context

Two terminology fixes, edited in **`.ts` source only** (markdown is the projection; do NOT hand-maintain
the legacy `.md` — it is being deleted in T6.1e). Industry-standard / reader=LLM, nothing bespoke ([[signify]]).

Already landed (commit `1733737`): `mission-command` (Auftragstaktik) canonicalized in `carry-on`;
inverted `principal-agency` collapsed into it; provenance `principal-ic-root-red` concept term updated.

## Work (remaining)

1. **`principal-ic` ≜ the most elite individual contributor** — kill the inverted "source of principal
   agency" reading. Edit `src/organs/provenance/principal-ic-root-red.ts` definiens so the genus name
   resolves unambiguously to _Principal-Engineer-tier IC_ (the most elite IC), founder-genus-eligible,
   the mission-command root that mav & the reviewer specialize.
2. **Sweep residual prose** in `provenance/README.md` (the human projection): "the source of principal
   agency" → mission-command; ensure every `principal-ic` mention reads as the elite-IC seniority term,
   not the inverted authority term. The specializers (reviewer/tester "principal-ic intrinsic",
   developer "specializes principal-ic", diagnostic "¬principal-ic-intrinsic") stay — now unambiguous.
3. Do NOT rename the slug or the agent — `principal-ic` is **kept** as the elite-IC anchor (Operator
   decision); no blind name-gate, no rename ceremony.

## Acceptance (blind-testable)

- A blind reader, given only the corpus, reads `principal-ic` as "the most elite individual contributor"
  with zero "principal-agency"/operator-authority ambiguity; `mission-command` (incl. Auftragstaktik) is
  the sole authority anchor; `principal-agency` appears nowhere.
- `.ts` is the edited source; `pnpm build/test/lint/typecheck` green.

## Out of scope

Deleting the `.md` (T6.1e); the deploy (T6.1g).
