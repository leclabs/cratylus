# T5.2 — Prescriptive twin: dont-reinvent-the-wheel → invoke-the-canonical

**Phase** 5 (organ hygiene) · **Lane** Nico · **Depends on** none (current corpus) · **Gates** nothing.

## Finding

`dont-reinvent-the-wheel` ≜ "invoke the canonical skill; don't re-derive it." Prohibitive slug + tail.

## Scope

1. Rename → `invoke-the-canonical` ≜ "invoke the canonical skill; re-derive only when none exists." (Identical semantics, enabling tone.)
2. Update every agent vector selecting it (incl. `agent/nico.md`); git mv the cell.
3. Re-resolve → verify PASS (no dangling `[[dont-reinvent-the-wheel]]`).

## Acceptance criteria

- New cell exists; old gone; all selectors updated; REFERENCES gate green; affected SOULs round-trip.

## Out of scope

Mass-rewriting other prohibitive tails — that's T5.3's call.
