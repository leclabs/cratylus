# T4.1 — Baseline/delta rollout (agents as deltas via spread)

**Phase** 4 (rollout) · **Lane** Nico (modules) + Mav (engine) · **Depends on** T0.2, T1.2, T2.2 · **Gates** T6.1.

## Scope

With the TS source (T1.2), the claude adapter reset (T2.2), and the spread-merge (T0.2) in place, make every agent a true delta.

1. `mind/agents/base.ts` finalized (harness-neutral polis floor).
2. Each agent refactored to `{ ...base, <distinctive organs> }` — omitting everything inherited from `base` or provided by the harness (the latter dropped at export by the adapter reset).
3. Prove the projected SOULs are unchanged (pure DRY refactor, zero behavioral change).

## Acceptance criteria

- nico authored as ~8 distinctive organs over `base`; projected SOUL **byte-identical** to today's.
- All 11 agents are deltas; projection byte-identical across the fleet; verify/round-trip green.
- A new agent can be authored as a handful of organ overrides (demonstrated).

## Out of scope

Cutover (T6.1).
