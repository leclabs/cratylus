# T6.1g — redeploy all agents + skills via koine ONLY (local + fleet)

**Concern** rollout · **Lane** Nico (drives) + Mav (engine support) · **Depends on** T6.1a, T6.1b, T6.1e · **Gates** plan retirement

## Context

The done-state the Operator named: **every agent and skill redeploys via koine machinery only.** This is
the Operator-authorized outward act (he set it as the objective). Runs after the corpus is finalized
(T6.1b), koine is the sole deploy path (T6.1a), and the cruft is gone (T6.1e).

## Work

1. Project the final corpus via koine (`pnpm --filter @leclabs/mind project` → render tree).
2. **Deploy local (fire) first** via `koine deploy --kind agent && --kind skill --scope user`; verify
   on-host: agents+skills present in `~/.claude/`, mission-command/principal-ic text landed, memory skill
   carries `episodic.mjs`. Re-verify by sha256 vs the render tree + grep a known line (never the exit msg).
3. **Fleet** via `koine deploy --fleet` (resolves `.polis.config`: fire local; forge/spark/ash=lex;
   upmav/upgoose=lcaraccioli; upgoose excluded → manual canary via `--host upgoose`). ssh: generous
   timeout (key-auth >10s). Seed-if-absent never clobbers SELF/MEMORY/EPISODIC. Deploy NEVER prunes →
   `weitermachen` already removed; verify no stale dirs.
4. Re-verify every host: sha256(landed) == sha256(render) for a sample agent + the memory skill; grep
   `Auftragstaktik` / `most elite individual contributor` present.

## Acceptance (blind-testable)

- All fleet hosts carry the new corpus, deployed by `koine deploy` with **zero Python invoked**; on-host
  sha256 matches the koine render tree; mission-command + principal-ic=elite-IC text present.
- A re-run is idempotent (defs overwrite; sidecars untouched).

## Out of scope

New harness adapters beyond koine's set.
