# T6.1e — delete the cruft: legacy .md cells + Python toolkit + retire the round-trip oracle

**Concern** deletion / .ts-as-sole-source · **Lane** Nico (corpus) + Mav (tests) · **Depends on** T6.1a, T6.1c, T6.1d · **Gates** T6.1f, T6.1g

## Context

The byte-identity oracle has done its job (TS proven byte-identical end-to-end). With koine the only
deploy path (T6.1a), gates ported/dissolved (T6.1c), and aux Python dispositioned (T6.1d), the legacy
markdown and the Python projector/deployer are pure cruft. Delete them; `.ts` becomes the **sole source**.

## Work

1. **Delete legacy `.md` cells** — the dual-maintained projections now sourced from `src/`:
   `packages/mind/{address,appraisal,charter,competence,comportment,construal,deliberation,disclosure,
disposition-memory,effectors,enaction,gestalt,heuristics,instructions,ledger,mandate,percept,persona,
register-fit,resolve,sensors,substrate,telos}/*.md` + `agent/*.md` + `skill/*.md` + `provenance/*.md`.
   **KEEP:** `ideas/memory.md` (read by project-cli), `ideas/AGENTS.md` (corpus rules), `references/`,
   any `README.md` docs being rewritten in T6.1f.
2. **Delete the Python projector/deployer**: `toolkit/*.py` (resolve, verify, deploy, glossary, init,
   emit*ir, emit_manifest, intake, rebase, config) + `toolkit/{compose,core,decorate,place,render}/` +
   `toolkit/test*_.py`+`toolkit/**pycache**`. **KEEP** `toolkit/{continuity,guardrail}/`(shell) and the`toolkit/_.md` docs (T6.1f rewrites them).
3. **Retire the round-trip oracle tests** (Mav): `test/agent-roundtrip.test.ts`, `full-roundtrip.test.ts`,
   `roundtrip.test.ts`, `skill-roundtrip.test.ts`, `agent-delta.test.ts` compare TS→`.render`/`.md`
   golden masters that no longer exist. Their proof (TS == legacy) is **complete**. Replace with a
   **projection-stability snapshot** test (TS projects all agents+skills without error; optional committed
   snapshot) so the suite still guards projection. `parity.test.ts` in koine (vs Python) is retired too.
4. Remove `.render/` (Python staging) from use; `.render-ts/` is the only render dir.

## Acceptance (blind-testable)

- Zero `.py` under `packages/mind/toolkit/` except none (shell + docs remain); zero legacy organ/agent/
  skill/provenance `.md` cells; `ideas/memory.md` + `references/` present.
- `pnpm build/test/lint/typecheck` green with no Python invoked anywhere; projection still runs
  (`pnpm --filter @leclabs/mind project`) and emits all 11 agents + 16 skills.
- `grep -rl 'toolkit/.*\.py' ` finds no live invocation (docs rewritten in T6.1f).

## Out of scope

Docs reframe (T6.1f); redeploy (T6.1g).
