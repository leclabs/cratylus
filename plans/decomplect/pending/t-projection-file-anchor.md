# Three forge files are named `anatomy` and none of them holds a meta-model

> Census: [`CENSUS-2026-08-05.md`](../CENSUS-2026-08-05.md). **Ruling owed before execution** —
> the target of a move is indeterminate until it is made.

## The concept needing a name

`adapters/claude/anatomy.ts` exports `agentToClaudeMd` · `skillToClaudeMd` · `claudeHarnessAdapter`.
`adapters/codex/anatomy.ts` exports `agentToCodexToml` · `agentsMdSurface` · `codexHooksJson` ·
`codexHarnessAdapter`. `core/anatomy-body.ts` exports `dimensionTitle` · `dimensionField` ·
`agentBody` · `skillBody`. **Zero meta-model in any of them.** The concept is _harness projection
surface_ wearing the sign of a metaphor that was already ruled against for a different concept.

## Blast radius

3 files + 16 importing lines within `forge`.

## Why it is blocked

The one derivation leg run returned `capabilities`, **rejected on occupancy** against runtime's
capability keyspace — which is `t-tap-anchor`'s subject. The two signs must be settled against each
other or the second will collide with the first.

## Acceptance

- The anchor is DISCOVERED — forward argmin, blind reverse decode, occupancy check including against
  runtime's keyspace and against `Target` (`t-soul-to-target-in-forge`'s anchor).
- If the derivation returns `⊥`, **record the candidate set and the date**. This census had to
  correct a `⊥` that was honored for weeks after being overturned; a refusal without its candidate
  set is unfalsifiable.
