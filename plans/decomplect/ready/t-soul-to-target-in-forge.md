# `SOUL` is a canon metaphor living in the doctrine-agnostic engine — and one site SHIPS

> Census: [`CENSUS-2026-08-05.md`](../CENSUS-2026-08-05.md). Every number below was measured,
> not quoted forward.

## Intent

`SOUL` appears in **11 files / 30 lines** under `forge/src`, and **0 times** in VISION, MODEL,
ENGINE, CANON, or ARCHITECTURE. The convergent anchor already exists in the same package:
`forge/src/validate/accept.ts:99` `interface Target`, glossed _"A deploy Target — REGENERABLE
quantifies over these"_, matching `MODEL.md:33 Target ≜ harness-declaration-artifact`.

## The finding the original filing hid

It was filed as "12 file headers". **29 of the 30 sites are comments. One is not.**
`forge/src/deploy/seeds.ts:57` sits inside `proceduralSeed()`'s template literal — prose written
into **every deployed agent's `PROCEDURAL.md`**. A comment sweep would have walked past the only
site that reaches a reader.

## Constraints

- Rival signs are live and must not be swept: `agentDef` (`claude/anatomy.ts:230`,
  `codex/anatomy.ts:304`, `core/harness-adapter.ts:153`) and `HarnessProjection`
  (`core/harness-adapter.ts:24`). This shard is `SOUL → Target` only.
- **Canon's own 35 `SOUL` lines are OUT OF SCOPE** — `t-canon-soul`, ruling-blocked, strictly larger.
- `seeds.ts:57` changes deploy output; it also intersects `t-seed-prose-has-drifted`. Sequence them.

## Acceptance

- `grep -rn SOUL packages/forge/src` → nothing.
- Render oracle unmoved (forge comments are not projected); **deploy seed output diff shown**.

## Execution

<!-- GENERATED from ../spec.mjs by ../sync-shards.mjs. Edit the spec, not this block. -->

- **slice** forge-deploy · **wave** 0
- **depends on** —
- **writes** `packages/forge/src/adapters/**` · `packages/forge/src/core/**` · `packages/forge/src/deploy/**` · `packages/forge/src/project/index.ts`
- **compiles against** `packages/forge/src/validate/accept.ts`
- **evidence** `packages/forge/src/deploy/seeds.ts` · `packages/forge/src/validate/accept.ts`
- **dispatchable** no ruling owed
