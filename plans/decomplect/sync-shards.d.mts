// Types for `sync-shards.mjs` — the projection of `spec.mjs` into each shard and into PLAN.md.
export declare function block(id: string): string;
export declare function waves(): string[][];
export declare function sequence(): string;
