# Census 2026-08-05 — every open item re-measured against the tree

> Four independent readers, one instruction: **verify, do not trust the filing**. Every number below
> came from a command the reader ran and cited. This supersedes the verdicts embedded in `PLAN.md`,
> `CRATYLISM-SWEEP.md` and `EVENT-VOCABULARY.md` wherever they disagree — those files stay as the
> record of what was believed when they were written.

**The headline is subtraction.** Of the items carried as open, **seven are dead** — discharged,
refuted, or superseded — and several of the survivors are not the defect they were filed as. The
plan was carrying work that no longer existed, and reading it as a work list would have produced
motion without value.

## Struck — do not re-open without new evidence

| item                                | why it dies                                                                                                                                                                                                                      |
| ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **C2** command-veracity             | DISCHARGED instance _and_ class by `06543456`. The gate exists, reaches `git ls-files`, and pins both original sites as positive controls.                                                                                       |
| **C1·A** (the sign)                 | DISCHARGED by `48baaddd`. `type Anatomy` → **0 hits**. `DimensionManifest`/`MANIFEST` are live. Only the _file basename_ survives — re-filed as a separate shard.                                                                |
| **A7** `dimensions/model/claude.ts` | REFUTED. `model` is a declared dimension (`anatomy.ts:84`); a closed enum of model names naming a vendor product is the dimension WORKING. Filed on a filename match.                                                            |
| **A9** σ\*-register law             | DISCHARGED by `a2205eb`. `Art ≜ every authored surface`; zero harness path tokens in the cell.                                                                                                                                   |
| **A10** `timeout` on cells          | REFUTED three ways: the schema ruled it harness-agnostic _in prose written for this question_; two independent adapters consume it identically; the 4 cells carrying it are exactly the 4 `substrate:'harness'` cells.           |
| **A6** `materialize`                | REFUTED (the other two members of the row survive). Its `.md`+front-matter is one NAMED STRATEGY's consumption table with `s ∉ S ⇒ ⊥`, not an asserted law. Filing it beside the other two conflated a strategy body with a law. |
| **B16** `core/anatomy-body.ts`      | REFUTED as misplacement. Enumerated against ARCHITECTURE's criterion it CARRIES rather than decides: section order is a parameter, values pass through verbatim. One-token residual re-filed.                                    |

## Superseded — and this one is a live hazard

**C6 — "the BRAND: ⊥, do not mint" is OVERTURNED and the sweep does not say so.** `49516a6f` minted
both: the discipline is **latent lexicography**, the instrument is **Cratylus**, and the scope is
`@cratylus/*` across six packages. Anyone reading `C6` as current state will be wrong.

**And the ⊥ was ruled without testing the candidate that won** — `Cratylus` appears nowhere in C6's
nine-candidate table.

> **The general lesson, and it is the most portable thing in this census.** A NEGATIVE result — "no
> name won", "this returns ⊥" — decays exactly like a positive one, but nothing re-checks it,
> because a `⊥` leaves no artifact to convict and no string to grep. A refusal must be carried with
> its DATE and its CANDIDATE SET, and the set re-verified before the refusal is honored again.

## Live and MECHANICAL — a determinate target exists today

| shard                           | subject                                                                             | measured blast radius                                                                                                                                                    |
| ------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `t-manifest-file-basename`      | C1·A residual — the sign is `manifest`, the module is `anatomy.ts`                  | **177** importing files — 171 `canon/src` + 6 `canon/test` (ARCHITECTURE's "154" is stale); `FIXTURE_ANATOMY` **75** sites / 17 files; 6 forge param names               |
| `t-anatomy-root-compose`        | C1·B — `anatomyRoot`, ⊥ confirmed by inspection                                     | 8 files / 18 sites, + 5 `anatomyProjectTemplate` sites. PLAN said 10 files, sweep said 19; **truth is 8**                                                                |
| `t-capture-row`                 | UA1 — `export interface Record` shadows the global; both consumers already alias it | 3 files / 8 sites. The discovered sign is in the alias                                                                                                                   |
| `t-shim-path-from-capability`   | A12 (`memory` half) — the cell restates a path the projector computes               | 15 literals / 3 cells. `eventTap`'s 5 wait on C4                                                                                                                         |
| `t-soul-to-target-in-forge`     | C5 — and 1 of the 30 sites is **not** a comment                                     | 11 files / 30 lines; `deploy/seeds.ts:57` ships into every agent's `PROCEDURAL.md`                                                                                       |
| `t-kind-root-ignores-agent-ext` | `KIND_ROOT` hardcodes `.md` while the port declares `agentExt`                      | **A live wrong answer**: `unattributable` reports zero orphans for every codex deploy. 1 constant, 1 fn, 1 call site                                                     |
| `t-init-hardcodes-harness-dir`  | `deploy/init.ts:76-77` hardcode `.claude` with no parameter                         | 2 lines. The sibling `deploy/scope.ts:50,71` already has it as a defaulted parameter                                                                                     |
| `t-policy-seam-unused`          | B11–B14 — **premise corrected: the seam is NOT empty**                              | `Policy` has 3 members, `canonPolicy` supplies all 3, threaded through 17 live call sites. Four doctrine constants sit beside a working seam and none goes through it    |
| `t-seed-prose-has-drifted`      | B7 — escalated                                                                      | The TODO's own release condition IS satisfied (`memory` exports `seedTemplates`), and the two writers now emit **different bytes** — 2 divergences across 3 seeded files |
| `t-canon-package-default`       | B17 — `cratylus init` offers no override                                            | 7 sites, **2 of them test pins** that turn the suite red on repair                                                                                                       |

## Live but RULING-BLOCKED — a move has no determinate target yet

Each names the decision owed. **None is an executor's**, and each was stopped at that boundary
rather than guessed.

| shard                       | ruling owed                                                                                                                                                                                                                                                                                                                                                                   |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `t-projection-file-anchor`  | C1·C — the sign for "harness projection file". The one leg run returned `capabilities`, rejected on occupancy against runtime's keyspace                                                                                                                                                                                                                                      |
| `t-tap-anchor`              | C4 — one capability wears three signs (`eventTap` · `event-tap` · `tap`). **Two corrections to the filed remedy:** the proposed `≡ canon skill name` axis has NO positive control (`memory` is claimed by three skills, none named `memory`), and `provisional-v9/` is deliberately outside the keyspace, so any `dir ≡ keyspace` gate needs an exemption — itself a decision |
| `t-project-human-vs-engine` | C3 — `projectHumanDimension` has 0 callers and is not even exported, but `ENGINE.md:40-44` declares `project-human` a member of `boundary-projection` and says ENGINE owns it. **Deleting the sole implementation leaves ground declaring an operation the engine does not realize.** Either ENGINE is revised in the same act or the deletion is refused                     |
| `t-tool-class-vocabulary`   | A2 — needs a canonical tool-class vocabulary (confirmed absent: 0 files) _and_ a ruling on which register owns `matcher`. **The schema contradicts itself three ways on this field**, and one description ships the defect as its example. Worse than filed: codex drops the matcher silently, so the hook fires on every tool call there                                     |
| `t-substrate-concept`       | A5 — the line names a field the shape does not have (`Agent` has no `substrate`) and denies a catalog membership that exists. Three incompatible repairs; picking one is a design                                                                                                                                                                                             |
| `t-authoring-surface`       | A6 — the asserted law matches **no** surface this repo produces or consumes. Four live surfaces, four shapes; the cells describe a fifth. Even on claude the front-matter keys are wrong (`kind:` is emitted nowhere)                                                                                                                                                         |
| `t-property-1-counter-gate` | The sole surviving property-1 breach, **double-pinned**: `bin-name-single-home.test.ts` REQUIRES the import. The ruling is on the GATE, not the cell — by what seam does a cell's verbatim worker payload obtain a runtime-owned literal? Any remedy must keep an emitted-artifact-level assertion alive                                                                      |
| `t-lifecycle-vocabulary`    | 28/28, identical as sets AND in order, nothing enforcing it, consumers fully disjoint. **Correction: the duplication is schema-vs-runtime now, not forge-vs-runtime** — and ARCHITECTURE has no edge between them in either direction, so unifying needs an edge the north star does not contain                                                                              |
| `t-definiens-vs-residue`    | UA4 — same package, same directory, one concept two signs, with a test writing `definiens: c.residue` purely to bridge them. Direction is determined (`residue` is MODEL's) **but `definiens` is also a live ρ-class name for a different referent** — the naive sweep is 88 sites and would be wrong                                                                         |
| `t-rule-cell-body`          | UA5 — `RuleCell.body` (verbatim directive) vs `HookCell.body` (`⟨α, residue⟩`) in sibling files. A ρ-class `'rule-target-body'` was already minted to route around the collision. Needs a mint                                                                                                                                                                                |
| `t-coined-classification`   | C3's separable half — `Classification = … \| 'coined'`, and `cratylism.ts` (the axiom saying `¬coined`) lives inside a dimension typed `coined`                                                                                                                                                                                                                               |
| `t-signify-marker`          | UA3 — one surviving `[SIGNIFY: …]` marker shipped in `catalog/index.ts:283`. An owed cold-decode pass                                                                                                                                                                                                                                                                         |
| `t-accept-fifth-kind`       | UA2 — `accept.ts:52` admits `'hook'`; `MODEL.md:10` has four Kinds. Needs the ground-conformance PROPERTY stated first; the schema already argues the counter-case                                                                                                                                                                                                            |
| `t-canon-soul`              | C5's linked question — 15 files / 35 lines in canon, **9 lines in 5 shipped cells**, so it moves the render oracle. Strictly larger than C5 and the operator's                                                                                                                                                                                                                |

## Newly discovered, not previously filed

- **The claude event map is duplicated too.** `forge/adapters/claude/events.ts` and
  `runtime/capabilities/event-tap/claude-serialize.ts` carry the **same 18 key→value pairs**,
  byte-identical, agreeing by the same coincidence with the same nothing enforcing it. The
  duplication is a declaration _and its adapter table_, cloned across the seam.
- **"Nine realizable on no harness" is ten.** Measured against both shipped adapters: 10 of 28 have
  no native peer. The symmetric-pair signature holds for 8; `notification` and `session.resume` are
  unpaired singletons and do not carry it.
- **`vcs.commit.post` moved with the schema** and is no longer "hardcoded in forge". The originating
  cell's prose still says forge — stale by one package.
- **`command-veracity` skips `**/test/**` by design**, so PLAN's "~45 more convictions across
  markdown links and source-comment path citations" is a **different, uncovered property**. One
  instance found: `docs/research/harness-reach-matrix.md:48` cites a test path that does not exist.
- **Four dead path citations in `CRATYLISM-SWEEP.md`** (`forge/src/anatomy/*`, both `project-cli*`),
  and the sweep's own header is out of date on its own contents: it carries C6, and PLAN says "three
  unassigned findings" where the sweep lists **five**.

## The open shards — and one that inverts

| shard                                           | verdict                                                                                                                                                                                                                                                                |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `elevate-installs-no-mechanism`                 | **LIVE, verbatim.** `carry-on/skill.ts:12` is a state assignment with no `install(…)` term, `:16` no release, and the cell declares no `runtime:` capability — it routes to no mechanism at all                                                                        |
| `hook-message-has-no-declared-home`             | **LIVE, verbatim.** `HookCell` has no message field; the four strings are `printf` bodies; the KNOWN GAP paragraph the acceptance says to delete is intact at `reader-density.test.ts:98-104`                                                                          |
| `project-never-cleans-its-out-dir`              | **LIVE.** `project/write.ts:25-35` imports `chmodSync, mkdirSync, writeFileSync` and nothing else; no `--clean` flag. **The corpus already depends on the workaround** — `render-oracle.sh:29` removes the out dirs first, so the oracle's reproducibility rests on it |
| `bin-name-gate-stops-at-the-language-boundary`  | **CHANGED — instance repaired, property not.** The gate now crosses the language boundary for exactly ONE file, reached by a `targetPath`, not by a scan. There are **13** shell/`.mjs` sources under `packages/*/src`; **12 are watched by nothing**                  |
| `deployed-drifts-from-rendered-unwatched`       | **LIVE, verbatim.** No `--check`, no drift module, nothing compares bytes. `deploy`'s manifest-bounded **prune** subtracts orphans — that is not the property                                                                                                          |
| `the-host-install-is-a-symlink-nobody-authored` | **LIVE.** The host works only via a hand-authored 86-byte shim no artifact in this repo produces, carrying an absolute checkout path — the original failure mode reproduced, not repaired                                                                              |

### `t-config-filename-carries-the-retired-brand` — REFUTED as written, and the defect INVERTED

This is the sharpest correction in the census, and it is worse than what `PLAN.md` recorded.

- **One-home: MET.** `memory/src/node.ts:306-318` is the single home; both former duplicates delegate
  to `resolveConfigPath`. The precedence chain is written once.
- **Derivation: ZERO RECORD.** No argmin, no blind reverse decode, no occupancy check, and no
  comparison against `.cratylus-run.json` anywhere in the tree.
- **The inversion:** the retired brand was `agent-factory`, and `61b85db7` did not merely rewrite the
  shard — **it executed the rename the shard forbade.** One commit both collapsed the literals into
  one home and picked `.cratylus.config` / `CRATYLUS_CONFIG`, the exact string the shard names as
  _"the obvious guess"_ and bans. `git log -S` confirms all three literals entered at that commit.

**So the plan's own diagnosis stopped one step short.** It said the derivation leg was _unpaid_. It
was not unpaid — **the name was already shipped on the un-derived guess, and the artifact that would
have said so is the one the same commit made unreadable.** The shard is not blocked pending a
decision; it is a decision already taken by default and never recorded.

And the sibling **is** derived: `runtime/src/runtime-config.ts:34` —
``RUNTIME_CONFIG_NAME = `.${RUNTIME_BIN}.json` ``. So the shard's question 1 is answerable and was
never asked.

## Two readers disagreed, and I recounted rather than picked

The mechanical table above quotes **177** and **75**. Two independent readers returned 171/74 and
177/75 for the same two questions. Neither was wrong: 171 counts `canon/src` alone, 177 adds the 6
files in `canon/test`; the `FIXTURE_ANATOMY` gap was a straight off-by-one.

I re-ran both myself rather than adopting either number:

```sh
grep -rlE "from '(\.\./)*anatomy\.js'|from '\./anatomy\.js'" packages/canon/src | wc -l   # 171
grep -rlE "from '(\.\./)*(src/)?anatomy\.js'" packages/canon/test | wc -l                  # 6
grep -ro 'FIXTURE_ANATOMY' packages | wc -l                                                 # 75
grep -rl 'FIXTURE_ANATOMY' packages | wc -l                                                 # 17
grep -ro 'anatomyRoot' packages | wc -l ; grep -rl 'anatomyRoot' packages | wc -l           # 18 / 8
```

Recorded because this census exists to stop numbers being quoted forward unmeasured, and adopting
one of two disagreeing figures without re-running it would have been that exact defect.

## Numbers this census corrects

Four load-bearing figures in ground and plan are wrong. Each was quoted forward and never recomputed
— the same class this session already caught itself committing.

| site                                 | says                                                                | truth                                                                                                                   |
| ------------------------------------ | ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `ARCHITECTURE.md:256`                | `anatomy.ts` has **154** importers                                  | **177** files (169 import statements; 6 files import twice)                                                             |
| `ARCHITECTURE.md:260`                | `FIXTURE_ANATOMY` at **~110** sites                                 | **75**, across 17 files — and it was 75 at `48baaddd` too, the very commit that wrote `~110`. **It was never measured** |
| `PLAN.md:135`                        | gate acceptance `cells 0 · build scripts 6 · root 1 · property-1 1` | `architecture.test.ts:375-399` asserts **build scripts 4 · root 0**                                                     |
| `ARCHITECTURE.md:259`, `PLAN.md:263` | the breaching import is at `memory-consolidation-nudge.ts:2`        | it is at **`:1`**; `:2` is the schema import                                                                            |

## Property 1 — confirmed, and the trap is tighter than recorded

The breach is real, both gates green, and **five assertions across two files** go red on a repair —
in a configuration with no single-file escape:

- `bin-name-single-home.test.ts:98` requires `RUNTIME_BIN` **in the cell's source**, while `:94-97`
  forbids the cell from **spelling the literal**. Together those admit exactly one mechanism —
  importing it. The import is required _by construction_, not merely tolerated.
- `architecture.test.ts:396-399` asserts the breach **count** is 1, so a repair fails there even
  with the pin removed; and `:445-455` is shrink-only, so **leaving** the pin after a repair fails
  too. Both exits are closed on purpose.

**The obvious alternative is not free.** Having forge interpolate at projection time moves the bin
name out of `workers[].content`, which is a **committed byte-anchor** regenerated to a `.sh` and
byte-locked by `hook-rule-boundary.test.ts`. That changes what "byte-lock" means for the class — a
change to the cell/worker contract in `schema`, not a refactor — and it intersects
`hook-message-has-no-declared-home`, which wants a declared field for exactly this kind of emitted
content. **These two shards are one design question wearing two filings.**
