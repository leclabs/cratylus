# T4 open-value-gen

R=LLM. lead: Nico. dep: T3.

obj ≜ for each **open** organ (from T3), blind-source a set of **opinionated but generalized** values that
**cover the scope of common opinionated open values** for that dimension — a curated composable vocabulary,
NOT per-agent one-offs.

do ≜ per open organ, spawn blind subagents asked for "a generalized, opinionated, composable set of values a
custom-agent builder would pick from for <organ>, covering the common cases without bespoke terminology."
Reconcile across draws; Nico signifies each to its `σ*_R` anchor (one name ⇔ one concept; fittest sign).
**Every fragment rendered for LLM-as-reader** (dense, symbolic, anchor-bearing — `σ*_LLM`, not human prose).

acc ⊨ each open organ has a proposed generalized value-set (anchors + LLM-reader bodies), signify-clean
(injective anchors, no synonym collisions), ready for T5 materialization. Proposal only — no cell writes yet.
