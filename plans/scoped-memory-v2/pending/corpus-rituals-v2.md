# corpus-rituals-v2 — CoALA stores + lattice/fold law in the cells

**Lane** Nico · **wave(0)** · deps: none (SPEC static) · HELD until Operator approves `../SPEC.md`.

## Static

`../SPEC.md` D1 (stores + projection-dedup bar), D3 (lattice, `node(cwd)`), D4 (fold + `--replay`,
plan-node next-steps law), D5 (ritual bindings, out-of-node law, lock, audit allow-file), D6 (in-repo
policy). Surfaces: `packages/agent-anatomy/ideas/memory.md` (Protocol kernel + schema + Tool) ·
`src/skills/{dream,wake,handoff}.ts` · `ideas/persona.md` (verify: no store-name references) ·
`src/toolkit/make-base.ts` chain (base.ts regen) · `src/organs/memory/long-term-memory.ts`
(definiens already CoALA — verify, don't churn).

## Scope

Corpus cells ONLY (no runtime, no hooks). (1) memory cell: kernel names the stores
(EPISODIC · SEMANTIC · PROCEDURAL; working = context window), encode duty with derived telemetry,
schema = the D2 record + lattice summary, Tool = v2 verbs; kernel stays one-glance (≤ current
+2 lines). (2) dream cell: fold law (deterministic manifest → semantic re-judge with the
projection-dedup bar), `--replay` law, D6 policy lines, lock precondition, routing over lattice
nodes incl. plan-node next-steps. (3) wake cell: `read --under <node(launch cwd)>` load +
out-of-node law + store names. (4) handoff cell: verify consistency, no new machinery. (5) Every
`SELF`/`MEMORY.md` store reference across cells → the CoALA names. make-base regen; project;
reader-density bar.

## Accept (falsifiers)

- Projected SOUL Memory Protocol names the three stores + the derived-telemetry encode line; grep
  finds NO live `SELF.md`/`MEMORY.md` store reference in any projected artifact.
- dream cell states fold-manifest → re-judge, the projection-dedup bar, `--replay`, and the D6
  policy; wake cell states the out-of-node law verbatim-equivalent to SPEC D5. A blind reader
  executes both rituals from the cells alone.
- Suite green (null-organ, reader-density, projection-stability, skill-shape, symbols); repo gates
  4×0.
