# corpus-rituals-v2 — CoALA stores + node/fold law in the cells

**Lane** Nico · **wave(0)** · deps: none (SPEC static) · HELD until Operator approves `../SPEC.md`.

## Static

`../SPEC.md` D1 (stores + projection-dedup bar), D2 (record shape), D3 (`node(p)` — resolved by the
tool, agents invoke via the memory skill), D4 (fold + total route incl. plan-node next-steps +
in-repo law), D5 (ritual bindings). Surfaces: `packages/agent-anatomy/ideas/memory.md` (Protocol
kernel + schema + Tool) · `src/skills/{dream,wake,handoff}.ts` · `ideas/persona.md` (verify: no
store-name references) · `src/toolkit/make-base.ts` chain (base.ts regen) ·
`src/organs/memory/long-term-memory.ts` (definiens already CoALA — verify, don't churn).

## Scope

Corpus cells ONLY (no runtime). (1) memory cell: kernel names the stores (EPISODIC · SEMANTIC ·
PROCEDURAL; working = context window); encode duty unchanged (tool derives cwd/host); schema = the D2
record + `node(p)` summary; Tool = v2 verbs; kernel one-glance (≤ current +2 lines). (2) dream cell:
fold manifest → semantic pass with the dedup bar; the **total route in set notation, no modal
verbs**; in-repo law (versioned AGENTS.md at nodes + gitignored node views, nothing else); lock
precondition. (3) wake cell: load = SEMANTIC + PROCEDURAL whole + `read --under` (tool-filtered;
out-of-node = counts); audit exit gate. (4) handoff cell: consistency check only. (5) Every
`SELF`/`MEMORY.md` store reference in any cell → the CoALA names. make-base regen; project;
reader-density bar.

## Accept (falsifiers)

- Projected SOUL Memory Protocol names the three stores; grep finds NO live `SELF.md`/`MEMORY.md`
  store reference in any projected artifact.
- dream cell carries the route as a total function (grep: no "may write"/hedge on route targets);
  wake cell's load line names `--under`. A blind reader executes both rituals from the cells alone.
- Suite green (null-organ, reader-density, projection-stability, skill-shape, symbols); repo gates
  4×0.
