# corpus-rituals-v2 — CoALA stores + lattice/fold law in the cells

**Lane** Nico · **wave(0)** · deps: none (SPEC static) · HELD until Operator approves `../SPEC.md`.

## Static

`../SPEC.md` D1 (store dissolution map), D3 (lattice, out-of-territory law), D4 (pipeline, re-dream),
D6 (in-repo policy), D5 (ritual bindings). Surfaces: `packages/agent-anatomy/ideas/memory.md`
(Protocol kernel + schema + Tool) · `src/skills/{dream,wake,handoff}.ts` · `ideas/persona.md`
(check: no store-name references) · `src/toolkit/make-base.ts` chain (base.ts regen) ·
`src/organs/memory/long-term-memory.ts` (definiens already CoALA — verify, don't churn).

## Scope

Corpus cells ONLY (no runtime, no hooks). (1) memory cell: kernel names the v2 stores
(SEMANTIC · PROCEDURAL · EPISODIC; working = context window), encode duty unchanged with derived
telemetry noted, schema section = the D2 record + lattice summary, Tool section = v2 verbs; kernel
stays one-glance (≤ current +2 lines). (2) dream cell: strategy-pipeline law (deterministic fold
manifest → semantic re-judge), D6 in-repo policy, re-dream law, lock precondition, routing table
re-expressed over lattice nodes. (3) wake cell: territory-filtered load + out-of-territory law +
store names. (4) handoff cell: `session`-less (registry declined) — verify no stale refs. (5) All
`SELF` mentions across cells → the dissolved stores. make-base regen; project; reader-density bar.

## Accept (falsifiers)

- Projected SOUL Memory Protocol names the three stores + the derived-telemetry encode line; grep
  finds NO live `SELF.md` reference in any projected artifact (corpus-wide; migration docs exempt).
- dream cell states: fold-manifest-then-re-judge, the D6 policy lines, re-dream; wake cell states the
  out-of-territory law verbatim-equivalent to SPEC D5. A blind reader executes both rituals from the
  cells alone.
- Suite green (incl. null-organ, reader-density, projection-stability, skill-shape, symbols); repo
  gates 4×0.
