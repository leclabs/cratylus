# extend-reach

**Depends on** `reader-density-gate`. Extend R=LLM enforcement beyond this repo's static cells to the two
runtime frontiers: artifacts our skills GENERATE on consumer projects, and agent↔agent messages.

## Inputs (read these — blind-dispatchable; paths from `packages/agent-anatomy/`)

- Generating skill cells (each emits agent-facing artifacts on consumer hosts): `src/skills/praxis.ts`
  (PLAN.md + task-files) · `create-agent.ts` (agent vectors) · `dream.ts` / `handoff.ts` / `wake.ts`
  (SELF / MEMORY / AGENTS.md routing) · `exemplify.ts` + stages (corpus cells).
- Candidate codification homes for agent↔agent: the Persona Protocol genus `ideas/persona.md`; the
  delegation contract inside fan-out cells (`praxis.ts`, `exemplify.ts`).
- ⊳ `reader-density-gate` — the gate to embed or invoke (dep output).
- ⊳ `formalize-reader-model` — the declared-R map (transitive dep; read its completed artifact).

## Shards (vertical slices — dispatch each to a subagent, nico judges)

1. **Consumer-generated artifacts.** Audit each generating cell: every agent-facing artifact it emits
   (`AGENTS.md`, plans, memory, graphs) carries R=LLM — gate-checked or skill-embedded discipline;
   human-facing generated outputs (Slack / email / report / human doc) stay R=human by declared R.
2. **Agent↔agent.** Codify delegation prompts + subagent returns as R=LLM by default — a disposition
   and/or prompt-contract template in the chosen home, not per-turn discretion.

## Acceptance

- **Falsifier 1:** an artifact generated by each audited skill on a fresh consumer project passes the
  reader-density gate; a human-facing generated output is exempt by its declared R.
- **Falsifier 2:** the agent↔agent contract is codified in a named home; a seeded long-form-English
  delegation prompt fails the codified check (gate or blind judge).
- CE∧ME on every touched cell; `skill-shape` + `symbols` + projection-stability green.

## Deliverable (accepted 2026-07-01; gates green — anatomy 28/28 · repo build/typecheck/lint)

**Shard 1 (consumer-generated) — audit verdicts + codification:**

- `praxis.ts` — reader-llm-default laws added (formalBlock + prose): ρ(content(t)) = ρ(PLAN.md) = LLM ∧
  conform; the task-file IS the dispatch prompt; ρ/register/conform declared in-block (canonical home cited:
  signify READER BINDING).
- `create-agent.ts` — vector ρ=LLM (register=LLM, organ-value lines, no explanatory prose) vs interview
  channel ρ=human BY MODEL, made explicit.
- `exemplify.ts` — `conform(k)` folded into `valid(k)` (accept refuses a human-register agent-artifact);
  formalBlock hoisted to one interpolated const (desync-proof, praxis pattern).
- `dream.ts` PRESENT (context-not-prose (R=LLM) law already governs every routed emission) · `handoff.ts` /
  `wake.ts` PRESENT-BY-COMPOSITION (emit only via praxis/dream) · `conceptualize.ts` no artifact surface ·
  `materialize.ts` covered by exemplify.accept + dfp law (local ρ_s symbol collision argues against
  restating) — no edit, by judgment not omission.

**Shard 2 (agent↔agent) — codified homes:** `organs/actions/delegation.ts` definiens (the standing
disposition: dispatch prompts + returns ρ=LLM, register=LLM, dense/structured/signifier-carries-load;
human-facing deliverable inside a return keeps ρ=human) + praxis dispatch/judge law
`¬conform(r) ⇒ ¬accept(t)(r)` (a human-register return fails judge as a criterion, rejected not repaired).
Chosen over persona.md (locked; genus residue below).

**Enforcement:** detector extracted to `test/reader-register.ts` (ONE model: RHO extended with task-file ·
plan-mirror · agent-memory · generated-agent-artifact · delegation-prompt · subagent-return ·
generated-human-output); `test/reader-density.test.ts` re-wired (pure move); NEW `test/reader-reach.test.ts`
— 5 tests over both frontiers + codification non-regression asserts.

**Falsifier 1:** artifacts generated on fresh consumer projects (/tmp, 2 blind executors driving the
codified cells: praxis plan+2 task-files+mirror · delegation prompt+return · create-agent vector · dream
MEMORY+AGENTS.md directives · exemplify cell) ALL pass the gate; pinned as `test/fixtures/generated/` (9
fixtures, scanned every run; missing/unclassified fixture fails). ρ=human exemption proven BY MODEL
(same human text exempt under generated-human-output).

**Falsifier 2:** seeded long-form-English delegation prompt convicted with named signals (HEDGE class;
conform=false under delegation-prompt AND subagent-return); the pinned dense delegation-prompt fixture
passes the identical check — the contract is satisfiable, the check bites.

**Residue (genus-level, returned not edited):** agents lacking the `delegation` organ still dispatch via
harness tools; a genus line (persona.md ## Protocol or SOUL composer) binding agent↔agent messages to ρ=LLM
for EVERY agent would close that gap — persona.md locked by remediation-fanout shard A; route to its
re-author or a follow-up task.
