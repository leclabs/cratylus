# T4 — four-way commutation table

Sample includes the plan's OWN artifacts (test case #0: PLAN Intent + T0 law) + high-signal realigned/coined
fragments. Agreement = each party equals `R_cold` (the fixed point), never consensus-against-it.

| #   | fragment                         | R_cold (oracle) recovers                                                                                                  | nico-in (warm) | nico-out (clean) | operator | ≡?     |
| --- | -------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | -------------- | ---------------- | -------- | ------ |
| 0a  | PLAN.md Intent                   | naive-cold read is the one true meaning; warm must match; mismatch=project bug, fix one-way, never bend the plain reading | ≡              | ≡                | ≡        | ✅ 4/4 |
| 0b  | T0 law `cold-decode-oracle`      | cold read = the oracle/authority; warm confirms never overrides; m1/m2; corpus=defendant                                  | ≡              | ≡                | ≡        | ✅ 4/4 |
| 1   | provenance `nico-archetype-cyan` | nico's standing/authority intrinsic (principal-IC lineage); exercise deferred to human-on-the-loop                        | ≡              | ≡                | ≡        | ✅ 4/4 |
| 2   | actions `delegation` (ρ=LLM)     | orchestrator dispatches sub-agents; ρ=reader-binding: agent-to-agent dense vs human-facing prose                          | ≡              | ≡                | ≡        | ✅ 4/4 |
| 3   | skill `signify`                  | assign each concept its injective canonical anchor (fittest sign, one-to-one), merge collisions                           | ≡              | ≡                | ≡        | ✅ 4/4 |

**Divergence count (3 agent parties): 0.** nico-inside (warm, full-K) and nico-outside (clean) both equal
`R_cold` on every sampled fragment — including the plan's own artifacts. No party reached agreement by
overriding the oracle (each reading is oracle-derived, attested against the isolated transcripts in
`completed/T3-sweep-result.md` + the GateA/GateB transcripts).

**Test case #0: GREEN** — the plan satisfies its own criterion (DoD(e)).

**Open (operator party):** the 4th reader is the operator by design — Lex confirms the sampled meanings match
intent. This is the one commutation cell I cannot fill by assertion (the falsifier forbids a party asserting a
reading). Surfaced at the commit gate. On confirmation → divergence count 0/4, T4 ratified.

---

## RATIFIED (2026-07-03) — operator confirmed

Lex confirmed the sample reads to intent. **Divergence 0/4.** All four parties (oracle · nico-inside · nico-outside · operator) commute through R_cold. Test case #0 green. DoD(c) `∀f warm≡cold≡intent` + DoD(e) plan-self-pass MET.
