# RUNTIME_CONFIG_NAME is brand-derived but an independent literal - a rebrand silently orphans every host existing config file

> FILED, not specified. A stub: symptom + locus + provenance, no census, no
> acceptance. It exists so the defect was not chased when it was found. Whoever
> promotes it to `ready` owes it a real spec (`/praxis upsert`).

**Symptom.** RUNTIME_CONFIG_NAME is brand-derived but an independent literal - a rebrand silently orphans every host existing config file

**Locus.** _(unfilled — the filer may not have known)_

**Provenance.** Filed 2026-07-26 from `b6cfc7b`, while executing `V5`.
