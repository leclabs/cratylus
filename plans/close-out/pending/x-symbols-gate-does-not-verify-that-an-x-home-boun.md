# symbols gate does not verify that an X @ home boundary-binding resolves to a real declaration in that home

> FILED, not specified. A stub: symptom + locus + provenance, no census, no
> acceptance. It exists so the defect was not chased when it was found. Whoever
> promotes it to `ready` owes it a real spec (`/praxis upsert`).

**Symptom.** symbols gate does not verify that an X @ home boundary-binding resolves to a real declaration in that home

**Locus.** _(unfilled — the filer may not have known)_

**Provenance.** Filed 2026-07-26 from `f35aafd`, while executing `praxis repair`.
