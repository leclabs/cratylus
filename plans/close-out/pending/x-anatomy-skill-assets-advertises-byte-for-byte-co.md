# anatomy Skill.assets advertises byte-for-byte companion shipping that the projection never honours - a lying type

> FILED, not specified. A stub: symptom + locus + provenance, no census, no
> acceptance. It exists so the defect was not chased when it was found. Whoever
> promotes it to `ready` owes it a real spec (`/praxis upsert`).

**Symptom.** anatomy Skill.assets advertises byte-for-byte companion shipping that the projection never honours - a lying type

**Locus.** _(unfilled — the filer may not have known)_

**Provenance.** Filed 2026-07-26 from `a386e1a`, while executing `retired-plan audit`.
