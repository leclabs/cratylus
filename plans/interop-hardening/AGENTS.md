# interop-hardening — plan-scope memory (open threads)

_The plan node's semantic sink (dream-written, net-current). `PLAN.md` is the state mirror; this
file carries the working thread._

## Open threads

- **Wave 4 resume (first act of the next session):** `engine-report-machinery` ·
  `exemplify-pipeline` · `zed-adapter` executors died on the account session limit (resets
  2026-07-04 01:30 CT); their partial edits sit UNCOMMITTED in the working tree as resume state —
  never commit/clean them. Resume each via SendMessage to its agent id, then: joint-tree gates 4×0
  → wave-4 judge sweep (owned-id graduation math against 220/64) → release wave 5 (10
  adapter-truth + 4 new-adapter + standards-surfaces shards; widest wave).
- **Hand-off notes into wave 5:** `test/stories/E7/s07` RULE_B carries a now-unneeded
  `as unknown as Rule` cast — drop it when its 4 adapter-owned tracked tests graduate (codex-class
  shards). IR `mcp.auth` modeled loosely (`{type: string, …}` per [CU5]) — tighten if the cursor
  shard finds richer documented structure. `test/stories/E4/support.ts` `EXCLUDED_FIELDS` carries
  dated exclusions with a graduation path: when an adapter shard lands emit/lift for a field, move
  it into `FIXTURES`.
- **Judge law for graduations:** every shard return re-verified by owned-id set math (count +
  set-equality vs `TRACKED-FAILING.md`); zero non-owned flips; suite count printed in `pnpm test`
  is the arithmetic check.
