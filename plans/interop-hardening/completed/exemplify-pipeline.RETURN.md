# RETURN — exemplify-pipeline (wave 4) · judged PASS

commit `2a9de62` — exemplify pipeline: optimize verb, accept gate, elevation, vector projection. 25 files, +1955/−163.

- **Graduated (owned 13/13):** E6.S1×2 (accept gate REC≽·minimal·conform; R3 manifest + withheld⇒refuse) · E6.S2×2 (cell well-formedness; answer-key round-trip) · E6.S3×2 (24-organ elevation w/ provenance; replacement semantics) · E6.S4×2 (ELICIT markers + bisecting scripts; invented-enum negative) · E6.S5 (idempotence byte-identical) · E6.S6 (optimized artifacts ride compile) · E6.S7 (import→optimize→compile e2e) · E6.S8 (rules first-class) · E4.S8 (`projectVector` on core surface).
- **Tracked:** 211 → 198/53 (−13). Judge verified: raw diff 14 removed = 13 net + 1 aider-row reflow re-add; zero non-owned flips.
- **Gates:** 4×0 in isolated HEAD worktree; e2e via shipped `dist/cli` bin (accept+manifest, `--prior` all-reuse byte-identical, conform refusal w/ named markers, `s=∅⇒⊥`, withheld/garbage refusals — exit 1, named reasons).
- **Semantic seams (doc'd in `src/core/exemplify/types.ts` + README):** plan is LLM-authored (frame refuses without one, never defaults) · `conform` = mechanical marker-lexicon under-approximation · REC≽ mechanical leg = anchor/data containment · ElevationSpec never-invent enforced by frame (quote-traces, 24-key completeness).
