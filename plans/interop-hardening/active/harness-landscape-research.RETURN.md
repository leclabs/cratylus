# harness-landscape-research — RETURN (2026-07-02, ρ=LLM)

Web-audited ground truth per harness; the library's own adapter code was read ONLY for §3
(divergence comparison), never cited as evidence about a harness. Citation keys `[XX#]` resolve in
§5 Sources. `UNVERIFIED` = could not be confirmed from a fetched source; never guessed.

## §0 Field state 2026-07 (affects target selection)

- **Consolidation events**: Windsurf → **Devin Desktop** (2026-06-02; Cascade → Devin Local; `.devin/rules/` preferred, `.windsurf/*` fallback) [WS7]. **Roo Code sunset** 2026-05-15 (docs archived; recommends Cline) [RO5]. **Kilo Code → Kilo**, rebuilt on an opencode-derived runtime (`.kilo/` + `kilo.jsonc`; `.kilocode/*` legacy auto-migrated) [KL1]. **Gemini CLI → Antigravity CLI** (consumer serving ceased 2026-06-18; skills/hooks/subagents/extensions carried over) [GM7]. **Copilot Extensions (GitHub Apps/skillsets) dead** 2025-11-10 → MCP is the extension path [CP9]. **Amp rebuilt ("neo")** 2026-05-06: toolboxes/custom-commands/hooks/permissions folded into a TS Plugin API [AM3].
- **Convergence standards** (the interop anchors):
  1. **AGENTS.md** read natively by: Codex, Copilot, Cursor, opencode, Crush, Amp, Zed, Cline, Roo, Kilo, Windsurf/Devin, Factory Droid, Goose, Warp, Trae, Augment, Devin cloud, Junie, Antigravity [CX3][CP3][CU1][OC3][CR2][AM1][ZD1][CL1][RO2][KL4][WS6][FS2][FS3][FS4][FS5][FS6][FS7][FS8][FS9]. Gemini CLI: only via `context.fileName` config (default `GEMINI.md`) [GM1]. **Claude Code: NOT natively** — `@AGENTS.md` import or symlink; `/init` ingests it [CC1]. aider: **not at all** [AI2].
  2. **Agent Skills** (`SKILL.md`, frontmatter `name`+`description`, progressive disclosure; shared dir `.agents/skills/` + `~/.agents/skills/`): Codex [CX2], Gemini [GM3], Cursor [CU4], Copilot [CP2], opencode [OC6], Crush [CR1], Amp [AM4], Zed [ZD2], Cline [CL5], Kilo [KL3], Windsurf [WS3].
  3. **`.claude/*` read by competitors**: `.claude/skills/` (opencode, Crush, Cursor, Copilot, Amp, Cline, Kilo, Windsurf), `.claude/agents/` (Cursor), `CLAUDE.md` (opencode, Crush, Cursor CLI, Copilot, Amp, Zed) [OC6][CR2][CU3][CU12][CP3][AM1][ZD1][CL5][KL3][WS3].
  4. **`mcpServers` JSON object** (`{command,args,env}` | `{url,...}`) is the majority MCP shape (Claude, Cursor, Windsurf, Cline, Roo, Warp, Factory, Junie, Qwen); dissenters: Codex (TOML `[mcp_servers.*]`) [CX7], opencode/Kilo (typed `local|remote`, `command` as ARRAY) [OC7][KL5], VS Code/Copilot (`servers` key) [CP6], Zed (`context_servers`) [ZD3], Continue (YAML LIST of blocks) [CT4], Amp (`amp.mcpServers`) [AM1], Gemini (`httpUrl` vs `url` distinction) [GM1].

## §1 Capability matrix

Legend: ✔ full · ◐ partial/qualified · ✖ absent · † legacy/retired mechanism. Every cell carries a source-ledger ref `[XX#]`; the ledger (§5) holds the full URLs — cell ref + ledger entry together are the citation.

| Harness               | Agents/subagents                                                                                                                                    | Skills                                                                                                 | Rules/instructions                                                                                                                                                                                                | Hooks                                                                                                                                             | Slash-cmds                                                                                                  | Custom tools                                                               | MCP                                                                                                                                                               | Scopes                                                                        | Plugin arch                                                                                                                                       |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Claude Code**       | ✔ `.claude/agents/*.md`, `~/.claude/agents/`, plugin `agents/`, `--agents` flag; rich frontmatter (tools/model/permissionMode/memory/hooks/…) [CC2] | ✔ `SKILL.md` dirs ×(enterprise/user `~/.claude/skills/`/project `.claude/skills/`/plugin/nested) [CC3] | ✔ CLAUDE.md (managed/user/project/`.claude/CLAUDE.md`) + CLAUDE.local.md + `@import` (4 hops) + `.claude/rules/*.md` with `paths:` globs; **AGENTS.md NOT read natively** (import/symlink; `/init` ingests) [CC1] | ✔ 27+ events; 5 hook types command/http/prompt/agent/mcp_tool; regex/literal matchers + `if` filter; settings.json/plugin/agent-frontmatter [CC6] | ✔ skills double as `/name` (+ legacy `.claude/commands/*.md`); plugin-namespaced `/plugin:skill` [CC3][CC5] | ◐ plugin `bin/` added to PATH + MCP; no ad-hoc tool-file format [CC4][CC7] | ✔ `.mcp.json` (project), `~/.claude.json` (user/local), `claude mcp add`; stdio/HTTP/SSE†/WS; OAuth/headersHelper; `${VAR}` expansion; tool-search deferral [CC7] | managed > CLI > local > project > user [CC8]                                  | ✔ plugins: marketplaces (GitHub/git/path/URL), `.claude-plugin/plugin.json`; carries skills/agents/hooks/MCP/LSP/monitors/settings/bin [CC4][CC5] |
| **Codex CLI**         | ✔ TOML agents `~/.codex/agents/`, `.codex/agents/` [CX1]                                                                                            | ✔ `.agents/skills/` chain [CX2]                                                                        | ✔ AGENTS.md chain + override files [CX3]                                                                                                                                                                          | ✔ 7 events, `hooks.json`/`[hooks]` [CX4]                                                                                                          | ◐ `~/.codex/prompts/*.md` deprecated→skills [CX5]                                                           | ✖ (sandboxed built-ins only) [CX6]                                         | ✔ client `[mcp_servers.*]` + server `codex mcp-server` [CX7][CX8]                                                                                                 | user+project(trusted)+profiles+managed [CX6][CX4]                             | ✖ general; MCP+skills+hooks+agents+notify [CX4][CX7]                                                                                              |
| **Cursor**            | ✔ `.cursor/agents/*.md` (2.4; reads `.claude/agents/`, `.codex/agents/`) [CU3][CU7]                                                                 | ✔ `.cursor/skills/`+std paths [CU4]                                                                    | ✔ `.cursor/rules/*.mdc` 4 types + AGENTS.md nested + User/Team rules [CU1]                                                                                                                                        | ✔ `hooks.json` 4 levels, ~20 events [CU2]                                                                                                         | ✔ `.cursor/commands/*.md` → migrating to skills [CU6][CU4]                                                  | ✖ native; via MCP/extensions [CU5][CU10]                                   | ✔ `.cursor/mcp.json` + `~/.cursor/mcp.json` [CU5]                                                                                                                 | enterprise/team/project/user [CU2][CU1]                                       | ✔ VS Code ext API (Open VSX) + MCP + hooks [CU10]                                                                                                 |
| **Copilot**           | ✔ `.github/agents/*.agent.md` + org/enterprise + `~/.copilot/agents/`; VS Code subagents via `agents:` [CP1][CP10]                                  | ✔ `.github/skills/`, `.claude/skills/`, `.agents/skills/`, `~/.copilot/skills/` [CP2][CP15]            | ✔ 4 tiers: copilot-instructions.md, `*.instructions.md` applyTo, AGENTS.md nearest-wins, org+personal [CP3][CP14]                                                                                                 | ✔ own JSON: `.github/hooks/*.json`, `~/.copilot/hooks/*.json`, 12 camelCase events, types command/http/prompt [CP4]                               | ✔ `.github/prompts/*.prompt.md` → `/name` (VS Code) [CP5]                                                   | ◐ VS Code LM Tools API `ext.id/tool` [CP11]                                | ✔ `.vscode/mcp.json` (`servers`), repo-settings UI, `~/.copilot/mcp-config.json` [CP6][CP7][CP12]                                                                 | repo/user/org/enterprise/policy [CP1][CP4]                                    | ✔ MCP (Extensions dead) + VS Code chat/LM APIs + CLI plugins preview + SDK [CP9][CP11]                                                            |
| **Gemini CLI**        | ✔ `.gemini/agents/*.md` (default-on) [GM2]                                                                                                          | ✔ `.gemini/skills/` + `.agents/skills/` alias [GM3]                                                    | ✔ `GEMINI.md` (configurable name), 3-tier + subdir scan [GM1]                                                                                                                                                     | ✔ settings.json `hooks`, 11 events, regex matcher [GM4][GM8]                                                                                      | ✔ `.gemini/commands/*.toml`, namespaced [GM5]                                                               | ✖ native; via extensions/MCP [GM6]                                         | ✔ `mcpServers` in settings.json (stdio/`url` SSE/`httpUrl` HTTP) [GM1]                                                                                            | system-defaults/user/project/**system**(wins)/env/CLI [GM1]                   | ✔ extensions (`gemini-extension.json`: MCP+commands+context+hooks) [GM6]                                                                          |
| **Windsurf→Devin**    | ◐ Devin Local subagents (announcement-level; file config UNVERIFIED) [WS7]                                                                          | ✔ `.windsurf/skills/` + std paths [WS3]                                                                | ✔ `.devin/rules/`(pref)/`.windsurf/rules/` `trigger:` 4 modes, global_rules.md 6K, AGENTS.md [WS1][WS6]                                                                                                           | ✔ `hooks.json` ws/user/system, 12 snake_case events [WS2]                                                                                         | ✔ workflows `.windsurf/workflows/*.md` → `/name`, manual-only [WS4]                                         | ✖ native; MCP only [WS5]                                                   | ✔ `~/.codeium/windsurf/mcp_config.json`, 100-tool cap [WS5]                                                                                                       | system/workspace/global(+enterprise dirs) [WS1][WS4]                          | ◐ VS Code-fork ext (Open VSX) + MCP + hooks [WS8]                                                                                                 |
| **Cline**             | ◐ native read-only parallel subagents (tool, not file-config) [CL7]                                                                                 | ✔ `.cline/skills/`, `~/.cline/skills/`, `.claude/skills/` [CL5]                                        | ✔ `.clinerules/` dir + `~/Documents/Cline/Rules` + AGENTS.md + `paths:` frontmatter [CL1]                                                                                                                         | ✔ executable scripts per event (6), `.clinerules/hooks/`; macOS/Linux only [CL2][CL3]                                                             | ✔ workflows `.clinerules/workflows/*.md` → `/name.md` [CL4]                                                 | ◐ via Cline SDK plugins [CL8]                                              | ✔ `cline_mcp_settings.json` (ext) / `~/.cline/mcp.json` (CLI) [CL6]                                                                                               | global(Documents)/project; workspace wins [CL1]                               | ✔ Cline SDK `AgentPlugin` (tools+hooks+commands+providers) [CL8]                                                                                  |
| **aider**             | ✖ [AI3]                                                                                                                                             | ✖ (absence in docs) [AI1]                                                                              | ◐ conventions files, explicit `read:` only — **no auto-discovery** [AI2]                                                                                                                                          | ◐ lint/test/auto-commit shell-outs only [AI4][AI8]                                                                                                | ✖ user-defined (~40 built-ins) [AI3]                                                                        | ✖ [AI3]                                                                    | ✖ (still; issue open) [AI5]                                                                                                                                       | home/git-root/cwd YAML chain [AI1]                                            | ✖ (unsupported Python scripting API) [AI6]                                                                                                        |
| **opencode**          | ✔ `.opencode/agents/*.md` + `agent` config key [OC2]                                                                                                | ✔ `.opencode/skills/` + `.claude/skills/` + `.agents/skills/` [OC6]                                    | ✔ AGENTS.md walk-up (+CLAUDE.md fallback) + global + `instructions` globs [OC3]                                                                                                                                   | ✔ via plugin events (tool.execute.before/after, session.\*, file.edited…) [OC5]                                                                   | ✔ `.opencode/commands/*.md`, `$ARGUMENTS`/`$1`/`` !`cmd` ``/@file [OC4]                                     | ✔ plugin `tool` key [OC5]                                                  | ✔ `opencode.json` `mcp` key, typed local/remote [OC7]                                                                                                             | remote/global/env/project/`.opencode`/managed/MDM deep-merge [OC1]            | ✔ JS/TS plugins (dirs + npm) [OC5]                                                                                                                |
| **Crush**             | ✖ custom (open FR #1807) [CR4]                                                                                                                      | ✔ multi-path incl. `.crush/skills`, `.claude/skills`, `.cursor/skills` [CR1]                           | ✔ auto-reads CRUSH.md/AGENTS.md/CLAUDE.md/.cursorrules/copilot-instructions (context_paths) [CR2]                                                                                                                 | ◐ `hooks.PreToolUse` only, in crush.json [CR3]                                                                                                    | ◐ `user-invocable` skills in palette; no command files [CR1]                                                | ✖ beyond MCP [CR1]                                                         | ✔ crush.json `mcp` (stdio/http/sse) [CR1]                                                                                                                         | project(.crush.json/crush.json)/global(~/.config/crush/) [CR1]                | ✖ plugin API; MCP+hook+skills+LSP [CR1][CR3]                                                                                                      |
| **Continue**          | ◐ agent = whole config.yaml assistant; `cn` CLI; no subagents (absence) [CT1][CT6]                                                                  | ✖ (absence) [CT3]                                                                                      | ✔ `.continue/rules/*.md` (globs/alwaysApply) + `rules:` block [CT2]                                                                                                                                               | ✖ (absence; `data` block is telemetry) [CT1]                                                                                                      | ✔ prompts `invokable: true` → `/name` [CT3]                                                                 | ✖ beyond MCP [CT4]                                                         | ✔ config.yaml `mcpServers` LIST + `.continue/mcpServers/` + foreign `mcp.json` [CT4]                                                                              | user `~/.continue/` / workspace `.continue/` + `.continuerc.json` merge [CT5] | ✔ hub.continue.dev blocks (`uses:`) + `config.ts`; no hook/plugin API [CT7]                                                                       |
| **Amp**               | ✔ built-ins (Oracle/Librarian/Painter) + `amp.createAgent()` plugin API [AM1][AM9]                                                                  | ✔ multi-path chain incl. `.agents/skills/`, `.claude/skills/` [AM1][AM4]                               | ✔ AGENTS.md cwd→$HOME + system + user; AGENT.md/CLAUDE.md fallback; @-imports [AM1]                                                                                                                               | ◐ legacy `amp.hooks` † → plugin events (session.start, tool.call…) [AM7][AM2]                                                                     | ✖ retired († `.agents/commands/`) → skills [AM5]                                                            | ✔ `amp.registerTool()` (toolboxes †) [AM2][AM6]                            | ✔ `amp.mcpServers` + `amp.mcpPermissions` [AM1]                                                                                                                   | managed/workspace(`.amp/settings.json`)/user [AM1]                            | ✔ Bun-run TS plugins `.amp/plugins/`, `~/.config/amp/plugins/` [AM2]                                                                              |
| **Zed**               | ✔ `subagent` tool + external agents via ACP (`agent_servers`) [ZD4][ZD6]                                                                            | ✔ exactly `~/.agents/skills/` + `<worktree>/.agents/skills/` [ZD2]                                     | ✔ project instructions: first-match of 9 filenames (.rules→.cursorrules→…→CLAUDE.md→GEMINI.md) + `~/.config/zed/AGENTS.md` [ZD1]                                                                                  | ✖ (absence; tool_permissions is nearest) [ZD5]                                                                                                    | ✖ (extension slash commands removed) [ZD8]                                                                  | ✖ native; MCP only [ZD4]                                                   | ✔ `context_servers` in settings + WASM-extension-delivered [ZD3][ZD7]                                                                                             | default/user/project(.zed/settings.json, nested) [ZD9]                        | ✔ WASM extensions (deliver MCP servers) + ACP [ZD7][ZD6]                                                                                          |
| **Roo Code** † sunset | ✔ `.roomodes` custom modes + Orchestrator/new_task [RO1][RO6]                                                                                       | ✖ [RO2]                                                                                                | ✔ `.roo/rules/`, `.roo/rules-{mode}/`, global `~/.roo/`, AGENTS.md [RO2]                                                                                                                                          | ✖ (never shipped) [RO2]                                                                                                                           | ✔ `.roo/commands/*.md` [RO3]                                                                                | ◐ experimental `.roo/tools/` [RO1]                                         | ✔ `.roo/mcp.json` + global mcp_settings.json [RO4]                                                                                                                | global(globalStorage+~/.roo)/project [RO2]                                    | ◐ Marketplace (modes+MCP) †; cloud shut down [RO5]                                                                                                |
| **Kilo**              | ✔ `.kilo/agents/*.md` (`mode: primary\|subagent\|all`) + legacy `.kilocodemodes` [KL1]                                                              | ✔ `.kilo/skills/` + `.agents/skills/` + `.claude/skills/` [KL3]                                        | ✔ `.kilo/rules/` + `instructions` globs + AGENTS.md always-on + nested lazy [KL2][KL4]                                                                                                                            | ✔ via plugin lifecycle hooks [KL6]                                                                                                                | ✔ `.kilo/commands/*.md` (`subtask:` frontmatter) [KL7]                                                      | ✔ plugin `ToolDefinition` [KL6]                                            | ✔ `kilo.jsonc` `mcp` key typed local/remote [KL5]                                                                                                                 | defaults/global(~/.config/kilo)/project/`.kilo/`/env [KL1]                    | ✔ TS plugin system (tools, interception, providers; CLI+ext) [KL6]                                                                                |

**Second-tier rows (field scan, one-line)**: Qwen Code (QWEN.md default, configurable; `~/.qwen/settings.json`; MCP ✔) [FS1] · Factory Droid (AGENTS.md native incl. nested; `~/.factory/settings.json`, `.factory/mcp.json`; MCP ✔) [FS2] · Goose (AGENTS.md then `.goosehints`; `~/.config/goose/config.yaml`; extensions ARE MCP servers) [FS3] · Warp (AGENTS.md now default, WARP.md legacy-wins; `~/.warp/.mcp.json`, `.warp/.mcp.json`) [FS4] · Trae (`.trae/rules/project_rules.md` + AGENTS.md; MCP ✔ IDE-managed) [FS5] · Augment (`.augment/rules/` + AGENTS.md/CLAUDE.md hierarchical; MCP ✔) [FS6] · Devin cloud (no local files; Knowledge ingests AGENTS.md/CLAUDE.md/.cursorrules/.mdc; MCP ✔ + is an MCP server) [FS7] · JetBrains Junie (AGENTS.md or `.junie/guidelines.md`; `.junie/mcp/mcp.json`) [FS8] · Antigravity (AGENTS.md+GEMINI.md+`.agent/rules/`; skills `~/.gemini/config/skills/` + `.agents/skills/`; `~/.gemini/config/mcp_config.json`; partially UNVERIFIED) [FS9] · Codex IDE ext (same surface as Codex CLI, shared `~/.codex/config.toml`) [FS10].

## §2 Per-harness config-contract sheets (fixture-precision)

### Claude Code

- **Memory/rules**: managed CLAUDE.md (`/etc/claude-code/CLAUDE.md`, macOS `/Library/Application Support/ClaudeCode/CLAUDE.md`) → `~/.claude/CLAUDE.md` → ancestor `CLAUDE.md`+`CLAUDE.local.md` (root→cwd) → cwd pair; project file may live at `CLAUDE.md` or `.claude/CLAUDE.md`; nested `.claude/` CLAUDE.md lazy-loads on subdir file-read; `@path` imports, max 4 hops, parsed once/session, HTML comments stripped; all scopes CONCATENATE (no override). Path-scoped rules: `.claude/rules/*.md` + `~/.claude/rules/*.md` with optional `paths:` glob frontmatter (loaded on matching file-read). **AGENTS.md is NOT read natively** — `@AGENTS.md` import or symlink; `/init` ingests an existing AGENTS.md [CC1].
- **Settings**: `~/.claude/settings.json` < `.claude/settings.json` < `.claude/settings.local.json` (gitignored) < CLI flags < managed (`/etc/claude-code/settings.json`|plist|registry, highest); keys incl. `permissions.{allow,deny,ask,defaultMode,additionalDirectories}`, `env`, `hooks`, `enabledPlugins`, `model`, `apiKeyHelper`, MCP policy keys (`allowedMcpServers`, `enabledMcpjsonServers`, `enableAllProjectMcpServers`) [CC8].
- **Agents**: `.claude/agents/*.md`, `~/.claude/agents/*.md`, plugin `agents/`, `--agents` CLI; frontmatter `name`(req), `description`(req), `tools`, `disallowedTools`, `model`(alias|full|`inherit`), `permissionMode`, `maxTurns`, `skills`, `mcpServers`, `hooks`, `memory`(user|project|local), `background`, `effort`, `isolation`(worktree), `color`, `initialPrompt` [CC2].
- **Skills**: `<scope>/skills/<name>/SKILL.md` (enterprise/user/project/plugin + nested `.claude/skills/` in subdirs); frontmatter `description`, `name`, `when_to_use`, `argument-hint`, `arguments`, `disable-model-invocation`, `user-invocable`, `allowed-tools`/`disallowed-tools`, `model`, `effort`, `context: fork` (+`agent`), `hooks`, `paths` (lazy activation globs), `shell`; body substitutions `$ARGUMENTS`, `$N`, `${CLAUDE_SKILL_DIR}`, `${CLAUDE_PROJECT_DIR}`…; skills double as slash commands (`.claude/commands/*.md` legacy equivalent); description budget ~1536 chars, listing budget ~1% of context [CC3].
- **Hooks**: config in settings.json (any scope), plugin `hooks/hooks.json`, agent/skill frontmatter; shape `{"hooks":{"Event":[{"matcher":"regex-or-literal","if":"perm-rule (v2.1.85+)","hooks":[{type:command|http|prompt|agent|mcp_tool,…,timeout}]}]}}`; ~27+ events: SessionStart, Setup, UserPromptSubmit, UserPromptExpansion, PreToolUse, PermissionRequest, PermissionDenied, PostToolUse, PostToolUseFailure, PostToolBatch, Notification, MessageDisplay, SubagentStart, SubagentStop, TaskCreated, TaskCompleted, Stop, StopFailure, TeammateIdle, InstructionsLoaded, ConfigChange, CwdChanged, FileChanged (literal `|`-split filenames), WorktreeCreate/Remove, PreCompact, PostCompact, Elicitation(Result), SessionEnd; matchers = regex (tool/agent names) or event-specific literals — NOT glob; exit 0 (stdout→context for UserPromptSubmit/SessionStart), exit 2 = block, JSON `hookSpecificOutput` decisions; parallel, most-restrictive-wins [CC6].
- **MCP**: project `.mcp.json` at repo root `{"mcpServers":{n:{type:stdio|http|sse†|ws, command/args/env | url/headers}}}`; user + local scopes in `~/.claude.json` (local keyed per-project path); `claude mcp add`; precedence local > project > user > plugins > claude.ai; `${VAR}`/`${VAR:-default}` expansion; OAuth + headersHelper; tool-search deferral default. **`mcpServers` is NOT a settings.json key** — settings.json carries only MCP _policy_ (allow/deny lists, `enabledMcpjsonServers`) [CC7][CC8].
- **Plugins**: `.claude-plugin/plugin.json` (`name` req, `description`, `version`, `author`…); components `skills/`, `agents/`, `hooks/hooks.json`, `.mcp.json`, `.lsp.json`, `monitors/`, `settings.json`, `bin/` (PATH); marketplaces = GitHub/git/path/URL + marketplace.json; install scopes user/project (`enabledPlugins` in settings)/local/managed; `${CLAUDE_PLUGIN_ROOT}` substitution [CC4][CC5].
- **Auto-load shape**: session start = managed+user+ancestor+cwd CLAUDE.md (full), auto-memory MEMORY.md (first 200 lines/25KB), skill+plugin metadata, MCP tool names (schemas deferred), git status; on-demand = nested CLAUDE.md, `paths:`-scoped rules, skill bodies (re-attached ≤5K tokens each post-compaction) [CC1][CC3][CC10].

### Codex CLI

- **Config**: `~/.codex/config.toml` (TOML); project `.codex/config.toml` (trusted projects); profiles as separate files `~/.codex/<name>.config.toml` selected via `--profile`; precedence CLI `-c` > profile > project > user; managed configs override; `CODEX_HOME` relocates [CX6][CX4].
- **Rules**: `~/.codex/AGENTS.override.md` > `~/.codex/AGENTS.md`; then per-level (repo root walked DOWN to cwd) `AGENTS.override.md` > `AGENTS.md` > `project_doc_fallback_filenames`; concatenated root-down, blank-line joined; combined cap `project_doc_max_bytes` = 32 KiB [CX3].
- **Skills**: `$CWD/.agents/skills`, `$REPO_ROOT/.agents/skills`, `~/.agents/skills`, `/etc/codex/skills`; `SKILL.md` frontmatter `name`+`description` mandatory; optional `agents/openai.yaml`; disable via `[[skills.config]]` [CX2].
- **Agents**: `~/.codex/agents/*.toml`, `.codex/agents/*.toml`; fields `name`, `description`, `developer_instructions` (+`model`, `sandbox_mode`, `mcp_servers`, `nickname_candidates`); `[agents] max_threads=6, max_depth=1` (exact TOML example not in docs — UNVERIFIED syntax) [CX1].
- **Hooks**: `~/.codex/hooks.json` | `.codex/hooks.json` | inline `[hooks]` in either config.toml (both present ⇒ loads both + warns); events `PreToolUse, PostToolUse, PreCompact, PostCompact, SessionStart, UserPromptSubmit, Stop`; TOML fixture:
  `[[hooks.PreToolUse]]` `matcher = "^Bash$"` / `[[hooks.PreToolUse.hooks]]` `type="command" command="python3 check.py" timeout=30` [CX4].
- **MCP**: `[mcp_servers.<name>]` — stdio: `command`,`args`,`env`,`env_vars`,`cwd`; HTTP: `url`,`bearer_token_env_var`,`http_headers`; options `enabled`, `enabled_tools`/`disabled_tools`, `startup_timeout_sec`(10), `tool_timeout_sec`(60); no SSE, no `type` key [CX7]. Codex is also consumable as MCP server (`codex mcp-server`) [CX8].
- **Prompts** (deprecated): `~/.codex/prompts/*.md`, frontmatter `description`,`argument-hint`; `$1..$9`,`$NAMED`,`$ARGUMENTS`; `/prompts:name` [CX5].
- **Other**: `notify = ["cmd", ...]` (JSON arg `agent-turn-complete`); `approval_policy` untrusted|on-request|never; `sandbox_mode` read-only|workspace-write|danger-full-access [CX4][CX6].

### Cursor

- **Rules**: `.cursor/rules/*.mdc` (`.md` in that dir IGNORED); frontmatter `description`, `globs` (comma-sep ok), `alwaysApply`; 4 types (always / intelligent / glob auto-attach / manual `@rule`); nested `.cursor/rules` in subdirs; `AGENTS.md` root + nested (subtree-scoped, no frontmatter); `.cursorrules` legacy; User Rules = settings UI (plain text); Team Rules = dashboard; precedence Team > Project > User; guidance <500 lines [CU1].
- **Hooks**: `hooks.json` — enterprise (`/etc/cursor/hooks.json`, `/Library/Application Support/Cursor/hooks.json`, `C:\ProgramData\Cursor\hooks.json`) > team > project `.cursor/hooks.json` > user `~/.cursor/hooks.json`; shape `{"version":1,"hooks":{"<event>":[{"command","type":"command"|"prompt","timeout","matcher","failClosed","loop_limit"}]}}`; events: sessionStart/sessionEnd, preToolUse/postToolUse/postToolUseFailure, subagentStart/subagentStop, beforeShellExecution/afterShellExecution, beforeMCPExecution/afterMCPExecution, beforeReadFile/afterFileEdit, beforeSubmitPrompt, preCompact, stop, afterAgentResponse/afterAgentThought, beforeTabFileRead/afterTabFileEdit, workspaceOpen; exit 0 = use JSON out, exit 2 = block, else fail-open [CU2].
- **Agents**: `.cursor/agents/*.md` (+ reads `.claude/agents/`, `.codex/agents/`; priority .cursor > .claude > .codex); user `~/.cursor/agents/`; frontmatter `name`,`description`,`model`(default `inherit`),`readonly`,`is_background` [CU3].
- **Skills**: `.cursor/skills/<name>/SKILL.md`, `.agents/skills/`, `~/.cursor/skills/`, `~/.agents/skills/`, legacy `.claude/skills/`+`.codex/skills/`; frontmatter `name`(=folder),`description`,`paths`(globs),`disable-model-invocation`,`metadata`; `scripts/`,`references/`,`assets/` [CU4].
- **Commands**: `.cursor/commands/<name>.md` (plain md body = prompt); folding into skills (`/migrate-to-skills`) [CU6][CU4].
- **MCP**: `.cursor/mcp.json` project > `~/.cursor/mcp.json` global; `{"mcpServers":{"n":{"type":"stdio","command","args","env","envFile"}}}` or remote `{"url","headers","auth"}`; interpolation `${env:VAR}`,`${userHome}`,`${workspaceFolder}` [CU5].
- **CLI**: `~/.cursor/cli-config.json` (or `$XDG_CONFIG_HOME/cursor/`, `CURSOR_CONFIG_DIR`); project perms `.cursor/cli.json` `{"permissions":{"allow":["Shell(npm)"],"deny":[]}}` (deny wins; Shell/Read/Write/WebFetch); CLI reads `.cursor/rules`, root AGENTS.md AND CLAUDE.md [CU8][CU9][CU12].

### GitHub Copilot

- **Instructions**: `.github/copilot-instructions.md` (always-on, ≤ "2 pages"); `.github/instructions/*.instructions.md` frontmatter `applyTo` glob (+`excludeAgent`); `AGENTS.md` anywhere nearest-wins (VS Code gates: `chat.useAgentsMdFile`, `chat.useNestedAgentsMdFiles`; `CLAUDE.md` via `chat.useClaudeMdFile`); org (settings UI) + personal (github.com profile); priority personal > repo > org (all sent) [CP3][CP14][CP16].
- **Agents**: `.github/agents/NAME.agent.md`; org: `agents/` in org `.github`/`.github-private` repo; personal CLI: `~/.copilot/agents/`; frontmatter `description`(req),`name`,`target`(vscode|github-copilot),`tools`(aliases execute/read/edit/search/agent/web/todo + `server/tool` + `ext.id/tool`),`model`,`disable-model-invocation`,`user-invocable`,`mcp-servers`,`metadata`; VS Code adds `agents:`,`handoffs:`; body ≤30,000 chars; `.chatmode.md` legacy → rename to `.agent.md` [CP1][CP10].
- **Skills**: `.github/skills/`, `.claude/skills/`, `.agents/skills/` (repo); `~/.copilot/skills/`, `~/.agents/skills/` (personal) [CP2].
- **Hooks**: `.github/hooks/*.json` (repo; CLI + coding agent), `~/.copilot/hooks/*.json` (CLI), `/etc/github-copilot/policy.d/*.json` (policy), inline `.github/copilot/settings.json`/`~/.copilot/settings.json`; shape `{"version":1,"hooks":{"preToolUse":[{"type":"command","bash":"...","timeoutSec":30}]}}`; events sessionStart, preToolUse (fail-closed; allow/deny/ask + modifiedArgs), postToolUse, postToolUseFailure, permissionRequest, agentStop, subagentStart/Stop, errorOccurred, notification, userPromptSubmitted, preCompact; types command(bash/powershell)/http/prompt [CP4].
- **Prompts**: `.github/prompts/*.prompt.md` → `/name`; frontmatter `description`,`name`,`argument-hint`,`agent`,`model`,`tools` [CP5].
- **MCP**: VS Code `.vscode/mcp.json` `{"servers":{...}}` + user-profile mcp.json [CP6]; coding agent: repo Settings → Copilot → MCP servers (`mcpServers` JSON, secrets `COPILOT_MCP_*`) [CP7]; CLI `~/.copilot/mcp-config.json` [CP12].
- **CLI home** `~/.copilot/` (`COPILOT_HOME`): settings.json, config.json, mcp-config.json, copilot-instructions.md, agents/, hooks/, skills/ [CP8]. Coding-agent env: `.github/workflows/copilot-setup-steps.yml` [CP13].

### Gemini CLI (→ Antigravity CLI)

- **Settings**: user `~/.gemini/settings.json`; project `.gemini/settings.json`; system `/etc/gemini-cli/settings.json` (macOS `/Library/Application Support/GeminiCli/`); system-defaults file; precedence defaults < system-defaults < user < project < **system** < env < CLI [GM1].
- **Context**: default `GEMINI.md`; `context.fileName` (string|array); 3-tier: `~/.gemini/GEMINI.md` + walk-up cwd→project root/home + **subdirectory scan below cwd** capped `context.discoveryMaxDirs`(200); `/memory show` [GM1].
- **Commands**: `.gemini/commands/*.toml` (project wins) + `~/.gemini/commands/`; subdirs → `/ns:cmd`; `prompt` required, `description` optional; `{{args}}`, `!{shell}`, `@{file}` [GM5].
- **Agents**: `.gemini/agents/*.md`, `~/.gemini/agents/*.md`; frontmatter `name`,`description`(req),`kind`(local|remote),`tools`(wildcards `mcp_*`),`model`,`temperature`,`max_turns`(30); body = system prompt [GM2].
- **Skills**: `~/.gemini/skills/` + `~/.agents/skills/` alias (alias wins); workspace `.gemini/skills/` + `.agents/skills/`; precedence built-in < extension < user < workspace; `activate_skill` tool; `gemini skills install` [GM3].
- **Hooks**: settings.json `"hooks"`: `{"BeforeTool":[{"matcher":"regex","hooks":[{"type":"command","command":"...","timeout":60000}]}]}`; 11 events BeforeTool/AfterTool/BeforeAgent/AfterAgent/BeforeModel/BeforeToolSelection/AfterModel/SessionStart/SessionEnd/Notification/PreCompress; exit 2 blocks; extensions can bundle hooks [GM4][GM8].
- **MCP**: `mcpServers.<n>`: stdio `command/args/env/cwd`; SSE `url`; streamable HTTP `httpUrl`+`headers`; `timeout`,`trust`,`includeTools`,`excludeTools` (exclude wins) [GM1].
- **Extensions**: `~/.gemini/extensions/<n>/gemini-extension.json` `{name,version,mcpServers,contextFileName,excludeTools}` + `commands/` TOML; install from GitHub URL/path [GM6].

### Windsurf / Devin Desktop

- **Rules**: workspace `.devin/rules/*.md` (preferred) / `.windsurf/rules/*.md` (legacy) / `.windsurfrules` (legacy single); discovered cwd+subdirs+parents to git root; 12,000 chars/file; frontmatter `trigger: always_on|model_decision|glob|manual` (+`globs:`, `description:`); global `~/.codeium/windsurf/memories/global_rules.md` (6,000 chars); enterprise `/etc/devin/rules/` etc.; AGENTS.md any level (root=always-on, subdir=glob `<dir>/**`) [WS1][WS6].
- **Workflows**: `.windsurf/workflows/*.md` → `/name` (manual-only); global `~/.codeium/windsurf/global_workflows/`; system > workspace > global > built-in; 12K/file [WS4].
- **Skills**: `.windsurf/skills/<n>/SKILL.md`, `~/.codeium/windsurf/skills/`, + `.agents/skills/`, `.claude/skills/` (both scopes) [WS3].
- **Hooks**: `.windsurf/hooks.json` (ws), `~/.codeium/windsurf/hooks.json` (user), system dirs; `{"hooks":{"<event>":[{"command","powershell","show_output","working_directory"}]}}`; 12 snake_case events (e.g. `post_write_code`); JSON stdin; exit 2 blocks (pre only); cannot modify inputs [WS2].
- **MCP**: `~/.codeium/windsurf/mcp_config.json` `{"mcpServers":{n:{command,args,env}}}`; remote `serverUrl`(+`headers`); `${env:VAR}`,`${file:...}`; 100-tool cap [WS5].

### Cline

- **Rules**: project `.clinerules/` dir (all .md/.txt combined; workspace wins); global `~/Documents/Cline/Rules` (OS-specific Documents path); AGENTS.md (root or `~/.agents/AGENTS.md`); `.cursorrules`/`.windsurfrules` also recognized; conditional frontmatter `paths:` globs; per-rule toggle UI [CL1].
- **Workflows**: `.clinerules/workflows/*.md` → `/name.md`; global `~/Documents/Cline/Workflows/` [CL4].
- **Hooks**: executable files named exactly as event (no extension) in `.clinerules/hooks/` + `~/Documents/Cline/Rules/Hooks/`; events PreToolUse, PostToolUse, UserPromptSubmit, TaskStart, TaskResume, TaskCancel; JSON stdin → `{"cancel":bool,"errorMessage"?,"contextModification"?}` stdout; macOS/Linux only; opt-in Settings→Features [CL2][CL3].
- **Skills**: `.cline/skills/` (proj), `~/.cline/skills/` (global, wins conflicts), `.clinerules/skills/`, `.claude/skills/` [CL5].
- **MCP**: extension → VS Code globalStorage `…/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`; CLI → `~/.cline/mcp.json`; `{"mcpServers":{n:{command,args,env | type:"streamableHttp",url,headers, disabled, autoApprove}}}` (remote sans `type` ⇒ legacy SSE) [CL6].
- **Ignore**: `.clineignore` (gitignore syntax) [CL9].

### aider

- `.aider.conf.yml` searched home → git root → cwd, later wins; `--config` exclusive; every key ≡ CLI flag ≡ `AIDER_*` env ≡ `.env` [AI1].
- **No conventional auto-load**: conventions files (any name, e.g. `CONVENTIONS.md`) must be listed `read: [...]` / `--read` / `/read`; startup context = read:/file: entries + tree-sitter repo map (~1k tokens, `--map-tokens`) [AI2][AI7].
- Hook-likes: `lint-cmd`/auto-lint, `test-cmd`/`auto-test`, auto git commits (`--no-auto-commits`, `--commit-prompt`) [AI4][AI8]. No MCP (verified; FR open) [AI5]. Extension = unsupported Python API / CLI batch mode [AI6].

### opencode

- **Config**: global `~/.config/opencode/opencode.json`; project-root `opencode.json` (JSON/JSONC, `$schema: https://opencode.ai/config.json`); env `OPENCODE_CONFIG*`; managed OS dirs; precedence (low→high) remote .well-known → global → env-config → project → `.opencode` dirs → inline env → managed → MDM; **deep-merged**; `{env:VAR}`/`{file:path}` substitution [OC1].
- **Rules**: AGENTS.md walk-up from cwd (CLAUDE.md fallback per level); global `~/.config/opencode/AGENTS.md`; `~/.claude/CLAUDE.md` compat (disable `OPENCODE_DISABLE_CLAUDE_CODE=1`); `instructions: [globs]` combined [OC3].
- **Agents**: `.opencode/agents/*.md`, `~/.config/opencode/agents/*.md` (filename = id; docs now PLURAL dirs, singular-alias status UNVERIFIED); frontmatter `description`,`mode: primary|subagent`,`model`,`temperature`,`tools`,`permission`; or inline `agent` config key [OC2].
- **Commands**: `.opencode/commands/*.md`, global equivalent; frontmatter `description`,`agent`,`model`,`subtask`; `$ARGUMENTS`,`$1..$n`,`` !`cmd` ``,`@file` [OC4].
- **Skills**: `.opencode/skills/<n>/SKILL.md`, `~/.config/opencode/skills/`, + `.claude/skills/` + `.agents/skills/`; `name` regex `^[a-z0-9]+(-[a-z0-9]+)*$`; invoked via native `skill` tool; permission-gated [OC6].
- **MCP**: `"mcp": {"<n>": {"type":"local","command":["npx","-y","…"],"enabled":true,"environment":{},"cwd"} | {"type":"remote","url","headers","oauth"}}` — note `command` is an ARRAY [OC7].
- **Permissions**: `"permission"` per-tool allow/ask/deny + nested glob patterns, last match wins; per-agent overrides [OC8].
- **Plugins**: `.opencode/plugins/`, `~/.config/opencode/plugins/`, npm names in `"plugin": []`; export async fn → hook map (`tool.execute.before/after`, `chat.message`, `session.created/updated/compacted`, `file.edited`, `shell.env`, `event`, custom `tool`) [OC5].

### Crush

- **Config**: `.crush.json` → `crush.json` → `~/.config/crush/crush.json`; schema `https://charm.land/crush.json`; keys `providers`,`models`,`mcp`,`lsp`,`permissions`,`hooks`,`options` [CR1].
- **Context**: default `context_paths` (from source): `.github/copilot-instructions.md`, `.cursorrules`, `.cursor/rules/`, `CLAUDE.md`/`CLAUDE.local.md`, `GEMINI.md`, `crush.md`/`CRUSH.md` (+case/local variants), `AGENTS.md` variants — all matches combined; global `~/.config/crush/CRUSH.md` + `~/.config/AGENTS.md`; override `options.context_paths`/`options.global_context_paths` [CR2][CR1].
- **MCP**: `"mcp": {"n": {"type":"stdio","command","args","timeout","env"} | {"type":"http"|"sse","url","headers"}}`; `$VAR`/`$(cmd)` expansion [CR1].
- **Hooks**: `"hooks":{"PreToolUse":[{"name","matcher":"^bash$","command","timeout"}]}` — only event; regex on tool name; input via `CRUSH_TOOL_INPUT_*`; exit 2 blocks [CR3].
- **Skills**: global `$CRUSH_SKILLS_DIR`, `$XDG_CONFIG_HOME/agents/skills`, `~/.agents/skills/`, `~/.claude/skills/`; project `.agents/skills`, `.crush/skills`, `.claude/skills`, `.cursor/skills`; frontmatter + `user-invocable`, `disable-model-invocation` [CR1].
- **Permissions**: `"permissions":{"allowed_tools":["view","mcp_<server>_<tool>"]}`; `--yolo` [CR1]. **LSP**: `"lsp":{"go":{"command":"gopls"}}` [CR1].

### Continue

- **Config**: `~/.continue/config.yaml` (config.json deprecated); required top-level `name`,`version`,`schema`; blocks `models`(list),`rules`,`prompts`,`context`,`docs`,`mcpServers`(LIST of `{name,command,args,env,cwd,type: stdio|sse|streamable-http,url}`),`data`; any block `uses: owner/slug` + `with:`/`override:` (hub); `config.ts` escape hatch; hot-reload [CT1][CT5][CT4].
- **Project**: `.continue/rules/*.md` (frontmatter `name`,`globs`,`regex`,`description`,`alwaysApply`; lexicographic); `.continue/prompts/*.md` (`invokable: true` → `/name`); `.continue/mcpServers/*.yaml` (needs name/version/schema) + `.continue/mcpServers/mcp.json` (foreign `{"mcpServers":{…}}` JSON auto-detected); `.continuerc.json` overlay (`mergeBehavior: merge|overwrite`) [CT2][CT3][CT4][CT5].
- **CLI**: `cn` (TUI) / `cn -p` (headless); permissions `--allow/--ask/--exclude`, persisted `~/.continue/permissions.yaml` [CT6].
- AGENTS.md auto-read: not documented (UNVERIFIED) [CT2].

### Amp

- **Settings**: user `~/.config/amp/settings.json(c)`; workspace: nearest `.amp/settings.json(c)` upward to repo root; managed `/etc/ampcode/managed-settings.json` etc.; precedence managed > workspace > user; flat `amp.*` keys [AM1].
- **Rules**: AGENTS.md cwd + parents to `$HOME`; subtree AGENTS.md lazy on file-read; system + user (`~/.config/amp/AGENTS.md`, `~/.config/AGENTS.md`); per-dir fallback AGENTS.md > AGENT.md > CLAUDE.md; `@path` / `@globs` imports [AM1].
- **Skills**: precedence `~/.config/agents/skills/` → `~/.agents/skills/` → `~/.config/amp/skills/` → `.agents/skills/` → `.claude/skills/` → `~/.claude/skills/` → plugins/legacy; extra dirs `amp.skills.path` [AM1][AM4].
- **MCP**: `"amp.mcpServers": {"n": {command,args,env} | {url,headers}}` + `includeTools`; `amp.mcpPermissions`; workspace-defined servers need approval [AM1].
- **Plugins**: `.amp/plugins/*.ts`, `~/.config/amp/plugins/*.ts` (Bun); default-export fn(PluginAPI): `registerTool`, `createAgent`, `registerAgentMode`, `registerCommand`, `on('session.start'|'tool.call'|'tool.result'|'agent.start'|'agent.end')`; tool.call handlers return allow/reject-and-continue/modify/synthesize/error (permissions now live here; legacy `amp.permissions`/`amp.hooks`/toolboxes/commands retired) [AM2][AM3][AM8].

### Zed

- **Settings**: `~/.config/zed/settings.json` (JSONC); project `.zed/settings.json` (nested allowed, deeper wins; scope-limited); default < user < project [ZD9].
- **Instructions**: project single file, first match: `.rules` → `.cursorrules` → `.windsurfrules` → `.clinerules` → `.github/copilot-instructions.md` → `AGENT.md` → `AGENTS.md` → `CLAUDE.md` → `GEMINI.md`; personal `~/.config/zed/AGENTS.md`; project overrides personal [ZD1].
- **Skills**: exactly `~/.agents/skills/` + `<worktree>/.agents/skills/`; no nesting; frontmatter `name` (lowercase/nums/hyphens ≤64), `description` (<1024 bytes), `disable-model-invocation` [ZD2].
- **MCP**: `"context_servers": {"n": {command,args,env} | {url,headers}}`; per-profile enablement; MCP also delivered by WASM extensions (`[context_servers.x]` in extension.toml + Rust `context_server_command`) [ZD3][ZD7][ZD10].
- **External agents**: ACP; `"agent_servers": {"my-agent": {"type":"custom","command","args","env"}}`; Zed instructions/skills do NOT auto-apply to external agents [ZD6].
- **Tools/permissions**: profiles (`agent.profiles.<n>.tools` map); `agent.tool_permissions` allow/deny/confirm + regex lists [ZD4][ZD5][ZD10]. No hooks; extension slash commands removed [ZD8].

### Roo Code († archived) / Kilo

- **Roo**: `.roomodes` YAML (or JSON): `customModes: [{slug,name,roleDefinition,groups(read|edit|command|mcp; edit+fileRegex),description?,whenToUse?,customInstructions?}]`; rules `.roo/rules/`, `.roo/rules-{slug}/`, global `~/.roo/…` (recursive, alphabetical; fallback single `.roorules`); commands `.roo/commands/*.md` (frontmatter description/argument-hint/mode); MCP `.roo/mcp.json` + globalStorage `mcp_settings.json` (project wins per-name); AGENTS.md default-on (`roo-cline.useAgentRules`); assembly order lang → global instr → mode instr → rules dirs → AGENTS.md → fallbacks [RO1][RO2][RO3][RO4].
- **Kilo**: `kilo.jsonc` root or `.kilo/kilo.jsonc` (latter wins) + global `~/.config/kilo/kilo.jsonc`; precedence defaults → global → project → `.kilo/` files → env; agents `.kilo/agents/*.md` frontmatter `description`,`mode: primary|subagent|all`,`permission`(ordered glob rules, last wins),`model`,`color`,`temperature`; rules `.kilo/rules/` + `instructions` globs; commands `.kilo/commands/*.md` (`subtask:`); skills `.kilo/skills/` + compat dirs; MCP `"mcp": {n: {type: local|remote, command:[…]|url, enabled, timeout}}`; legacy `.kilocode/*`/`.kilocodemodes` auto-migrate; TS plugin system (`@kilocode/plugin`: tools, tool-call interception, events, providers) [KL1][KL2][KL7][KL3][KL5][KL6].

## §3 Divergences from agent-forge's shipped adapters (explicit; per adapter)

Read for comparison only: `packages/agent-forge/src/adapters/*/{index,paths,read,write,events}.ts`.

### claude adapter

1. **MCP-in-settings.json fabricated**: `read.ts`/`write.ts` treat `mcpServers` as a `settings.json` key (both scopes) — settings.json carries only MCP _policy_ keys; server definitions live in `.mcp.json` (project) and `~/.claude.json` (user/local). Emitted user-scope MCP config is invisible to Claude Code; project scope works only because the adapter ALSO writes `.mcp.json` [CC7][CC8].
2. **Event map fully vindicated**: all 19 mapped event names (incl. `StopFailure`, `TeammateIdle`, `PostToolUseFailure`, `PostCompact`, `FileChanged`, `ConfigChange`, `InstructionsLoaded`, `PermissionRequest`, `PermissionDenied`) exist in the documented 27+-event surface. Lossy-but-correct: unmapped events Setup, UserPromptExpansion, PostToolBatch, MessageDisplay, TaskCreated/Completed, CwdChanged, WorktreeCreate/Remove, Elicitation(Result) [CC6].
3. **Matcher semantics wrong**: `capabilities.hooks.matchers: 'glob'` — matchers are regex or event-specific literals (FileChanged: literal `|`-split filenames), never glob [CC6].
4. **Hook shape undermodeled**: only `type: command` + `timeout` handled; real types http/prompt/agent/mcp_tool plus `if`, `args`, `shell`, `env` fields are dropped on read and unreachable on write [CC6].
5. **Rules surfaces unmodeled**: `.claude/rules/*.md` (+`paths:` frontmatter), `CLAUDE.local.md` (the actual local-scope rules surface — adapter warns "scope 'local' does not support rules" instead), `.claude/CLAUDE.md` alt location, `@import` semantics (adapter concatenates bodies destructively; overwrites a hand-maintained CLAUDE.md), managed scope [CC1].
6. **Plugins unmodeled**: the plugin system (skills/agents/hooks/MCP/LSP/monitors/bin per plugin, marketplaces, `enabledPlugins`) is a first-class config-carrier surface with no IR representation [CC4][CC5].
7. Correct as shipped: `~/.claude/`+`.claude/` layouts, `CLAUDE.md`-not-AGENTS.md as the rules file [CC1], `settings.local.json` for local scope [CC8], `permissions.{allow,deny,ask}` + `env` as settings keys [CC8], agents/skills/commands dir shapes (agent/skill frontmatter richer than IR: permissionMode, memory, `context: fork`, `paths` — lossy) [CC2][CC3].

### codex adapter

1. **Skills path wrong**: writes `.codex/skills/<n>/SKILL.md` & `~/.codex/skills/`; real discovery is `.agents/skills/` (cwd + repo root), `~/.agents/skills/`, `/etc/codex/skills` — emitted skills are invisible to Codex [CX2].
2. **Agent TOML fields wrong**: emits `system_prompt`, `tools`, `color`; documented fields are `name`, `description`, `developer_instructions`, `model`, `sandbox_mode`, `mcp_servers`, `nickname_candidates` [CX1].
3. **Hooks gate fabricated**: `[features] codex_hooks = true` not documented; hooks live in `hooks.json` or `[hooks]` tables [CX4].
4. **Hook event set wrong**: adapter maps 6 incl. `PermissionRequest` (not documented); real set is 7: PreToolUse, PostToolUse, PreCompact, PostCompact, SessionStart, UserPromptSubmit, Stop [CX4]. "Bash-only firing" claim: UNVERIFIED in current docs.
5. **`permissions` and `env` TOML keys fabricated**: Codex uses `approval_policy`/`sandbox_mode` (+ granular table) and `shell_environment_policy`, not `permissions {allow,deny,ask}` / `env` [CX6].
6. **Remote MCP shape wrong**: emits `{url, type: http|sse}`; Codex uses `url` + `bearer_token_env_var`/`http_headers`, no `type`, no SSE [CX7].
7. **Prompts**: `~/.codex/prompts/` real but deprecated → skills; project `.codex/prompts/` not documented (user-level only) [CX5].
8. Read-side gaps (lift fidelity): AGENTS.override.md, root→cwd walk-down chain, 32 KiB cap, fallback filenames, profiles (`<name>.config.toml`), `notify` [CX3][CX4].

### gemini adapter

1. **Rules filename wrong**: reads/writes `AGENTS.md` (both scopes); Gemini default context file is `GEMINI.md` (only reachable via `context.fileName`) — emitted rules silently ignored by a stock install [GM1].
2. **`commands: 'none'` false**: `.gemini/commands/*.toml` (TOML `prompt`/`description`, namespaced dirs) exists; adapter's "Gemini has no slash-command system" warning is wrong [GM5].
3. **Hooks largely right**: settings.json `hooks` + entry shape match; adapter's 10 events all real; missing `BeforeToolSelection`; matcher is **regex** (adapter declares `glob`); `timeout` is ms [GM4].
4. **`permissions`/`env` settings keys fabricated** (adapter warns for permissions but emits anyway; emits `env` silently): real controls are `tools.core`/`excludeTools`/MCP `includeTools`, and `.env` loading [GM1].
5. **Remote MCP shape wrong**: emits `{url, type}`; Gemini distinguishes `url` (SSE) vs `httpUrl` (streamable HTTP) — HTTP servers mis-encoded; `trust`/`includeTools`/`excludeTools`/`timeout` unmodeled [GM1].
6. Agents dir right (`.gemini/agents/*.md`); frontmatter `kind`/`temperature`/`max_turns` unmodeled (acceptable as `partial`) [GM2].
7. Scope model misses system + system-defaults tiers (system OUTRANKS project) and extensions [GM1][GM6].
8. Strategic: target renamed — Antigravity CLI succession [GM7].

### cursor adapter

1. **Hook file/schema close but incomplete**: paths right; emitted JSON lacks required `"version": 1`; `type`/`failClosed`/`loop_limit`/prompt-hooks unmodeled; enterprise/team tiers unmodeled [CU2]. Event map: all 17 adapter events verified real [CU2].
2. **`commands: 'none'` false**: `.cursor/commands/*.md` exists (now folding into skills) [CU6][CU4].
3. **Agents skipped**: `.cursor/agents/*.md` subagents are first-class (2.4); adapter emits nothing [CU3].
4. **Primary rules surface unhandled**: `.cursor/rules/*.mdc` declared in paths.ts but never read/written; `.mdc` frontmatter (description/globs/alwaysApply) and 4 activation types lost; adapter writes only a single `AGENTS.md` [CU1].
5. **User-scope rules file dubious**: `~/.cursor/AGENTS.md` — User Rules live in settings UI; no doc for a user-level AGENTS.md file (UNVERIFIED as consumed) [CU1].
6. Remote MCP: Cursor documents `{url, headers, auth}`; adapter's extra `type` key undocumented [CU5].
7. `permissions: not emitted` — Cursor CLI has `.cursor/cli.json` permissions (allow/deny) the adapter could target [CU9].

### copilot adapter

1. **Hooks premise false**: "Copilot natively parses `.claude/settings.json`" is not documented anywhere; real hooks = Copilot's own format (`.github/hooks/*.json`, `~/.copilot/hooks/*.json`, inline settings.json) with camelCase events, `{"version":1,...}` envelope, `bash`/`powershell` command fields, `timeoutSec`, types command/http/prompt. Adapter both writes to and reads from the wrong file in the wrong dialect [CP4].
2. **User scope fabricated**: `~/.config/github-copilot/{AGENTS.md,skills/,mcp.json}` — real personal surface is `~/.copilot/` (`COPILOT_HOME`): agents/, skills/, hooks/, mcp-config.json, copilot-instructions.md [CP8].
3. **Skills project dir wrong**: `.copilot/skills/` — real repo dirs are `.github/skills/` (also `.claude/skills/`, `.agents/skills/`) [CP2].
4. **Rules undermodeled**: single root AGENTS.md is valid, but the Copilot-native tiers (`.github/copilot-instructions.md`, `.github/instructions/*.instructions.md` + `applyTo`, org/personal instructions) are neither read nor written [CP3][CP14].
5. **`commands: 'none'` false**: prompt files `.github/prompts/*.prompt.md` → `/name` [CP5].
6. **Agents skipped as "experimental"**: `.github/agents/*.agent.md` is a GA, documented surface (+ org distribution, `~/.copilot/agents/`) [CP1].
7. **Event map stale**: real events are camelCase; superset includes permissionRequest, errorOccurred, postToolUseFailure, notification — adapter's 8 PascalCase names don't match the documented dialect [CP4].
8. MCP: `.vscode/mcp.json` `{servers}` right for VS Code; coding-agent (repo settings UI `mcpServers`) and CLI (`~/.copilot/mcp-config.json`) unmodeled [CP6][CP7][CP12].

### opencode adapter

1. **`commands: 'none'` false**: `.opencode/commands/*.md` with frontmatter + `$ARGUMENTS` [OC4].
2. **`agents: 'none'` false**: `.opencode/agents/*.md` + `agent` config key, incl. `mode: subagent` [OC2].
3. **MCP file fabricated**: `.opencode/mcp.json` is not a config surface; MCP lives in `opencode.json` under `"mcp"` with typed entries and `command` as an ARRAY (adapter's `{command, args}` split and `mcpServers` key are the wrong dialect) [OC7].
4. **`permissions.json` / `env.json` fabricated**: permissions are the `"permission"` key (a real, expressible DSL the adapter could map); no env-file surface exists (`{env:VAR}` substitution instead) [OC8][OC1].
5. **Hooks via plugin shim**: sound strategy (plugins are real; `.opencode/plugins/` matches current plural docs); adapter's event names partially verified — `tool.execute.before/after`, `session.created`, `file.edited` documented; `session.deleted`, `session.idle`, `permission.asked`, `permission.replied`, `tui.toast.show`, `file.watcher.updated`, `command.executed`, `session.compacted` UNVERIFIED in current plugin docs [OC5].
6. Rules right (AGENTS.md + global); misses CLAUDE.md fallback + `instructions` globs on read [OC3].
7. Skills path right; `allowed_tools`-dropped warning consistent with opencode's permission-gating model [OC6].

### cline adapter

1. **Global rules path wrong**: `~/.cline/rules` — real global dir is `~/Documents/Cline/Rules` (OS Documents variants) [CL1].
2. **Hooks dialect fabricated**: `.cline/hooks.json` with Claude-shaped JSON does not exist; real hooks are per-event **executable scripts** in `.clinerules/hooks/` and `~/Documents/Cline/Rules/Hooks/` with a JSON stdin/stdout contract [CL2][CL3].
3. **Hook events wrong**: adapter maps `TaskComplete` (session.end) and `PreCompact` — neither in Cline's 6-event set (PreToolUse, PostToolUse, UserPromptSubmit, TaskStart, TaskResume, TaskCancel) [CL2].
4. **MCP path wrong for the extension**: `.cline/mcp.json` (project) not documented; extension uses VS Code globalStorage `cline_mcp_settings.json`; `~/.cline/mcp.json` is real only for the CLI. `disabled`/`autoApprove`/`streamableHttp` fields unmodeled [CL6].
5. **`skills: 'none'` false**: `.cline/skills/` + `~/.cline/skills/` + `.claude/skills/` [CL5].
6. **`commands: 'none'` false**: workflows `.clinerules/workflows/*.md` [CL4].
7. Rules dir `.clinerules/` right; `paths:` conditional frontmatter and AGENTS.md recognition unmodeled [CL1].

### continue adapter

1. **MCP dialect wrong**: writes `{mcpServers: {name: {...}}}` (map) as the whole `config.yaml` — real schema: `mcpServers` is a LIST of blocks and top-level `name`/`version`/`schema` are required; the emitted file also clobbers any full user config. The adapter's map shape IS accepted, but only at `.continue/mcpServers/mcp.json` (foreign-format auto-detect) — retarget there [CT4][CT1].
2. **`rules` via AGENTS.md unverified**: no doc says Continue auto-reads root or `~/.continue/AGENTS.md`; real rules surface is `.continue/rules/*.md` (+ `rules:` block) — adapter writes a file with no documented consumer [CT2].
3. **`commands: 'none'` false**: `.continue/prompts/*.md` `invokable: true` → `/name` [CT3].
4. `hooks: 'none'` confirmed correct [CT1]. `.continuerc.json` overlay + hub `uses:` unmodeled [CT5][CT7].

### crush adapter

1. **MCP file fabricated**: `.crush/mcp.json` / `~/.config/crush/mcp.json` don't exist; MCP is the `"mcp"` key inside `crush.json`/`.crush.json`/`~/.config/crush/crush.json`, with `type` REQUIRED-style (`stdio|http|sse`) entries [CR1].
2. **`hooks: 'none'` stale**: Crush has `hooks.PreToolUse` (regex matcher, `CRUSH_TOOL_INPUT_*`, exit-2 block) in crush.json [CR3].
3. **User-scope rules path wrong**: `~/.config/crush/AGENTS.md` — documented global context files are `~/.config/crush/CRUSH.md` and `~/.config/AGENTS.md` [CR1][CR2].
4. Project rules OK by luck: root `AGENTS.md` is in default `context_paths` [CR2].
5. Skills `.crush/skills/` valid (one of several roots); frontmatter extras (`user-invocable`) unmodeled [CR1].
6. `permissions: 'none'` stale: `permissions.allowed_tools` exists [CR1]. LSP config unrepresentable in IR (no resource type) [CR1].

### aider adapter

1. **Rules premise false**: aider auto-discovers NO conventions file; root `AGENTS.md` is ignored unless wired via `read:`/`--read`; `~/AGENTS.md` (user scope) is fabricated. A functional adapter must write the conventions file AND a `.aider.conf.yml` containing `read: [<file>]` [AI2][AI1].
2. `mcp: 'none'` confirmed correct (open FR) [AI5]. Everything-else-'none' correct [AI3].
3. Unmodeled but mappable: `lint-cmd`/`test-cmd` (hook-like), `AIDER_*` env, YAML chain home→git-root→cwd (three scopes, later wins) [AI1][AI4].

### Cross-cutting (engine/IR level)

- **IR `McpServer {command, args}`** can't express opencode/Kilo `command`-as-array, Gemini `httpUrl` vs `url`, Codex `bearer_token_env_var`, Cline `autoApprove`/`disabled`, Gemini/Codex `includeTools`/`excludeTools`/`trust`/timeouts.
- **Rule model = one concatenated body** loses every activation dialect: Cursor `.mdc` (globs/alwaysApply/description), Windsurf `trigger:` 4 modes + char caps, Continue frontmatter, Cline `paths:`, Copilot `applyTo`, Roo/Kilo mode-scoped dirs, subdir/nested AGENTS.md semantics (Codex walk-down, Amp walk-up-to-$HOME, Copilot nearest-wins, Zed first-match-of-9). Multi-file rules dirs are only modeled for cline.
- **Write is destructive**: every adapter overwrites the target rules file / settings file wholesale (only copilot write merges hooks); no read-merge for foreign content.
- **Hook capability declarations misdescribe dialects**: `payload: 'claude-json'` true only for Claude(+Codex hooks.json); Copilot/Cursor/Gemini/Windsurf/Cline each have native shapes; `matchers: 'glob'` wrong for Gemini/Cursor/Crush/Copilot (regex) and Cline (none).
- **Missing resource types** the field now has: LSP config (Crush), typed permission DSLs (opencode/Kilo/Amp/Zed/Cursor-CLI), plugins as a deliverable, agent `permission`/`mode` fields, skills supporting-file trees, context-file naming (`GEMINI.md`/`QWEN.md`/`CRUSH.md`) as a parameter.
- **Adapter roster vs field**: no adapters for Amp, Zed, Windsurf/Devin, Kilo (and second-tier Factory Droid, Goose, Warp, Qwen, Antigravity, Junie); one shipped target sunset (Roo never shipped — fine), one renamed twice (Gemini→Antigravity; Windsurf→Devin), aider is a no-op target in practice.
- **Cheapest interop lever confirmed**: emitting the Claude dialect + AGENTS.md + `.agents/skills/` reaches most harnesses natively (see §0 convergence), often better than the adapter's current bespoke-but-wrong dialects.

## §4 Fixture-readiness checklist (per accept bullet 2)

Every §2 sheet names: exact path per scope, format, frontmatter/keys, precedence, and a minimal example (inline or via the referenced fixture block in the source sheet). Blind-fixture status: Codex ✔ (agent TOML exact syntax UNVERIFIED) · Cursor ✔ · Copilot ✔ · Gemini ✔ · Windsurf ✔ · Cline ✔ (rules single-file legacy UNVERIFIED) · aider ✔ · opencode ✔ (singular-dir alias UNVERIFIED) · Crush ✔ (Windows global path UNVERIFIED) · Continue ✔ (AGENTS.md consumption UNVERIFIED-absent) · Amp ✔ · Zed ✔ · Roo ✔ (archived) · Kilo ✔ · Claude Code ✔ (all six fixture classes in §2 sheet: CLAUDE.md, agent, skill, settings+hooks, rule, .mcp.json, plugin) [CC1–CC8].

## §5 Sources

Codex: [CX1] https://developers.openai.com/codex/subagents · [CX2] https://developers.openai.com/codex/skills · [CX3] https://developers.openai.com/codex/guides/agents-md · [CX4] https://developers.openai.com/codex/config-advanced · [CX5] https://developers.openai.com/codex/custom-prompts · [CX6] https://developers.openai.com/codex/config-reference · [CX7] https://developers.openai.com/codex/mcp · [CX8] https://developers.openai.com/codex/cli/reference
Gemini: [GM1] google-gemini.github.io/gemini-cli/docs/get-started/configuration.html · [GM2] geminicli.com/docs/core/subagents/ · [GM3] geminicli.com/docs/cli/skills/ · [GM4] geminicli.com/docs/hooks/reference/ · [GM5] google-gemini.github.io/gemini-cli/docs/cli/custom-commands.html · [GM6] google-gemini.github.io/gemini-cli/docs/extensions/ · [GM7] developers.googleblog.com/an-important-update-transitioning-gemini-cli-to-antigravity-cli/ · [GM8] developers.googleblog.com/tailor-gemini-cli-to-your-workflow-with-hooks/
Cursor: [CU1] cursor.com/docs/rules · [CU2] cursor.com/docs/hooks · [CU3] cursor.com/docs/subagents · [CU4] cursor.com/help/customization/skills · [CU5] cursor.com/docs/context/mcp · [CU6] cursor.com/changelog/1-6 · [CU7] cursor.com/changelog/2-4 · [CU8] cursor.com/docs/cli/reference/configuration · [CU9] cursor.com/docs/cli/reference/permissions · [CU10] cursor.com/help/customization/extensions · [CU11] cursor.com/docs/agent/tools · [CU12] cursor.com/docs/cli/using
Copilot: [CP1] docs.github.com/en/copilot/reference/custom-agents-configuration · [CP2] docs.github.com/en/copilot/concepts/agents/about-agent-skills · [CP3] docs.github.com/en/copilot/how-tos/configure-custom-instructions/add-repository-instructions · [CP4] docs.github.com/en/copilot/reference/hooks-configuration · [CP5] code.visualstudio.com/docs/copilot/customization/prompt-files · [CP6] code.visualstudio.com/docs/copilot/customization/mcp-servers · [CP7] docs.github.com/copilot/how-tos/agents/copilot-coding-agent/extending-copilot-coding-agent-with-mcp · [CP8] docs.github.com/en/copilot/reference/copilot-cli-reference/cli-config-dir-reference · [CP9] github.blog/changelog/2025-09-24-deprecate-github-copilot-extensions-github-apps/ · [CP10] code.visualstudio.com/docs/copilot/customization/custom-agents · [CP11] code.visualstudio.com/api/extension-guides/ai/chat · [CP12] docs.github.com/en/copilot/how-tos/copilot-cli/customize-copilot/add-mcp-servers · [CP13] docs.github.com/en/copilot/how-tos/use-copilot-agents/coding-agent/customize-the-agent-environment · [CP14] code.visualstudio.com/docs/copilot/customization/custom-instructions · [CP15] github.blog/changelog/2025-12-18-github-copilot-now-supports-agent-skills/ · [CP16] docs.github.com/en/copilot/how-tos/copilot-cli/customize-copilot/add-custom-instructions
Windsurf/Devin: [WS1] https://docs.devin.ai/desktop/cascade/memories · [WS2] https://docs.devin.ai/desktop/cascade/hooks · [WS3] https://docs.devin.ai/desktop/cascade/skills · [WS4] https://docs.devin.ai/desktop/cascade/workflows · [WS5] https://docs.devin.ai/desktop/cascade/mcp · [WS6] https://docs.devin.ai/desktop/cascade/agents-md · [WS7] https://devin.ai/blog/windsurf-is-now-devin-desktop/ · [WS8] https://docs.devin.ai/windsurf/plugins/getting-started
Cline: [CL1] docs.cline.bot/features/cline-rules · [CL2] docs.cline.bot/features/hooks · [CL3] cline.bot/blog/cline-v3-36-hooks · [CL4] docs.cline.bot/features/slash-commands/workflows · [CL5] docs.cline.bot/customization/skills · [CL6] docs.cline.bot/mcp/configuring-mcp-servers · [CL7] docs.cline.bot/features/subagents · [CL8] docs.cline.bot/sdk/plugins · [CL9] docs.cline.bot/customization/clineignore
aider: [AI1] aider.chat/docs/config/aider_conf.html · [AI2] aider.chat/docs/usage/conventions.html · [AI3] aider.chat/docs/usage/commands.html · [AI4] aider.chat/docs/usage/lint-test.html · [AI5] github.com/Aider-AI/aider/issues/4506 · [AI6] aider.chat/docs/scripting.html · [AI7] aider.chat/docs/repomap.html · [AI8] aider.chat/docs/git.html
opencode: [OC1] https://opencode.ai/docs/config/ · [OC2] https://opencode.ai/docs/agents/ · [OC3] https://opencode.ai/docs/rules/ · [OC4] https://opencode.ai/docs/commands/ · [OC5] https://opencode.ai/docs/plugins/ · [OC6] https://opencode.ai/docs/skills/ · [OC7] https://opencode.ai/docs/mcp-servers/ · [OC8] https://opencode.ai/docs/permissions/
Crush: [CR1] github.com/charmbracelet/crush (README) · [CR2] github.com/charmbracelet/crush/blob/main/internal/config/config.go · [CR3] github.com/charmbracelet/crush/tree/main/docs/hooks · [CR4] github.com/charmbracelet/crush/issues/1807
Continue: [CT1] https://docs.continue.dev/reference · [CT2] https://docs.continue.dev/customize/deep-dives/rules · [CT3] https://docs.continue.dev/customize/deep-dives/prompts · [CT4] https://docs.continue.dev/customize/deep-dives/mcp · [CT5] https://docs.continue.dev/customize/deep-dives/configuration · [CT6] https://docs.continue.dev/guides/cli · [CT7] https://docs.continue.dev/guides/understanding-configs
Amp: [AM1] ampcode.com/manual · [AM2] ampcode.com/manual/plugin-api · [AM3] ampcode.com/news/neo · [AM4] ampcode.com/news/agent-skills · [AM5] ampcode.com/news/slashing-custom-commands · [AM6] ampcode.com/news/toolboxes · [AM7] ampcode.com/news/hooks · [AM8] ampcode.com/news/tool-level-permissions · [AM9] ampcode.com/news/custom-agents
Zed: [ZD1] https://zed.dev/docs/ai/instructions · [ZD2] https://zed.dev/docs/ai/skills · [ZD3] https://zed.dev/docs/ai/mcp · [ZD4] https://zed.dev/docs/ai/tools · [ZD5] https://zed.dev/docs/ai/tool-permissions · [ZD6] https://zed.dev/docs/ai/external-agents · [ZD7] https://zed.dev/docs/extensions/mcp-extensions · [ZD8] https://github.com/zed-industries/zed/blob/main/docs/src/extensions/slash-commands.md · [ZD9] https://zed.dev/docs/configuring-zed (via https://github.com/zed-industries/zed/blob/main/docs/src/configuring-zed.md) · [ZD10] https://zed.dev/docs/ai/agent-profiles
Roo: [RO1] https://docs.roocode.com/features/custom-modes · [RO2] https://docs.roocode.com/features/custom-instructions · [RO3] https://docs.roocode.com/features/slash-commands · [RO4] https://docs.roocode.com/features/mcp/using-mcp-in-roo · [RO5] https://docs.roocode.com/sunset · [RO6] https://docs.roocode.com/features/boomerang-tasks
Kilo: [KL1] https://kilo.ai/docs/customize/custom-modes · [KL2] https://kilo.ai/docs/advanced-usage/custom-rules · [KL3] https://kilo.ai/docs/customize/skills · [KL4] https://kilo.ai/docs/customize/agents-md · [KL5] https://kilo.ai/docs/automate/mcp/using-in-kilo-code · [KL6] https://kilo.ai/docs/automate/extending/plugins · [KL7] https://kilo.ai/docs/features/slash-commands/workflows
Field scan: [FS1] qwenlm.github.io/qwen-code-docs/en/users/configuration/settings/ · [FS2] docs.factory.ai/cli/configuration/agents-md · [FS3] github.com/block/goose (docs/guides/context-engineering/using-goosehints.md) · [FS4] docs.warp.dev/knowledge-and-collaboration/rules · [FS5] docs.trae.ai/ide/rules · [FS6] docs.augmentcode.com/cli/rules · [FS7] docs.devin.ai/work-with-devin/mcp · [FS8] junie.jetbrains.com/docs/junie-cli-mcp-configuration.html · [FS9] codelabs.developers.google.com/getting-started-google-antigravity · [FS10] developers.openai.com/codex/ide
Claude Code: [CC1] https://code.claude.com/docs/en/memory.md · [CC2] https://code.claude.com/docs/en/sub-agents.md · [CC3] https://code.claude.com/docs/en/skills.md · [CC4] https://code.claude.com/docs/en/plugins.md · [CC5] https://code.claude.com/docs/en/discover-plugins.md (+ https://code.claude.com/docs/en/plugin-marketplaces.md) · [CC6] https://code.claude.com/docs/en/hooks-guide.md · [CC7] https://code.claude.com/docs/en/mcp.md · [CC8] https://code.claude.com/docs/en/settings.md · [CC9] https://code.claude.com/docs/en/how-claude-code-works.md · [CC10] https://code.claude.com/docs/en/context-window.md · [CC11] https://code.claude.com/docs/en/claude-directory.md · [CC12] https://code.claude.com/docs/en/commands.md
