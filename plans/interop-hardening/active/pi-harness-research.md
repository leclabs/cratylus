# pi-harness-research — the Pi harness joins the roster

**Lane** research (deep-research class), Nico judges · dispatchable now (no deps) · joins the
wave(0) research corpus.

## Static

`../completed/harness-landscape-research.RETURN.md` (the matrix + sheet format to extend — emit the
same shape) · `../completed/standards-compat-research.RETURN.md` (standards columns).

## Scope

Deep-research the **Pi** harness (identify the canonical project — candidate referent: badlogic's
`pi` minimal agent — confirm identity first, from primary sources): capabilities/features ·
configuration surfaces (paths, formats, frontmatter, scopes/precedence) · consumer context shapes ·
standards posture (AGENTS.md / Agent Skills / MCP) · and ESPECIALLY its **plugin/extension
architecture** — Pi is the roster's demonstration case for delivering common harness capabilities
(agents · skills · rules · hooks) via plugin where native support is absent. Deliver: the Pi row +
config-contract sheet + a plugin-arch capability map (what a plugin CAN reach: events, context
injection, tools), all cited or UNVERIFIED.

## Accept (falsifiers)

- Identity pinned to a canonical repo/doc URL (an unpinned "Pi" fails).
- Row + sheet in the landscape RETURN's exact format, every cell cited or UNVERIFIED; sheet
  fixture-precise.
- Plugin-arch map states, per common capability, whether a Pi plugin can deliver it, with the
  mechanism named (an unmapped capability fails).
