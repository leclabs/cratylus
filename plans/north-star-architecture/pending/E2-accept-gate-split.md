# E2 — R2: accept-gate — algorithm → forge, policy-data injected from CANON

**static:** `packages/agent-anatomy/src/toolkit/cold-oracle/{accept.ts:166-169, residue.ts, structural-parsimony.ts,
oracle.ts}` · `operator-lexicon.ts` · `cold-oracle.sh:29` · `../census/C1` · `../NORTH-STAR.md §2 R2`.
**scope:** move the pure leg-witness ALGORITHM (`canonical/signified/coldBlindStatic/partitioned/parsimonious/
regenerable`, `universalCell`) into `agent-forge` (`forge/src/validate/`). Keep the corpus POLICY-DATA
(palimpsest tokens `polis/oikos/conatus`, `operator-lexicon`, the repo-guard) in anatomy; INJECT via
`universalCell(cell, homes, policy)`. `exemplify` (the pipeline gate) is a DISTINCT concern — do not merge.
**accept:** `git grep -iE 'polis|oikos|conatus' packages/agent-forge/src` = empty; the gate algorithm lives in
forge; `universalCell` takes an injected `policy`; the accept suite still passes on the corpus; typecheck green.
**dep:** E1 (forge structure + the validate home).
