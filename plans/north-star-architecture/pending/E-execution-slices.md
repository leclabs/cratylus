# E — execution slices (DESIGN-DEPENDENT — specced after D1 ratifies the north-star)

**Status:** PLACEHOLDER. The refactor execution shards are cut along the north-star's ratified module
boundaries and CANNOT be faithfully specced until D1 lands (census-not-format: no from-memory sketch of an
execution whose target boundary is still under debate).

**Anticipated slices (provisional, MECE along the current findings — to be finalized by D1):**

- **E1 — relocate forge-concern tooling** anatomy/toolkit → agent-forge (project*, cold-oracle accept-gate,
  human-README projection, project-targets), splitting the `hooks.ts` generic-lift (forge) from the specific
  stance-guard cells (anatomy). Guardrail `*.sh` + judge-prompt STAY in anatomy (runtime substance).
- **E2 — memory concern ownership**: move memory-store seed CONTENT + filename set + v1-retirement doctrine
  out of `deploy/seeds.ts` into agent-memory (resolve the forge-cannot-import-memory D4 seam — likely via a
  dependency-inversion interface or by making the seed a memory-tool responsibility); dedup `.agent-factory.config`
  parsing (A1); purge genus/dream prose that re-specifies tool mechanics (C3-Inv2).
- **E3 — citation cruft removal**: delete the orphaned `REF_RE` parser + unimplemented docs:check gate;
  finish/settle the `[[…]]`→(anchor|nothing) per the D1 fork resolution; supersede the Operator's shotgun WT.
- **E4 — purity / DI**: extract pure cores per C2 §C (seed template, dream.compact partition, loadNodeConfig);
  remove the codex→claude sideways adapter edge (D2) into a neutral home.

Each E-slice becomes its own blind-dispatchable task-file (static · scope · accept) authored during/after D1.

**dep:** D1 (blocked until north-star ratified + Operator-signed).
