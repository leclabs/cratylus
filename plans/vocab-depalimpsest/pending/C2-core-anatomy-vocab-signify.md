# C2 — signify pass on the CORE anatomy vocabulary (`anatomy` · `organ` · `organ-value`)

**GATE (why pending):** blocked on (a) the Operator's steer on the project-IDENTITY dimension — the package is
named `agent-anatomy`; retiring "anatomy"/"organ" is a foundational rename, not a mechanical sweep — and (b) a
fuller cold signify pass to firm up n. NOT a task-dep. Orthogonal to plugin-cli and to C1 (genus rename), though
all three are facets of the same finding: the anatomy's abstract/metaphorical vocabulary carries signification debt.

**Evidence on record (why this exists):** an isolated cold probe (`/tmp/cold-panel/altitude.txt`, 2026-07,
unprimed) for the model's natural vocabulary produced — per-AGENT structure: "cognitive architecture / **modules**",
"**primitives**"; shared LIBRARY unit: "**trait**" (×2), with "organ? **No** — an organ is load-bearing and singular
within one body, not swapped between organisms." **`organ`/`anatomy` appeared in 0 of 4 unprimed reads.** This
contradicts the recalled ORIGINAL signify ("anatomy/organs"), indicating the original was primed / confirmation-biased
/ drift — not a clean cold generation. The plugin-review's "organ mis-signifies" is corroborated; nico's
"altitude defends organ" hypothesis was REFUTED by the probe (`apparatus-under-zero-trust` — the instrument
disagreed with the warm prior).

**scope (the signify pass — NOT yet a rename):**

1. **Probe (bigger n).** Run the isolated cold panel (≥5 samples/question, `/tmp`) on: (a) the DIMENSION concept —
   "a named configurable attribute of one agent that holds a chosen value (its role, its objective, its reasoning
   strategy)"; (b) the shareable VALUE concept — "one reusable, installable configuration piece"; (c) the CORPUS/
   collective — "the library/model of all such attributes + pieces." Recover each concept's best-fit sign
   unprimed, then a primed compare of candidates (organ · module · attribute · trait · primitive · fragment · …).
2. **Decide (signify, nico) the replacements — IFF the panel confirms** organ/anatomy mis-signify. Preserve the
   real granularity split: the DIMENSION (`role`, `objective`) vs the VALUE (`role/curate`) are two concepts and
   may take two signs (e.g. dimension→`attribute`/`module`; value→`fragment`/`trait` — `fragment` already the
   MODEL term). "anatomy" (the corpus/model) is a third.
3. **Assess blast radius** before any rename: `Organ`/`ORGAN_NAMES`/`organs/` dirs · the `agent-anatomy` package
   name + all imports · MODEL/ENGINE/CANON prose · the plugin `definePlugin` field (NORTH-STAR §3) · every organ
   README · the accept-gate. This is the largest rename in the repo — its own execution spec follows the decision.

**accept (of THIS signify pass, not the rename):** a cold panel result on record (≥5×3); a signify DECISION
(keep-or-rename, with the chosen signs + rationale) ratified against the panel; if rename, a blast-radius census +
a follow-on execution spec authored `census-grounds-spec`. The rename itself is a SEPARATE gated task.

**dep:** GATE(Operator steer on project-identity + go-ahead for the fuller panel). Coordinate with C1 (genus) and
plugin-cli §10.2 (the `definePlugin` field name depends on this outcome).
