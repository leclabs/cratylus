# A2 — reconcile the stale golden fixture (palimpsest vocab + retired memory model)

**static:** `packages/agent-forge/test/adapters/ir-bridge/agent-anatomy.agent-forge.json` ·
`packages/agent-forge/test/adapters/ir-bridge/round-trip.test.ts` (the consumer) · `MAPPING.md`.

**context (censused):** the fixture's agent `body` fields carry DOUBLE palimpsest — the retired founding vocab
(`founder of polis`, `commons`, `founder-charter`) AND the retired 3-layer memory model (`SELF.md`/`MEMORY.md`/
`EPISODIC.md`, `vault`, `AGENTS.md` routing) that E6a–E6c already replaced in SOURCE with the CoALA stores
(SEMANTIC/PROCEDURAL/EPISODIC.jsonl) + the `memory` tool. The fixture is STALE against current source.

**scope:** FIRST determine the fixture's provenance — is it regenerated by a script/`project` run, or hand-frozen?
(`round-trip.test.ts` is the tell: read whether it re-derives the fixture or asserts against the committed bytes.)

- If REGENERATED: fix the SOURCE cells it derives from, then regenerate; the vocab + memory model correct
  themselves. (Verify no source cell still carries the retired vocab per `MAPPING.md`.)
- If HAND-FROZEN: update the fixture bytes to net-current — retired founding vocab → `MAPPING.md` concretes;
  the SOUL memory-model prose → the current CoALA/`memory`-tool model (mirror what the live projection emits for
  one agent; do not invent). EXCLUDE the `Genus`-axis `STANCE`/`CONATUS` tokens.

**accept (falsifier):** `git grep -iE '\bpolis\b|commons|founder-charter|SELF\.md|MEMORY\.md|\bvault\b' -- packages/agent-forge/test/adapters/ir-bridge/` empty; the fixture reflects the current memory model
(SEMANTIC/PROCEDURAL/EPISODIC.jsonl + `memory` tool, no SELF/MEMORY/vault); `pnpm -C packages/agent-forge test`
green (the round-trip suite passes on the reconciled fixture); a cold read of a sample agent body decodes the
current model.

**dep:** none (test fixture; independent of the CLI redesign). Sequence AFTER any source-cell vocab fix if the
fixture is regenerated.
