# cite-once-sweep

**State:** active (1/6 — `signify` ✓) · **Lead:** Nico · **Source:** the `self-sufficient-formalism` cite-once convention; the clause refinement (`93299ae`) unblocked it.

## Progress

- ✅ `signify` — `≜` formula → `Bindings:` region; deduped the twice-cited `[[anchor-routing]]`; composition derives identical (precise-circumscription · anchor-routing); verify.py PASS; blind-judge B ≽ A.
- ⬜ `materialize` · `conceptualize` · `dream` · `exemplify` · `formalize` — remaining.

## Intent

Migrate the older pipeline skills to the `Bindings:`-region cite-once convention so every formal cell has a
single citation home, then harden the gate to enforce it. This is the idea-corpus sweep that the formal
set-builder / notation work pointed toward.

## Scope

The pre-bindings pipeline cells still carrying a standalone `≜` formula + step/see-also re-citations:
`signify` · `materialize` · `conceptualize` · `dream` · `exemplify` · `formalize`. `praxis` is the done exemplar.

## Approach

Per cell: add a `Bindings:` region, derive composition from it, drop the standalone `≜` formula, dedupe
re-citations. Each migration **blind-judge-gated** (a fresh blind nico judges equivalence ≽ the original).
Smallest / most-foundational first (`signify`). Per-cell, not mechanical.

## Done when

- All six cells use the Bindings convention; round-trip + blind-judge PASS on each.
- The transitional **CITE-TWICE NOTE is promoted to FAIL** in `verify.py` once the last both-forms cell clears.
- The convention scope-clause (claim-not-concept, `93299ae`) holds across all migrated cells.
