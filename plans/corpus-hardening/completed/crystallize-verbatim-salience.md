# crystallize-verbatim-salience

**State:** completed (2026-06-17, `6106f1d`) · **Lead:** Nico · **Source:** the recommendation-style verbatim-organ fix (`d46af93`) proved the insight in code.

**DONE.** Rule homed in `reader-prior-projection.md` (the projection floor): dereferenceability necessary-not-sufficient; a contested disposition projects `render: verbatim`, not a bare anchor; `render: verbatim` documented as legit for a *disposition* (not identity-protocol-only). `recommendation-style`'s inline re-derivation trimmed to a citation (cite-once). CE∧ME blind-judge PASS; verify.py PASS.

## Intent

Make canon the rule this session proved: a disposition that must **override a strong competing base-prior**
cannot ride a density-collapsed bare anchor — it needs verbatim salience. Dereferenceable ≠ wins-at-inference.

## Approach

Refine `reader-prior-projection`'s floor (and/or `anchor-legibility-budget`): "the reader can dereference"
is necessary but not sufficient — add the clause that a competing-prior disposition must project **verbatim**
(the `render: verbatim` organ path), not as a bare anchor. Cite the recommendation-style instance as the
proof. Gate by round-trip + blind-judge.

## Done when

- The competing-prior ⇒ verbatim-salience rule is stated in a corpus cell (not just my EPISODIC).
- `render: verbatim`-for-a-disposition is documented as a legitimate use of the organ mechanism (it currently
  reads as identity-protocol-only).
