# polis — Claude Code memory

Agent conventions are maintained in `AGENTS.md` (tool-agnostic, industry-standard). Claude Code does
not auto-load `AGENTS.md`, so it is imported here as a partial:

@AGENTS.md
