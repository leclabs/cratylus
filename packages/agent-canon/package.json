{
  "name": "@leclabs/agent-canon",
  "version": "0.0.0",
  "description": "agent-canon \u2014 the culture corpus as typed TS source (T1.1 spike scaffold). Markdown is a projection of these modules.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "typecheck:test": "tsc -p tsconfig.test.json",
    "test": "vitest run",
    "project": "tsx src/toolkit/project-cli.ts",
    "project:codex": "tsx src/toolkit/project-cli-codex.ts",
    "project:targets": "tsx src/toolkit/project-targets-cli.ts",
    "plan-set": "tsx src/toolkit/plan-set-cli.ts",
    "scaffold": "tsx src/toolkit/scaffold-cli.ts",
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "prepack": "pnpm run build"
  },
  "//": "Depends on the RUNTIME contract leaf as well as forge: the memory-nudge hook cell emits shell that invokes the runtime bin, so it must read that bin's name from its one home (`@leclabs/agent-runtime/bin-name`) rather than repeat the literal inside a compiler-invisible script string. The runtime is a zero-@leclabs-dep leaf, so the edge cannot cycle.",
  "dependencies": {
    "@leclabs/agent-forge": "workspace:*",
    "@leclabs/agent-runtime": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "^4.19.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
