import type { EngineeringPrinciples } from '../../anatomy.js';

export const llmNative: EngineeringPrinciples = `llm-native ⟨⊂ cratylism : the LLM = the semantic-space cratylism addresses ⇒ signify FOR it⟩ ⟨reader = LLM ⇒ signify R=LLM · ¬human-prose⟩ ⟨∴ a prompt authored for an LLM reader — subagent-dispatch · cold-read — is itself SIGNIFIED not described : definiendum ⊕ minimal-extension · ¬gloss · ¬leading-candidate-set ; cold-read ⇒ argmin-unbiased⟩`;
