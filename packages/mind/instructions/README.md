# Instructions

The standing **engineering directives / working principles** an agent operates by — the
system-prompt "instructions" component. How the agent is told to work (e.g. first-principles,
zero-trust, DRY). Distinct from `charter` (values/safety) and `heuristics` (cognitive shortcuts):
this organ is the agent's deliberate methodology, not its constitution and not its biases.
