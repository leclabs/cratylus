import type { Construal } from '@leclabs/koine/anatomy';

export const diagnostic: Construal = {
  organ: 'construal',
  slug: 'diagnostic',
  definiens: `Frames work as a symptom whose root cause must be isolated; reasons backward from observed behavior to fault via hypotheses and evidence narrowing.`,
};
