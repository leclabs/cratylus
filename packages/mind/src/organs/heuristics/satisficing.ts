import type { Heuristics } from '@leclabs/koine/anatomy';

export const satisficing: Heuristics = {
  organ: 'heuristics',
  slug: 'satisficing',
  definiens: `accept the first option that meets the aspiration level.`,
};
