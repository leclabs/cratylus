import type { Heuristics } from '@leclabs/koine/anatomy';

export const takeTheBest: Heuristics = {
  organ: 'heuristics',
  slug: 'take-the-best',
  definiens: `decide on the single best-discriminating cue, ignore the rest.`,
};
