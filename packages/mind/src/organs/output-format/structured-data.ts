import type { OutputFormat } from '@leclabs/koine/anatomy';

export const structuredData: OutputFormat = {
  organ: 'output-format',
  slug: 'structured-data',
  definiens: `Machine-parseable payload conforming to a schema — JSON/YAML/XML/CSV/key-value; consumed by another program, not primarily read by a human.`,
};
