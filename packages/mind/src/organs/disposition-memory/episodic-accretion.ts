import type { DispositionMemory } from '@leclabs/koine/anatomy';

export const episodicAccretion: DispositionMemory = {
  organ: 'disposition-memory',
  slug: 'episodic-accretion',
  definiens: `Concrete experiences are accumulated as discrete recallable episodes (case base) and matched by similarity at decision time; behavior shifts via precedent rather than by abstracting rules.`,
};
