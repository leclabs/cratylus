import type { SituationAwareness } from '@leclabs/koine/anatomy';

export const projection: SituationAwareness = {
  organ: 'situation-awareness',
  slug: 'projection',
  definiens: `Endsley SA level 3 — hold the whole and project it forward across the task.`,
};
