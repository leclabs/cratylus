import type { Ledger } from '@leclabs/koine/anatomy';

export const workingMemory: Ledger = {
  organ: 'ledger',
  slug: 'working-memory',
  definiens: `short-term, in-context state for the current cycle.`,
};
