import type { Framing } from '@leclabs/koine/anatomy';

export const riskOriented: Framing = {
  organ: 'framing',
  slug: 'risk-oriented',
  definiens: `Frames work through what can fail, break, or be exploited; foregrounds failure modes, edge cases, blast radius, and worst-case before benefits.`,
};
