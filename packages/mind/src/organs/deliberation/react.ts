import type { Deliberation } from '@leclabs/koine/anatomy';

export const react: Deliberation = {
  organ: 'deliberation',
  slug: 'react',
  definiens: `ReAct — interleave reasoning with tool/subagent actions and observations.`,
};
