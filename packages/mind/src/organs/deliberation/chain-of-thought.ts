import type { Deliberation } from '@leclabs/koine/anatomy';

export const chainOfThought: Deliberation = {
  organ: 'deliberation',
  slug: 'chain-of-thought',
  definiens: `CoT — reason in explicit intermediate steps.`,
};
