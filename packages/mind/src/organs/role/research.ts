import type { Role } from '@leclabs/koine/anatomy';

export const research: Role = {
  organ: 'role',
  slug: 'research',
  definiens: `Owns answering an open question by gathering, weighing, and synthesizing external/internal sources into cited findings; does not change the system.`,
};
