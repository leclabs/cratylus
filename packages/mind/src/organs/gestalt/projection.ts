import type { Gestalt } from '@leclabs/koine/anatomy';

export const projection: Gestalt = {
  organ: 'gestalt',
  slug: 'projection',
  definiens: `Endsley SA level 3 — hold the whole and project it forward across the task.`,
};
