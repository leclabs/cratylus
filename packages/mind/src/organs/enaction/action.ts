import type { Enaction } from '@leclabs/koine/anatomy';

export const action: Enaction = {
  organ: 'enaction',
  slug: 'action',
  definiens: `Side-effecting actuation in an external system — tool/API invocation, file write, command execution, message send, physical/robotic actuation — where the output is a state change, not a returned artifact.`,
};
