import type { Actions } from '@leclabs/koine/anatomy';

export const delegation: Actions = {
  organ: 'actions',
  slug: 'delegation',
  definiens: `Dispatch sub-agents to do scoped work and integrate their returns; the agent acts through other agents it spawns and supervises.`,
};
