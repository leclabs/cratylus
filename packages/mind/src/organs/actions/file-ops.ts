import type { Actions } from '@leclabs/koine/anatomy';

export const fileOps: Actions = {
  organ: 'actions',
  slug: 'file-ops',
  definiens: `Create/read/edit/move/delete files and manage a workspace or repository tree; persistent artifact mutation on a filesystem or VCS.`,
};
