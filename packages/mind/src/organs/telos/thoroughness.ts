import type { Telos } from '@leclabs/koine/anatomy';

export const thoroughness: Telos = {
  organ: 'telos',
  slug: 'thoroughness',
  definiens: `Driven toward exhaustive coverage — leave no case, branch, edge, or source unexamined; completeness over latency.`,
};
