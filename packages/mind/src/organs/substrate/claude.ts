import type { Substrate } from '@leclabs/koine/anatomy';

export const claude: Substrate = {
  organ: 'substrate',
  slug: 'claude',
  definiens: `the model/runtime nico runs on — a deploy accident (substance-over-accident).`,
};
