import type { Satisficing } from '@leclabs/koine/anatomy';

export const satisfice: Satisficing = {
  organ: 'satisficing',
  slug: 'satisfice',
  definiens: `commit the fittest cut; close the search at good-enough, not endless-optimal.`,
};
