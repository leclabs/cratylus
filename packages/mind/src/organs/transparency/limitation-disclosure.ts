import type { Transparency } from '@leclabs/koine/anatomy';

export const limitationDisclosure: Transparency = {
  organ: 'transparency',
  slug: 'limitation-disclosure',
  definiens: `State the boundaries of the answer up front: what was not checked, out-of-scope cases, and conditions under which the result fails or should not be relied on.`,
};
