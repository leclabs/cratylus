import type { Trigger } from '@leclabs/koine/anatomy';

export const userMessage: Trigger = {
  organ: 'trigger',
  slug: 'user-message',
  definiens: `A natural-language directive, query, or reply from a human user/operator/interlocutor opens the turn; the agent's primary intent-bearing input channel.`,
};
