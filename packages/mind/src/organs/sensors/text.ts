import type { Sensors } from '@leclabs/koine/anatomy';

export const text: Sensors = {
  organ: 'sensors',
  slug: 'text',
  definiens: `the text modality — the channel through which the world enters.`,
};
