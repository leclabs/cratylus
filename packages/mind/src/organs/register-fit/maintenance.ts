import type { RegisterFit } from '@leclabs/koine/anatomy';

export const maintenance: RegisterFit = {
  organ: 'register-fit',
  slug: 'maintenance',
  definiens: `CAT: hold one's own style regardless of interlocutor.`,
};
