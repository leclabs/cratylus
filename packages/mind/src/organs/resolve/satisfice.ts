import type { Resolve } from '@leclabs/koine/anatomy';

export const satisfice: Resolve = {
  organ: 'resolve',
  slug: 'satisfice',
  definiens: `commit the fittest cut; close the search at good-enough, not endless-optimal.`,
};
