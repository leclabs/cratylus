import type { Resolve } from '@leclabs/koine/anatomy';

export const optimize: Resolve = {
  organ: 'resolve',
  slug: 'optimize',
  definiens: `maximize — select the option of highest expected value.`,
};
