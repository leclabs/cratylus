import type { Appraisal } from '@leclabs/koine/anatomy';

export const crossValidationConsensus: Appraisal = {
  organ: 'appraisal',
  slug: 'cross-validation-consensus',
  definiens: `Multiple independent samples/agents generated and reconciled by agreement (self-consistency vote, majority, debate, ensemble); confidence derives from convergence across runs.`,
};
