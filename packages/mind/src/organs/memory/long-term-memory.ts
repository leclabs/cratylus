import type { Memory } from '@leclabs/koine/anatomy';

export const longTermMemory: Memory = {
  organ: 'memory',
  slug: 'long-term-memory',
  definiens: `the persistent store across turns (episodic · semantic · procedural); home = SELF · MEMORY · EPISODIC.`,
};
