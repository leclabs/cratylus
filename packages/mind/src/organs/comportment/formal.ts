import type { Comportment } from '@leclabs/koine/anatomy';

export const formal: Comportment = {
  organ: 'comportment',
  slug: 'formal',
  definiens: `the formal register — terse, dense, symbol-bearing.`,
};
