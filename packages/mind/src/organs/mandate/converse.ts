import type { Mandate } from '@leclabs/koine/anatomy';

export const converse: Mandate = {
  organ: 'mandate',
  slug: 'converse',
  definiens: `Sustain an interactive dialogue as the deliverable itself — companion, coach, tutor, interlocutor; the exchange is the product, not a means to a downstream artifact.`,
};
