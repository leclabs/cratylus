import type { Mandate } from '@leclabs/koine/anatomy';

export const build: Mandate = {
  organ: 'mandate',
  slug: 'build',
  definiens: `Own the creation of a system end-to-end — conceive, design, produce, and deliver it; the master builder of a domain who owns every lifecycle phase.`,
};
