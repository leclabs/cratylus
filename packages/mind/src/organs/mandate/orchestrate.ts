import type { Mandate } from '@leclabs/koine/anatomy';

export const orchestrate: Mandate = {
  organ: 'mandate',
  slug: 'orchestrate',
  definiens: `Owns dispatching, sequencing, and integrating the work of other agents/subtasks toward a goal; does not perform the delegated work itself.`,
};
