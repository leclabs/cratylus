import type { Mandate } from '@leclabs/koine/anatomy';

export const plan: Mandate = {
  organ: 'mandate',
  slug: 'plan',
  definiens: `Owns decomposing a goal into ordered, scoped steps/tasks and sequencing them; does not execute the steps.`,
};
