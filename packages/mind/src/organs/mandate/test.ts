import type { Mandate } from '@leclabs/koine/anatomy';

export const test: Mandate = {
  organ: 'mandate',
  slug: 'test',
  definiens: `Owns designing and running checks (cases, fixtures, harnesses) that exercise an artifact to surface defects, reporting pass/fail evidence; does not fix what it finds.`,
};
