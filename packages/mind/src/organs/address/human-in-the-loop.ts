import type { Address } from '@leclabs/koine/anatomy';

export const humanInTheLoop: Address = {
  organ: 'address',
  slug: 'human-in-the-loop',
  definiens: `each action awaits human approval before it executes.`,
};
