import type { Instructions } from '@leclabs/koine/anatomy';

export const dry: Instructions = {
  organ: 'instructions',
  slug: 'dry',
  definiens: `one canonical home per idea; reference it, never duplicate.`,
};
