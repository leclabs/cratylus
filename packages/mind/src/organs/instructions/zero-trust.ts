import type { Instructions } from '@leclabs/koine/anatomy';

export const zeroTrust: Instructions = {
  organ: 'instructions',
  slug: 'zero-trust',
  definiens: `derive a subject's properties from observation, never its self-report.`,
};
