import type { Instructions } from '@leclabs/koine/anatomy';

export const invokeTheCanonical: Instructions = {
  organ: 'instructions',
  slug: 'invoke-the-canonical',
  definiens: `invoke the canonical skill; re-derive only when none exists.`,
};
