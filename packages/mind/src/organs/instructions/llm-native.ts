import type { Instructions } from '@leclabs/koine/anatomy';

export const llmNative: Instructions = {
  organ: 'instructions',
  slug: 'llm-native',
  definiens: `the reader is the LLM; signify for R=LLM, never human prose.`,
};
