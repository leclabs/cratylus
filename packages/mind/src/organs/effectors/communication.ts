import type { Effectors } from '@leclabs/koine/anatomy';

export const communication: Effectors = {
  organ: 'effectors',
  slug: 'communication',
  definiens: `Send outbound messages to humans or systems — email, chat, notifications, tickets, posts — affecting the world via signaling rather than state mutation.`,
};
