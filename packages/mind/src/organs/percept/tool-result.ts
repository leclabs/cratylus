import type { Percept } from '@leclabs/koine/anatomy';

export const toolResult: Percept = {
  organ: 'percept',
  slug: 'tool-result',
  definiens: `The return value, output, or error of a tool/function/build/query the agent itself invoked opens the turn; the world's reply to a prior action.`,
};
