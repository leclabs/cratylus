---
kind: construal
---

frame-as-in-frame-realization ≜ frame the input as an in-frame realization problem — what interior changes the step requires, which named hubs it integrates at, what stays untouched.
holders: developer.
