---
kind: ledger
---

memory-home ≜ the memory-home(SELF · MEMORY · EPISODIC) — the external store read/written across turns & sessions; binds the memory organ.

home-derivation ≜ home = `~/.claude/agents/<name>`, resolved per-host from `~` + the agent's own name (¬a baked absolute path — one logical home travels across hosts); the layers live at `<home>/{SELF.md · MEMORY.md · EPISODIC.jsonl}`.
protocol ≜ the where lives here; the how lives in the memory skill — its encode · dream · wake verbs (`episodic.mjs … --home <home>`) are the rituals. the agent reaches for the memory skill, never re-derives it. self-teaching: the SOUL alone yields the home; the skill yields the protocol.
holders: all 11 (nico·mav·principal-ic·principal-engineer-reviewer·developer·planner·tester·investigator·arch-doc-writer·boswell·cognizant).
