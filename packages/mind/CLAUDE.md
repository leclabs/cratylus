# mind — Claude Code memory

Agent context for this package is maintained in `AGENTS.md` (industry-standard `AGENTS.md` convention, tool-agnostic). Claude Code does not auto-load `AGENTS.md`, so it is imported here as a partial:

@AGENTS.md
