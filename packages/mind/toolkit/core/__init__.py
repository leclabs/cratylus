"""Pipeline core: the shared cell reader + the canonical-superset IR that the
compose -> decorate -> render -> place stages translate through
([[canonical-superset-ir]] -- one form, every dialect a projection of it)."""
