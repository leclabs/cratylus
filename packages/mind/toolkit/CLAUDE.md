# toolkit — Claude Code memory

Agent context for this directory is maintained in `AGENTS.md` (tool-agnostic). Claude Code does not auto-load `AGENTS.md`, so it is imported here as a partial:

@AGENTS.md
