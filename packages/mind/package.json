{
  "name": "@leclabs/mind",
  "version": "0.0.0",
  "private": true,
  "description": "mind — the culture corpus as typed TS source (T1.1 spike scaffold). Markdown is a projection of these modules.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "codegen": "tsx src/toolkit/codegen.ts",
    "project": "tsx src/toolkit/project-cli.ts"
  },
  "dependencies": {
    "@leclabs/koine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "^4.19.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
