---
kind: disposition-memory
---

in-context-recall ≜ Past lessons persist only as retrievable artifacts (notes, logs, vault) injected into context at runtime; the agent reads them but its underlying policy is unchanged between reads.
