---
kind: enaction
---

emit-context-dump ≜ emit the labeled context-dump — inputs, instructions, tools, state, constraints, blind-spots, each tagged observed|inferred — at R=LLM density; re-point divergences to canonical homes.
holders: cognizant.
