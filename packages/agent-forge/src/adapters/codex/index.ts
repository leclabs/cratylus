// The codex harness surface. Since depalimpsest-ir-intake S6 this barrel is
// PROJECTION-ONLY: the IR `Adapter` (detect/read/write) it used to also export
// went with the excised IR-intake lineage.

// The anatomy→codex projection — the second harness, proving agent-canon reaches
// every agent-forge harness for free. The composed SOUL body is harness-neutral,
// so the `ResolvedSkill` shape is shared with the claude adapter.
export {
  type ResolvedSkill,
  agentToCodexToml,
  agentToCodexTomlObject,
  skillToCodexMd,
  agentsMdSurface,
  codexHarnessAdapter,
} from './anatomy.js';
