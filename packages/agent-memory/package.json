{
  "name": "@leclabs/agent-memory",
  "version": "0.0.0",
  "private": true,
  "description": "The standalone `memory` tool — a dependency-free CLI (dist/memory.mjs) an agent runs to encode events and consolidate memory across sessions",
  "license": "MIT",
  "type": "module",
  "bin": {
    "memory": "./dist/memory.mjs"
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsup": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
