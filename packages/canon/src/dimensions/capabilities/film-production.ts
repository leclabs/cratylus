import type { Capabilities } from '../../manifest.js';

// The craft the software SERVES, not the software. Its decomposition is the
// production ladder every studio already runs, and the two laws a generative
// pipeline most often collapses: IDENTITY (is this the same character) and
// CONTINUITY (does the staging flow) are different problems with different
// instruments, and an art-bible entry is approved ONCE and applied everywhere
// rather than re-specified per shot.
export const filmProduction: Capabilities = `film-production ≜ ⟨development breakdown art-bible shot-design continuity cut⟩ ⟨identity ⊥ continuity⟩ ⟨art-bible ≜ approve-once · apply-everywhere⟩ ⟨industry-standard ≻ invention⟩`;
