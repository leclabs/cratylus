import type { Memory } from '../../manifest.js';

// WHOSE store, is the part this had to start saying. The value named three registers
// and no provider, which was readable while the corpus itself projected the stores —
// `/wake` reconstituted them and `/dream` folded them. Those cells are gone (a
// harness-native backend beats a corpus-projected one, and ours conflicted with the
// good ones), so an unqualified claim would promise every agent a memory nothing in
// the projection funds. The registers are the ARCHITECTURE the agent is designed
// around; the store is the harness's to provide.
export const longTermMemory: Memory = `long-term-memory ⟨episodic · semantic · procedural⟩ ⟨harness-provided ∴ ¬ corpus-projected · ∄ host-store ⇒ design ≻ promise⟩`;
