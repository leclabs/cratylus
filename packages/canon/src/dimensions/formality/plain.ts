import type { Formality } from '../../manifest.js';

// The pole neither `formal` nor `expansive` occupied. `formal` is terse but
// carries notation, so an agent bound to it emits glyphs at its operator.
// `expansive` drops the notation and pays for it by being unhurried, which
// licenses exactly the volume the register was meant to cut. `plain` is the
// conjunction that was missing: prose, and economical about it.
//
// ECONOMY BINDS EMISSION VOLUME, and it lives here rather than on `satisficing`
// or `transparency` because both of those govern the WORK — `satisfice` lowers
// the engineering bar and `answer-only` withholds the rationale. Trading either
// for shorter replies buys prose with behaviour.
//
// REGISTER ONLY. The closing tail a report owes its operator is `autonomy`'s —
// `check-in` names it, because what a reply must CONTAIN at the end is report
// structure, not a register. A value that governed both would be the one home
// for two concepts, and would apply the tail to whichever formality happened to
// be bound.
//
// THIS DEFINIENS IS PROSE, AND THAT IS THE POINT. ρ(`emission-definiens`) =
// human (`test/reader-register.ts`): a value whose referent is the operator's
// reading experience is written in the register it describes. The σ* form of
// these same clauses was comprehended and disobeyed — quoted back byte-for-byte
// by the session it governed while that session kept emitting fragments.
//
// CONCISE, BUT NOT AT THE COST OF THE PROHIBITIONS. A shorter draft — "Fragments
// belong inside list items, and a list is for a real enumeration" — dropped
// `never standing alone`, and body fragmentation rose sharply across three runs.
// The prohibitions ARE the binding clauses; the positive halves alone are read
// as a preference. Economy means no wasted words, never a dropped constraint.
//
// THE RATIOS IN THIS FILE'S HISTORY WERE MEASURED TWICE, and the first
// instrument was wrong: it scored every markdown line matching a list OR HEADING
// marker, so a reply of long prose paragraphs under two section headings scored
// as fragmented. Direction held under both instruments and the effect was large,
// which is why the ruling stands; magnitudes from the first are not comparable to
// the second. Current instrument — body bullet/numbered lines only, action-item
// tail excluded because it is mandated: 0.11 over five runs, three of them zero.
export const plain: Formality = `plain ≜ Write prose: complete sentences joined into paragraphs, each point made once, no more length than the decision carries. Notation stays in your reasoning and out of any reply to the operator. Fragments belong inside list items only, never standing alone as a sentence or a paragraph. The one list a reply carries is its closing tail of action items; everywhere else, explain in prose.`;
