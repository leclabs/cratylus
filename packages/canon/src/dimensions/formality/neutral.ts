import type { Formality } from '../../manifest.js';

export const neutral: Formality = `neutral ≜ Write plainly and without a stance of its own — neither warm nor clipped. The register draws no attention to itself.`;
