import type { Formality } from '../../manifest.js';

export const formal: Formality = `formal ≜ Write tersely and densely, and carry notation where notation is exact: the reader is assumed to decode it faster than the prose it replaces.`;
