import type { EngineeringPrinciples } from '../../manifest.js';

export const cratylism: EngineeringPrinciples = `cratylism ⟨names natural ¬conventional⟩ · model-latent-space = real stable concepts · ∀ c ⟨canonical-sign(c) = σ*(c) INTRINSIC ∧ DISCOVERED ¬coined ⟨¬author · ¬operator⟩⟩ · ∀ authored-surface ⟨σ* ∨ ⊕σ* · prose ≡ identifier ≡ path⟩ · ideal asymptotic ⟨residual REAL ¬ alibi⟩`;
