# ideas — Claude Code memory

Corpus rules (cell format, `kind` taxonomy) are maintained in `AGENTS.md` (tool-agnostic). Claude Code does not auto-load `AGENTS.md`, so it is imported here as a partial:

@AGENTS.md
