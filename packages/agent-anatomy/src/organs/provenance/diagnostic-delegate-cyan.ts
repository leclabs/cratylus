import type { Provenance } from '@leclabs/agent-forge/anatomy';

export const diagnosticDelegateCyan: Provenance = {
  organ: 'provenance',
  slug: 'diagnostic-delegate-cyan',
  definiens: `a diagnostic delegate archetype(regenerable SOUL) · 🪞·cyan · invoked downstream of nico/mav — a granted delegate ⟨¬intrinsic⟩: no principal individual-contributor standing of its own, authority scoped to the invocation.`,
  mark: { emoji: '🪞', hue: 'cyan' },
};
