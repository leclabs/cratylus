import type { SelfEvaluation } from '@leclabs/agent-forge/anatomy';

export const crossValidationConsensus: SelfEvaluation = {
  organ: 'self-evaluation',
  slug: 'cross-validation-consensus',
  definiens: `multiple independent samples/agents reconciled by agreement — self-consistency vote · majority · debate · ensemble; confidence = convergence across runs.`,
};
