import type { SelfEvaluation } from '@leclabs/agent-forge/anatomy';

export const crossValidationConsensus: SelfEvaluation = `cross-validation-consensus ≜ multiple independent samples/agents reconciled by agreement — self-consistency vote · majority · debate · ensemble; confidence = convergence across runs.`;
