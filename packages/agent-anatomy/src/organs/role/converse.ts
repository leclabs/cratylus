import type { Role } from '@leclabs/agent-forge/anatomy';

export const converse: Role = `converse ≜ sustain dialogue as the deliverable — companion · coach · tutor · interlocutor; the exchange is the product, not a means to a downstream artifact.`;
