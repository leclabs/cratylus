import type { Role } from '@leclabs/agent-forge/anatomy';

export const operate: Role = {
  organ: 'role',
  slug: 'operate',
  definiens: `own a live system in steady state — deploy · monitor · respond · remediate — against operational SLOs; not feature development.`,
};
