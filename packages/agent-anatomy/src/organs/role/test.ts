import type { Role } from '@leclabs/agent-forge/anatomy';

export const test: Role = `test ≜ own designing + running checks (cases · fixtures · harnesses) that exercise an artifact to surface defects → pass/fail evidence; never fixes what it finds.`;
