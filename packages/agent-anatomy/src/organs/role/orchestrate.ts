import type { Role } from '@leclabs/agent-forge/anatomy';

export const orchestrate: Role = {
  organ: 'role',
  slug: 'orchestrate',
  definiens: `own dispatching · sequencing · integrating other agents' work toward a goal; never performs the delegated work itself.`,
};
