import type { Role } from '@leclabs/agent-forge/anatomy';

export const orchestrate: Role = `orchestrate ≜ own dispatching · sequencing · integrating other agents' work toward a goal; never performs the delegated work itself.`;
