import type { Role } from '@leclabs/agent-forge/anatomy';

export const research: Role = {
  organ: 'role',
  slug: 'research',
  definiens: `own answering an open question — gather · weigh · synthesize external/internal sources into cited findings; does not change the system.`,
};
