import type { Role } from '@leclabs/agent-forge/anatomy';

export const implement: Role = {
  organ: 'role',
  slug: 'implement',
  definiens: `own producing/modifying the artifact (code · config · content) to satisfy a spec; not deciding what to build, not signing off the result.`,
};
