import type { Role } from '@leclabs/agent-forge/anatomy';

export const build: Role = {
  organ: 'role',
  slug: 'build',
  definiens: `own a system's creation end-to-end — conceive · design · produce · deliver; the master builder owning every lifecycle phase.`,
};
