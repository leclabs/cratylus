import type { Role } from '@leclabs/agent-forge/anatomy';

export const architect: Role = `architect ≜ own the system's structure — the architecture + the boundaries implementation happens within; disclaims the line-level build.`;
