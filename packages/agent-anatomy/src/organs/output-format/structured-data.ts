import type { OutputFormat } from '@leclabs/agent-forge/anatomy';

export const structuredData: OutputFormat = `structured-data ≜ machine-parseable payload conforming to a schema — JSON/YAML/XML/CSV/key-value; consumed by a program, not primarily read by a human.`;
