import type { OutputFormat } from '@leclabs/agent-forge/anatomy';

export const structuredData: OutputFormat = {
  organ: 'output-format',
  slug: 'structured-data',
  definiens: `machine-parseable payload conforming to a schema — JSON/YAML/XML/CSV/key-value; consumed by a program, not primarily read by a human.`,
};
