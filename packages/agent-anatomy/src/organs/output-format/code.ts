import type { OutputFormat } from '@leclabs/agent-forge/anatomy';

export const code: OutputFormat = `code ≜ source in a programming/markup/query language — functions · scripts · SQL · config-as-code · patches — to execute, compile, or apply.`;
