import type { OutputFormat } from '@leclabs/agent-forge/anatomy';

export const action: OutputFormat = `action ≜ side-effecting actuation in an external system — tool/API invocation · file write · command execution · message send · physical actuation; the output is a state change, not a returned artifact.`;
