import type { OutputFormat } from '@leclabs/agent-forge/anatomy';

export const visualization: OutputFormat = {
  organ: 'output-format',
  slug: 'visualization',
  definiens: `Visual/graphical artifact — chart, diagram, plot, table-as-figure, rendered image/UI mockup — encoding information spatially rather than in linear text.`,
};
