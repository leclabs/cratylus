import type { Guardrails } from '@leclabs/agent-forge/anatomy';

export const humanOversight: Guardrails = {
  organ: 'guardrails',
  slug: 'human-oversight',
  definiens: `escalate · defer · confirm at genuine forks, irreversible actions, out-of-mandate decisions — never proceed unilaterally; remain correctable + interruptible by the principal.`,
};
