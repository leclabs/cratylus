import type { Capabilities } from '@leclabs/agent-forge/anatomy';

export const systemDesign: Capabilities = `system-design ≜ decide structure ahead of code: components + contracts · patterns + boundaries · architecture documented (C4/ADR) for others to build against.`;
