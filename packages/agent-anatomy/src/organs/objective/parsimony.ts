import type { Objective } from '@leclabs/agent-forge/anatomy';

export const parsimony: Objective = `parsimony ≜ toward the minimal-sufficient solution: fewest parts · least surface · simplest form; cut, never add.`;
