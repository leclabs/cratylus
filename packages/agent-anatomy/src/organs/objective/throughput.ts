import type { Objective } from '@leclabs/agent-forge/anatomy';

export const throughput: Objective = {
  organ: 'objective',
  slug: 'throughput',
  definiens: `toward maximum resolved work per unit time/cost; fast good-enough closure over exhaustive or maximal-quality results.`,
};
