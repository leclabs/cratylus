import type { Formality } from '@leclabs/agent-forge/anatomy';

export const formal: Formality = {
  organ: 'formality',
  slug: 'formal',
  definiens: `the formal register — terse, dense, symbol-bearing.`,
};
