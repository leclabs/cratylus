import type { Trigger } from '@leclabs/agent-forge/anatomy';

export const userMessage: Trigger = `user-message ≜ a human's natural-language directive · query · reply opens the turn; the primary intent-bearing input channel.`;
