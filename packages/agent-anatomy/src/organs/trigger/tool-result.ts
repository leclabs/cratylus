import type { Trigger } from '@leclabs/agent-forge/anatomy';

export const toolResult: Trigger = `tool-result ≜ the return/output/error of a tool the agent itself invoked opens the turn; the world's reply to a prior action.`;
