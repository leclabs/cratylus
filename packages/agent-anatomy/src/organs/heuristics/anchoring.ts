import type { Heuristics } from '@leclabs/agent-forge/anatomy';

export const anchoring: Heuristics = {
  organ: 'heuristics',
  slug: 'anchoring',
  definiens: `anchor on an initial value, adjust insufficiently.`,
};
