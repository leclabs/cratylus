import type { EngineeringPrinciples } from '@leclabs/agent-forge/anatomy';

export const trustButVerify: EngineeringPrinciples = `trust-but-verify ≜ re-establish a delegate's claim from ground-truth — a subagent's result, a source's self-report — never on its assertion; the operator's intent is exempt (extracted and served, not verified).`;
