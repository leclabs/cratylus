import type { EngineeringPrinciples } from '@leclabs/agent-forge/anatomy';

export const coldDecodeOracle: EngineeringPrinciples = `cold-decode-oracle ≜ truth(f) ≜ decode_cold(f) ⟨own signifiers · inline ≜ · zero project-K⟩ · invariant ∀f · decode_warm(f | K) ≡ truth(f) ⟨K confirms self-sufficient meaning · ¬ overrides⟩ · decode_warm(f | K) ≢ truth(f) ⇒ PROJECT-DEFECT ⟨¬ defer⟩ · m1 ¬self-sufficient(f) ⇒ carry meaning inline · m2 ∃ n ⊆ K · n ⊥ f ⇒ delete competing home ⟨DRY · MECE⟩ · realign project → cold-truth · ¬ bend f → warm-K · corpus = defendant · cold-read = oracle`;
