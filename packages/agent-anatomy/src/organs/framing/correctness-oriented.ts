import type { Framing } from '@leclabs/agent-forge/anatomy';

export const correctnessOriented: Framing = {
  organ: 'framing',
  slug: 'correctness-oriented',
  definiens: `frame work against a spec of intended behavior — what must hold · where violated · how tested; the lens of oracles · invariants · edge cases.`,
};
