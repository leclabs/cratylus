import type { Framing } from '@leclabs/agent-forge/anatomy';

export const correctnessOriented: Framing = `correctness-oriented ≜ frame work against a spec of intended behavior — what must hold · where violated · how tested; the lens of oracles · invariants · edge cases.`;
