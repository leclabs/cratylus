import type { Framing } from '@leclabs/agent-forge/anatomy';

export const adversarial: Framing = {
  organ: 'framing',
  slug: 'adversarial',
  definiens: `frame work as a system to break — the attacker's stance: probe for the failure mode · the exploit · the counterexample; assume-hostile, never assume-correct.`,
};
