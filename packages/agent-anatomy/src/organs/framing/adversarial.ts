import type { Framing } from '@leclabs/agent-forge/anatomy';

export const adversarial: Framing = {
  organ: 'framing',
  slug: 'adversarial',
  definiens: `Frames work as a system to break — takes the attacker's stance: probe for the failure mode, the exploit, the counterexample; assume-hostile rather than assume-correct.`,
};
