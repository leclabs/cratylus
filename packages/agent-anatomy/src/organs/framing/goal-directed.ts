import type { Framing } from '@leclabs/agent-forge/anatomy';

export const goalDirected: Framing = `goal-directed ≜ frame work as a concrete objective — the end-state + shortest viable path; analysis instrumental to delivery.`;
