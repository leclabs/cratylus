import type { Framing } from '@leclabs/agent-forge/anatomy';

export const firstPrinciples: Framing = `first-principles ≜ frame work stripped to fundamentals: question inherited assumptions; reason up from primitives + constraints, never from convention · precedent · analogy.`;
