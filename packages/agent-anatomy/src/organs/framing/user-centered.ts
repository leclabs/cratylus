import type { Framing } from '@leclabs/agent-forge/anatomy';

export const userCentered: Framing = {
  organ: 'framing',
  slug: 'user-centered',
  definiens: `Frames work from the affected human's goals, mental model, and experience; abstracts requirements from need rather than implementation.`,
};
