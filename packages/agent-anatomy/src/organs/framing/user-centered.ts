import type { Framing } from '@leclabs/agent-forge/anatomy';

export const userCentered: Framing = {
  organ: 'framing',
  slug: 'user-centered',
  definiens: `frame work from the affected human's goals · mental model · experience; requirements abstracted from need, not implementation.`,
};
