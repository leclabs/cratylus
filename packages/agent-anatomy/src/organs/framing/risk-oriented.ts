import type { Framing } from '@leclabs/agent-forge/anatomy';

export const riskOriented: Framing = {
  organ: 'framing',
  slug: 'risk-oriented',
  definiens: `frame work through what can fail · break · be exploited — failure modes · edge cases · blast radius · worst-case before benefits.`,
};
