import type { Framing } from '@leclabs/agent-forge/anatomy';

export const decompositional: Framing = {
  organ: 'framing',
  slug: 'decompositional',
  definiens: `Frame work as a structure to be broken into parts: identify components, dependencies, and seams, then sequence subproblems; default lens for planning, scoping, and tractability.`,
};
