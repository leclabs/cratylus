import type { Framing } from '@leclabs/agent-forge/anatomy';

export const decompositional: Framing = `decompositional ≜ frame work as structure to break into parts — components · dependencies · seams — then sequence subproblems; the default lens for planning · scoping · tractability.`;
