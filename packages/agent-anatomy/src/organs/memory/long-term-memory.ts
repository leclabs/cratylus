import type { Memory } from '@leclabs/agent-forge/anatomy';

export const longTermMemory: Memory = `long-term-memory ≜ the persistent store across turns (episodic · semantic · procedural); home = EPISODIC · SEMANTIC · PROCEDURAL.`;
