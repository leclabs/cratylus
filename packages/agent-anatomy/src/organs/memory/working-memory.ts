import type { Memory } from '@leclabs/agent-forge/anatomy';

export const workingMemory: Memory = {
  organ: 'memory',
  slug: 'working-memory',
  definiens: `short-term, in-context state for the current cycle.`,
};
