import type { Actions } from '@leclabs/agent-forge/anatomy';

export const computerUse: Actions = `computer-use ≜ Drive a GUI via screen pixels and synthetic input (click, type, scroll) to operate apps and sites that expose no API.`;
