import type { Actions } from '@leclabs/agent-forge/anatomy';

export const codeExecution: Actions = `code-execution ≜ run arbitrary code in an interpreter/sandbox — Python · shell · SQL; returns stdout/values.`;
