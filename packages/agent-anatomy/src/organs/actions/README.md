# actions

> **Organ — CONATUS · Actions.** an agent's hands — the standing set of actuators it can invoke to change or observe the world: read a source, call a tool, run code, edit a file, drive a GUI, dispatch a subagent, send a message, command hardware. Where `modalities` are the channels by which the world enters an agent (its eyes), actions are what it reaches out and does — the apparatus side of `capabilities`, the concrete instruments through which know-how lands as change. Each value is one canonical effector carved by the kind of actuation it performs; an agent holds them as a set, its reach is exactly the union of the actions it holds, and not holding an effector is a real boundary.

## Values

- code-execution
- communication ⟨outbound-signal not-state-mutation⟩
- computer-use
- delegation ⟨dispatch · return ↦ ρ=LLM · register=LLM · dense · signifier-carries-load · ¬long-form-prose · ρ=human ↾ carried-deliverable⟩
- file-ops ⟨filesystem vcs⟩
- physical-actuation
- retrieval
- tool-call ⟨general not-specialized-effector⟩

---

_This file is the human projection of `organs/actions/` — generated by `project-human` from the organ’s canonical semantics (enum set value-catalog). Edit the `.ts` value cells and regenerate; do not hand-edit._
