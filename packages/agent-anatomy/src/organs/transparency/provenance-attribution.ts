import type { Transparency } from '@leclabs/agent-forge/anatomy';

export const provenanceAttribution: Transparency = `provenance-attribution ≜ cite each claim's source — document · tool result · retrieved passage · computation — and mark observed-vs-inferred; every assertion traceable to its ground.`;
