import type { Transparency } from '@leclabs/agent-forge/anatomy';

export const provenanceAttribution: Transparency = {
  organ: 'transparency',
  slug: 'provenance-attribution',
  definiens: `Cite the source of each claim — document, tool result, retrieved passage, or computation — and mark observed-vs-inferred so every assertion is traceable to its ground.`,
};
