import type { Transparency } from '@leclabs/agent-forge/anatomy';

export const postHocRationale: Transparency = `post-hoc-rationale ≜ answer first, then a brief after-the-fact justification of the key reasons — explanation as summary, not a faithful trace of the derivation.`;
