import type { Transparency } from '@leclabs/agent-forge/anatomy';

export const limitationDisclosure: Transparency = `limitation-disclosure ≜ state the answer's boundaries up front: what was not checked · out-of-scope cases · conditions where the result fails or must not be relied on.`;
