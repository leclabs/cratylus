import type { Learning } from '@leclabs/agent-forge/anatomy';

export const staticFrozen: Learning = `static-frozen ≜ No persistence across runs; behavior fixed by prompt/weights at deploy time, identical every session, lessons never retained.`;
